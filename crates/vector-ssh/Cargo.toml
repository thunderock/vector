[package]
name = "vector-ssh"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Async SSH client — PTY channel, port-forward, agent — Phase 7 (russh)."

[dependencies]
[lints]
workspace = true
