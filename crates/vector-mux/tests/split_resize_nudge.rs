//! WIN-03: Cmd-Shift-Arrow 1-cell ratio shift (D-60).
//! Plan 04-02 un-ignores and fills.

#[test]
#[ignore = "Wave-0 stub: Plan 04-02"]
fn cmd_shift_arrow_nudges_ratio_one_cell() {
    // Plan 04-02: HSplit ratio 40:40 -> Mux::nudge_split(focused_pane, Direction::Right)
    // -> ratio 41:39. Repeat 100x -> assert min size floor (20 cells) enforced.
    panic!("Wave-0 stub — implemented by Plan 04-02");
}
