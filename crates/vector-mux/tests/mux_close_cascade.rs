//! WIN-02: Cmd-W cascade pane -> tab -> window -> quit (D-61).
//! Plan 04-02 un-ignores and fills.

#[test]
#[ignore = "Wave-0 stub: Plan 04-02"]
fn cmd_w_cascade_pane_tab_window_quit() {
    // Plan 04-02: enumerate the 4 cascade states and assert the post-close mux
    // topology + an `exit_requested: bool` flag on a test harness.
    panic!("Wave-0 stub — implemented by Plan 04-02");
}
