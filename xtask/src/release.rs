// release subcommand lands in Task 2; placeholder here so main.rs compiles.
use anyhow::Result;
use xshell::Shell;

pub fn release(_sh: &Shell) -> Result<()> {
    anyhow::bail!("release subcommand lands in Task 2 of plan 01-04");
}
