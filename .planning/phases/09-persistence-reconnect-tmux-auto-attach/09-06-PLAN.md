---
phase: 09-persistence-reconnect-tmux-auto-attach
plan: 06
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - crates/vector-tunnels/tests/live_devtunnel_smoke.rs
  - .github/workflows/ci.yml
  - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md
autonomous: false
requirements: [PERSIST-04]
must_haves:
  truths:
    - "Live e2e test file exercises a real Dev Tunnel + USER-STARTED tmux 3.4+ on the remote (the test never runs `tmux new` / `tmux attach` / `tmux set-option`)"
    - "Tests cover OSC 52 round-trip (DCS-wrapped through user-managed tmux), DECSCUSR cursor shapes, mouse modes 1000/1002/1003 with SGR 1006, and `TERM=xterm-256color` advertisement"
    - "OSC 52 test fails fast with a clear message if `$TMUX` is empty on the remote (proves the test refused to bootstrap tmux itself)"
    - "All three tests are `#[ignore]`d and gated on the `VECTOR_E2E_TUNNEL_ID` env var (no impact on default `cargo test --workspace`)"
    - "A new CI job `persist-e2e` mirrors the existing `tmux-smoke` shape: `continue-on-error: true`, runs `-- --ignored` only when the `VECTOR_E2E_TUNNEL_ID` repository secret is present"
    - "09-SMOKE.md skeleton is created by the executor and includes a USER-RUN setup section instructing the user to run `tmux new -s smoke; tmux set-option -g allow-passthrough on` on the remote BEFORE running the smoke tests"
    - "The PERSIST-04 verification is recorded in 09-SMOKE.md, signed off by the user (separate checkpoint task)"
  artifacts:
    - path: "crates/vector-tunnels/tests/live_devtunnel_smoke.rs"
      provides: "Live e2e test bodies for PERSIST-04 (no Vector-managed tmux)"
      min_lines: 100
    - path: ".github/workflows/ci.yml"
      provides: "persist-e2e CI job (continue-on-error)"
      contains: "persist-e2e:"
    - path: ".planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md"
      provides: "Manual UAT script + USER-RUN tmux setup + user sign-off"
      contains: "PERSIST-04"
  key_links:
    - from: "crates/vector-tunnels/tests/live_devtunnel_smoke.rs"
      to: "VECTOR_E2E_TUNNEL_ID env var"
      via: "std::env::var(\"VECTOR_E2E_TUNNEL_ID\")"
      pattern: "VECTOR_E2E_TUNNEL_ID"
    - from: ".github/workflows/ci.yml"
      to: "crates/vector-tunnels/tests/live_devtunnel_smoke.rs"
      via: "cargo test -p vector-tunnels --test live_devtunnel_smoke -- --ignored"
      pattern: "live_devtunnel_smoke"
---

<objective>
Close Phase 9 by validating PERSIST-04: tmux pass-through correctness over a real Dev Tunnels relay, with the user running tmux themselves on the remote (CONTEXT D-04/D-05 — Vector does not detect, create, attach, share, or name tmux sessions). Fill in the three `#[ignore]`d test bodies in `live_devtunnel_smoke.rs`, add the `persist-e2e` CI job (mirroring `tmux-smoke`), create the `09-SMOKE.md` skeleton with USER-RUN setup instructions, and gate phase close on user sign-off of the smoke matrix.

Purpose: PERSIST-01..03 are exercised by automation that runs in the default test suite. PERSIST-04 cannot run in CI without a live tunnel, so it is the same `#[ignore]` + env-var gate Phase 5 used for `osc52_tmux.rs`. The CI job is `continue-on-error: true` — if no `VECTOR_E2E_TUNNEL_ID` secret is configured, it's a no-op; with the secret, it runs against a Microsoft-hosted Dev Tunnels relay + user's own Linux box.

Output:
- `live_devtunnel_smoke.rs` with three real test bodies (no `unimplemented!()`). The OSC 52 test PRE-CHECKS that `$TMUX` is set in the remote shell and fails fast if not — it never starts tmux itself.
- New CI job `persist-e2e` in `.github/workflows/ci.yml`.
- `09-SMOKE.md` skeleton with USER-RUN setup section (created by the executor).
- User sign-off recorded inside `09-SMOKE.md` after running the smoke matrix against their own tunnel + own tmux session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-CONTEXT.md
@.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-RESEARCH.md
@.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-VALIDATION.md
@crates/vector-term/tests/osc52_tmux.rs
@.github/workflows/ci.yml
@crates/vector-tunnels/src/transport.rs
@crates/vector-tunnels/src/domain.rs

<interfaces>
Reference patterns:
- `crates/vector-term/tests/osc52_tmux.rs` — the Phase 5 `#[ignore]`d test that runs tmux locally and asserts OSC 52 round-trips. Mirror its `#[ignore]` annotation and env-var gating shape. NOTE: Phase 5's local test starts tmux itself because it owns the local environment. Phase 9 is different — Vector does NOT manage tmux on the remote; the USER manages it. The tests REFUSE to run unless the user has already prepared a tmux session.
- `.github/workflows/ci.yml` lines 97-113 — the existing `tmux-smoke` CI job pattern (`continue-on-error: true`, manual `cargo test ... -- --ignored`).
- `crates/vector-tunnels/src/domain.rs::connect_tunnel` — programmatically obtains a live `Box<dyn PtyTransport>` against a real Dev Tunnel.

Env var contract:
- `VECTOR_E2E_TUNNEL_ID`: the tunnel id (e.g. `abc-123xyz`) on which a `vector-tunnel-agent` is running. Empty / unset → test exits early via `eprintln!` (do NOT `panic` — `#[ignore]` already excludes the test from default runs; the early-exit is for `--ignored` runs without the env var set).

Microsoft auth for the test: the test cannot run a device-flow OAuth from CI. Solution: read a pre-minted access token from `VECTOR_E2E_MICROSOFT_TOKEN` env var. CI provides this as a repo secret. The local dev path: user runs `cargo test ... -- --ignored` after `vector` itself is signed-in, then exports the cached Keychain token via `VECTOR_E2E_MICROSOFT_TOKEN=$(security find-generic-password -s vector.microsoft -w)` (or whatever the existing service name is — check `crates/vector-tunnels/src/auth.rs` for the Keychain account string).

User-managed tmux contract (CONTEXT D-04/D-05):
- The user must run `tmux new -s smoke` (and optionally `tmux set-option -g allow-passthrough on`) on the remote BEFORE running the smoke tests.
- The OSC 52 test probes `$TMUX` in the remote shell. If empty, the test FAILS FAST with: `"tmux must be running on remote before running this smoke test — see 09-SMOKE.md setup"`.
- Tests NEVER issue `tmux new`, `tmux attach`, `tmux set-option`, `tmux kill-session`, or any other `tmux` control command.

OSC 52 outbound chunking: Phase 5 D-71 chunks at 58 bytes. The test sends a 200-byte payload to verify multi-chunk reassembly through the relay (Pitfall 7 in 09-RESEARCH.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill in three live e2e test bodies in live_devtunnel_smoke.rs (user-managed tmux contract)</name>
  <files>crates/vector-tunnels/tests/live_devtunnel_smoke.rs</files>
  <read_first>
    - crates/vector-tunnels/tests/live_devtunnel_smoke.rs (Plan-01 stubs)
    - crates/vector-term/tests/osc52_tmux.rs (template `#[ignore]` + env-var gating pattern; IGNORE its in-test `tmux new` calls — those are local-only)
    - crates/vector-tunnels/src/domain.rs (`connect_tunnel` signature)
    - crates/vector-tunnels/src/api.rs (`DevTunnelsApi::new` + `list_tunnels` + `get_tunnel`)
    - crates/vector-tunnels/src/model.rs (`AuthProvider::Microsoft(String)`, `TunnelRecord`)
    - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-VALIDATION.md (test rows 9-06-01..03 — note: re-keyed to Plan 06 in the same revision pass)
    - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-CONTEXT.md (D-04/D-05 — Vector does not detect, create, attach, share, or name tmux sessions; D-06 reframed PERSIST-04 list — DCS OSC 52, DECSCUSR, mouse modes 1000/1002/1003 with SGR 1006, TERM advertisement)
  </read_first>
  <action>
    REPLACE the contents of `crates/vector-tunnels/tests/live_devtunnel_smoke.rs`. Each test:
    - Starts with `let Ok(tunnel_id) = std::env::var("VECTOR_E2E_TUNNEL_ID") else { eprintln!("VECTOR_E2E_TUNNEL_ID unset; skipping"); return; };`
    - Reads `VECTOR_E2E_MICROSOFT_TOKEN` the same way (early-return if missing).
    - Builds `AuthProvider::Microsoft(token)`, constructs `DevTunnelsApi::new(...)`, calls `api.list_tunnels(&auth).await` to find the `TunnelRecord` for `tunnel_id`.
    - Calls `connect_tunnel(&api, &auth, &tunnel, 24, 80).await?` to get a `Box<dyn PtyTransport>`.
    - Drives the transport: write commands, read back output until matching expected bytes, assert.
    - Cleans up by dropping the transport.

    **CRITICAL — user-managed tmux contract (CONTEXT D-04/D-05):** None of the three tests may issue `tmux new`, `tmux attach`, `tmux set-option`, `tmux kill-session`, or any other `tmux` control command. The OSC 52 test pre-checks that the user has tmux running and fails fast if not. The other two tests do not interact with tmux at all (they exercise terminal-level sequences directly on the remote shell).

    Test bodies:

    1. **`osc52_round_trip`** (PERSIST-04 #1, Pitfall 7):
       - Connect via `connect_tunnel(...)`. Set up a deterministic prompt: send `PS1='READY> '\n` then `read_until` `READY> `.
       - **PRE-CHECK** `$TMUX` is set: send `printf '%s' "$TMUX"\n` and read the response. Assert the response is NON-EMPTY (the user is inside a tmux session). If empty, `panic!("tmux must be running on remote before running this smoke test — see 09-SMOKE.md setup")`. This fails fast and tells the user how to fix it.
       - **DO NOT** issue any `tmux new`, `tmux attach`, or `tmux set-option` commands. The user is responsible for `set-option -g allow-passthrough on` (documented in 09-SMOKE.md skeleton from Task 3a).
       - Send a 200-byte payload via OSC 52 (DCS-wrapped form per Phase 5 D-71):
         `printf '\\eP\\e]52;c;%s\\a\\e\\\\' "$(printf '%200s' x | tr ' ' x | base64)"\n`
       - Send `tmux show-buffer | base64\n` then read until the buffer contents return.
       - Assert the b64 contents match the original 200-`x` payload. Confirms multi-chunk DCS reassembly through the relay and through the user's tmux.

    2. **`decscusr_and_mouse_modes`** (PERSIST-04 #2):
       - Connect via `connect_tunnel(...)`. (NO `$TMUX` check — these sequences are terminal-level, not tmux-level.)
       - Send `printf '\\e[3 q'\n` to request a bar cursor (DECSCUSR 3). Wait. (No direct way to read the cursor shape back — this test asserts no error / no hang; the visual side is manual.)
       - Send `printf '\\e[?1000h\\e[?1006h'\n` to enable mouse 1000 + SGR 1006. Send `printf '\\e[?1002h'\n` for 1002 (button-event tracking). Send `printf '\\e[?1003h'\n` for 1003 (any-event tracking).
       - Send `stty -a 2>&1 | head -1\n` and read back the terminal-attributes string. Assert it contains `rows 24` and `columns 80` (proves window size propagated through `open_pty` on connect).
       - This is a smoke-pass test — confirms the sequences are accepted by the remote shell without error. Visual verification is the manual UAT in Task 3b below.

    3. **`term_xterm_256color_advertised`** (PERSIST-04 #4):
       - Connect via `connect_tunnel(...)`.
       - Send `printf %s "$TERM"\n` and read until newline.
       - Assert exact equality with `"xterm-256color"` (the value `vector-tunnel-agent` sets per Phase 8 contract).

    Each test wraps the body in `#[tokio::test(flavor = "multi_thread")] #[ignore = "live e2e — requires VECTOR_E2E_TUNNEL_ID + VECTOR_E2E_MICROSOFT_TOKEN"]`. Use `tokio::time::timeout(Duration::from_secs(30), ...)` around every `read` to fail fast on hangs.

    Helper functions (module-private):
    - `async fn connect_and_login(api, auth, tunnel) -> (Box<dyn PtyTransport>, ())` — runs `connect_tunnel`, sends `PS1='READY> '\n` so we have a deterministic prompt for `read_until_prompt`.
    - `async fn read_until(transport: &mut Box<dyn PtyTransport>, marker: &[u8], timeout: Duration) -> Result<Vec<u8>>` — drains the reader until `marker` appears.

    Remove every `#[ignore]` annotation that pointed to "Wave 0 — implemented in Plan 09-06"; replace with the live-e2e `#[ignore]` annotation noted above. The default `cargo test --workspace` MUST still skip these tests.
  </action>
  <verify>
    <automated>cargo test -p vector-tunnels --test live_devtunnel_smoke -- --list 2>&1 | grep -E "osc52_round_trip|decscusr_and_mouse_modes|term_xterm_256color_advertised" | wc -l && ! grep -E 'tmux (new|set-option|attach|kill-session)' crates/vector-tunnels/tests/live_devtunnel_smoke.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/vector-tunnels/tests/live_devtunnel_smoke.rs` has at least 100 lines.
    - `grep -c "VECTOR_E2E_TUNNEL_ID" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns at least 3 (one per test).
    - `grep -c "VECTOR_E2E_MICROSOFT_TOKEN" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns at least 3.
    - `grep -c "#\\[ignore" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns 3.
    - `grep -c "Wave 0 — implemented in Plan 09-06" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns 0 (old stub annotation removed).
    - **`! grep -E 'tmux (new|set-option|attach|kill-session)' crates/vector-tunnels/tests/live_devtunnel_smoke.rs`** — the test file MUST NOT contain any `tmux new`, `tmux set-option`, `tmux attach`, or `tmux kill-session` command strings (CONTEXT D-04/D-05 absence assertion). Note: `tmux show-buffer` IS allowed (it's a read-only query, not a state-changing tmux control command).
    - `grep -n "tmux must be running on remote" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns a match (the fail-fast message in `osc52_round_trip`).
    - `grep -n "\\$TMUX\\|printf '%s' \"\\$TMUX\"" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns at least one match (the $TMUX pre-check probe).
    - `cargo test -p vector-tunnels --test live_devtunnel_smoke -- --list` shows three test functions.
    - `cargo test -p vector-tunnels --test live_devtunnel_smoke` reports `3 passed; 0 failed; 3 ignored` on default runs (no env vars, tests skipped via `#[ignore]`).
    - `grep -n "tokio::time::timeout" crates/vector-tunnels/tests/live_devtunnel_smoke.rs` returns at least one match (hang-resistance).
  </acceptance_criteria>
  <done>Three live e2e test bodies exist, are `#[ignore]`d by default, gated on the two env vars, and structured to fail fast (≤ 30 s timeouts). The OSC 52 test pre-checks `$TMUX` and refuses to bootstrap tmux itself. The test file contains zero `tmux new` / `tmux set-option` / `tmux attach` / `tmux kill-session` strings.</done>
</task>

<task type="auto">
  <name>Task 2: Add persist-e2e CI job mirroring tmux-smoke pattern</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file; especially the `tmux-smoke:` job at lines 97-113)
    - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-RESEARCH.md (CI gating pattern; Pitfall 8 — live smoke test rig doesn't exist in CI without secrets)
  </read_first>
  <action>
    Append a new CI job after `tmux-smoke` (or in whichever section keeps related jobs together):

    ```yaml
      # Phase 9 PERSIST-04 live e2e: real Dev Tunnels relay + user-managed tmux on a
      # remote Linux box running `vector-tunnel-agent`. Not branch-protection required.
      # continue-on-error: requires VECTOR_E2E_TUNNEL_ID + VECTOR_E2E_MICROSOFT_TOKEN
      # repository secrets to be set; otherwise the tests #[ignore] themselves.
      persist-e2e:
        needs: [test]
        runs-on: macos-14
        continue-on-error: true
        env:
          VECTOR_E2E_TUNNEL_ID: ${{ secrets.VECTOR_E2E_TUNNEL_ID }}
          VECTOR_E2E_MICROSOFT_TOKEN: ${{ secrets.VECTOR_E2E_MICROSOFT_TOKEN }}
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@1.88.0
            with:
              targets: aarch64-apple-darwin
          - uses: Swatinem/rust-cache@v2
            with:
              shared-key: ci-persist-e2e
          - name: Skip when secrets absent
            if: env.VECTOR_E2E_TUNNEL_ID == '' || env.VECTOR_E2E_MICROSOFT_TOKEN == ''
            run: echo "VECTOR_E2E_* secrets unset; skipping live smoke test."
          - name: Run live e2e
            if: env.VECTOR_E2E_TUNNEL_ID != '' && env.VECTOR_E2E_MICROSOFT_TOKEN != ''
            run: cargo test -p vector-tunnels --test live_devtunnel_smoke -- --ignored --test-threads=1
    ```

    Match the indentation + style of the existing `tmux-smoke` block exactly. Do NOT add this job to branch protection requirements.
  </action>
  <verify>
    <automated>grep -n "persist-e2e:" .github/workflows/ci.yml && yamllint .github/workflows/ci.yml 2>&1 | head -20 || cat .github/workflows/ci.yml | grep -A 20 "persist-e2e:"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "persist-e2e:" .github/workflows/ci.yml` returns exactly one match.
    - `grep -n "VECTOR_E2E_TUNNEL_ID:" .github/workflows/ci.yml` returns at least one match (in the env block).
    - `grep -n "continue-on-error: true" .github/workflows/ci.yml` returns at least two matches (`tmux-smoke` + `persist-e2e`).
    - `grep -n "cargo test -p vector-tunnels --test live_devtunnel_smoke" .github/workflows/ci.yml` returns exactly one match.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` succeeds (YAML parses cleanly).
  </acceptance_criteria>
  <done>The `persist-e2e` CI job exists, mirrors `tmux-smoke`'s `continue-on-error: true` pattern, gates execution on the presence of both repository secrets, and never blocks merges.</done>
</task>

<task type="auto">
  <name>Task 3a: Create 09-SMOKE.md skeleton with USER-RUN tmux setup section</name>
  <files>.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md</files>
  <read_first>
    - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-CONTEXT.md (D-04/D-05 — user owns tmux lifecycle; D-06 reframed PERSIST-04 matrix)
    - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-VALIDATION.md (manual-only verifications + automated matrix mapping)
  </read_first>
  <action>
    CREATE `.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` with the skeleton below. The file is structured so Task 3b (the human-verify checkpoint) is a pure user action: fill checkboxes, sign off. The executor's only responsibility here is creating the skeleton — do not pre-fill any checkboxes, do not fabricate a sign-off, and do not edit the file again until the user replies with results.

    Skeleton contents:

    ```markdown
    # Phase 9 SMOKE — PERSIST-04 + reconnect end-to-end

    **User:** astiwari
    **Date:** {YYYY-MM-DD — user fills}
    **Tunnel id:** {redacted-on-publish}
    **Remote OS:** Ubuntu 22.04 (or your equivalent)
    **Remote tmux:** 3.4+ (`tmux -V` must report ≥ 3.4)
    **Remote agent:** `vector-tunnel-agent` running (Phase 8 install procedure)

    ## USER-RUN setup (REQUIRED before any test below)

    CONTEXT D-04/D-05: Vector does not detect, create, attach, share, or name tmux sessions. The user must prepare tmux on the remote BEFORE running any smoke test.

    On the remote box (separate SSH session, NOT via Vector), run:

    ```
    tmux new -s smoke
    tmux set-option -g allow-passthrough on
    ```

    Leave that tmux session attached. The smoke tests connect a new shell into the same tunnel; the OSC 52 round-trip test pre-checks `$TMUX` and aborts with a clear error if you skip this setup.

    ## Automated portion (PERSIST-04)

    Set env vars locally:
    ```
    export VECTOR_E2E_TUNNEL_ID=<your-tunnel-id>
    export VECTOR_E2E_MICROSOFT_TOKEN=<from-keychain>
    ```
    Run:
    ```
    cargo test -p vector-tunnels --test live_devtunnel_smoke -- --ignored --test-threads=1 --nocapture
    ```

    | Test | Result |
    |------|--------|
    | osc52_round_trip                  | ⬜ pass / ❌ fail / 📝 notes |
    | decscusr_and_mouse_modes          | ⬜ pass / ❌ fail / 📝 notes |
    | term_xterm_256color_advertised    | ⬜ pass / ❌ fail / 📝 notes |

    ## Manual portion (full reconnect UX, end-to-end)

    Connect to the tunnel via Vector UI (Cmd-Shift-T → pick tunnel). The user's tmux session from "USER-RUN setup" above is already running on the remote.

    | # | Step | Expected | Actual |
    |---|------|----------|--------|
    | 1 | Inside the user's tmux session on the remote, run `vim hello.txt` | vim opens, cursor visible | ⬜ |
    | 2 | Type some text, save with `:wq` | file written | ⬜ |
    | 3 | Run `htop` inside the same tmux session | htop renders correctly w/ colors + ProMotion-smooth scrolling | ⬜ |
    | 4 | OSC 52 inside tmux: `printf '\\e]52;c;%s\\a' "$(echo hello | base64)"` | macOS clipboard contains `hello` | ⬜ |
    | 5 | OSC 52 large payload: `printf '\\e]52;c;%s\\a' "$(head -c 200 /dev/urandom | base64)"` | macOS clipboard contains 200 bytes of base64 (verify by `pbpaste | base64 -d | wc -c`) | ⬜ |
    | 6 | DECSCUSR cursor shapes: `printf '\\e[1 q'` (blink block), `printf '\\e[3 q'` (blink underline), `printf '\\e[5 q'` (blink bar) | Cursor shape visibly changes in Vector | ⬜ |
    | 7 | Mouse mode SGR 1006: paste `printf '\\e[?1000h\\e[?1006h'`, click in the terminal area | Vector sends SGR 1006 sequences to the remote (visible via `cat` if shell echoes) | ⬜ |
    | 8 | `tput cols && tput lines` | reports the actual viewport size | ⬜ |
    | 9 | echo $TERM | reports `xterm-256color` | ⬜ |
    | 10 | **Reconnect:** with htop still running inside the user's tmux, in a separate SSH session run `pkill -f vector-tunnel-agent` | Vector inline status bar appears; tab title flips to `[reconnecting]`; cursor stops blinking | ⬜ |
    | 11 | Wait 8 s. Try typing. | Toast `Input ignored — reconnecting` appears once; subsequent keystrokes silent-drop | ⬜ |
    | 12 | Restart `vector-tunnel-agent` on remote | Vector reconnects within next backoff slot; tab title returns to `[remote]`; reattach to user's tmux: `tmux attach -t smoke` shows htop STILL RUNNING (user's tmux session persisted) | ⬜ |
    | 13 | During a forced disconnect (re-run step 10), close pane with Cmd-W during reconnect attempt | Pane closes immediately (does NOT wait for next backoff slot) | ⬜ |

    ## Sign-off

    - [ ] USER-RUN tmux setup completed before any test.
    - [ ] All automated tests pass.
    - [ ] All manual matrix items pass (or known-deviations documented inline).
    - [ ] PERSIST-04 acceptance: tmux pass-through correctness verified end-to-end through Dev Tunnels relay with user-managed tmux.

    **Approved by:** ___________  **Date:** ___________
    ```

    After writing the skeleton, this task is done. Do NOT run the smoke matrix — that is Task 3b (user-only checkpoint).
  </action>
  <verify>
    <automated>test -f .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` exists.
    - `grep -n "USER-RUN setup" .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` returns at least one match.
    - `grep -n "tmux new -s smoke" .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` returns at least one match (USER-RUN setup instructs the user, not Vector).
    - `grep -n "tmux set-option -g allow-passthrough on" .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` returns at least one match.
    - `grep -n "PERSIST-04" .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` returns at least one match.
    - `grep -n "Sign-off" .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` returns exactly one match.
    - `grep -c "Approved by:" .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` returns 1 (skeleton has the sign-off line, not pre-filled).
    - `cargo build --workspace` succeeds (no-op verify confirming this docs-only task did not break the build).
  </acceptance_criteria>
  <done>09-SMOKE.md exists with the USER-RUN setup section, automated matrix, manual matrix, and a blank sign-off block. The executor did NOT pre-fill any results; Task 3b (the human checkpoint) is what fills + signs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3b: User runs the smoke matrix + signs off in 09-SMOKE.md</name>
  <files>.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md</files>
  <action>This is a checkpoint task — the user runs the matrix from Task 3a's skeleton, fills the checkboxes + actual results in the document, and replies `approved`. The executor does not run the tests or fill the file; it only commits the user's edits after `approved` is signaled.</action>
  <verify>09-SMOKE.md has every row of the manual + automated matrix filled with a pass/fail/notes mark and the sign-off block checked + dated. User has replied `approved`.</verify>
  <done>Phase 9 closes with PERSIST-04 verified end-to-end and 09-SMOKE.md committed with the user's sign-off.</done>
  <what-built>
    PERSIST-04 verification surface:
    - Three `#[ignore]`d live e2e tests in `crates/vector-tunnels/tests/live_devtunnel_smoke.rs` (Task 1)
    - `persist-e2e` CI job (no-op without secrets) (Task 2)
    - 09-SMOKE.md skeleton with USER-RUN tmux setup section (Task 3a)
    This task is where the user actually runs the matrix.
  </what-built>
  <how-to-verify>
    The user runs the full smoke matrix on their own remote box and records pass/fail per item in `.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` (created by Task 3a).

    **Step 1 (user, in a separate SSH session — NOT via Vector):** Prepare tmux on the remote per the "USER-RUN setup" section of 09-SMOKE.md:
    ```
    tmux new -s smoke
    tmux set-option -g allow-passthrough on
    ```
    Leave that session attached.

    **Step 2 (user, locally):** Set env vars and run the automated portion:
    ```
    export VECTOR_E2E_TUNNEL_ID=<your-tunnel-id>
    export VECTOR_E2E_MICROSOFT_TOKEN=$(security find-generic-password -s vector.microsoft -w)
    cargo test -p vector-tunnels --test live_devtunnel_smoke -- --ignored --test-threads=1 --nocapture
    ```
    Record results in the "Automated portion" table.

    **Step 3 (user):** Walk the "Manual portion" matrix rows 1-13. Connect to the tunnel via Vector UI (Cmd-Shift-T → pick tunnel), and inside the pane use the existing user-managed `smoke` tmux session. For each row, record actual results in the table.

    **Step 4 (user):** Tick the four "Sign-off" checkboxes. Fill in "Approved by:" and "Date:".

    **Step 5 (executor after user replies):** Commit the SMOKE.md edits.

    Reply `approved` once the matrix is filled and the sign-off block is checked, or describe any failures so they can be turned into gap-closure tasks.
  </how-to-verify>
  <resume-signal>Type `approved` (after filling in 09-SMOKE.md) or describe failures (which will gate a Plan 09-07 gap-closure).</resume-signal>
</task>

</tasks>

<verification>
- `cargo test -p vector-tunnels --test live_devtunnel_smoke` (default flags, no env vars) reports `0 passed; 0 failed; 3 ignored`.
- `cargo test -p vector-tunnels --test live_devtunnel_smoke -- --list` shows three test functions.
- `! grep -E 'tmux (new|set-option|attach|kill-session)' crates/vector-tunnels/tests/live_devtunnel_smoke.rs` — the test file contains ZERO Vector-managed tmux commands (D-04/D-05 absence assertion at the test layer).
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` parses without error.
- `.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md` exists with the USER-RUN setup section and the sign-off block.
- Manual UAT (Task 3b) returns `approved`.
- `grep -RIn "tmux new -A" crates/` is still empty (PERSIST-03 absence holds through phase close).
</verification>

<success_criteria>
PERSIST-04 is exercised by three `#[ignore]`d live e2e tests + a manual UAT signed off by the user. The OSC 52 test pre-checks `$TMUX` instead of bootstrapping tmux itself (honoring CONTEXT D-04/D-05). The 09-SMOKE.md skeleton instructs the user to run tmux themselves before any test. The CI job exists as a no-op when secrets aren't present and runs the live tests when they are. Phase 9 closes with all four PERSIST requirements verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-06-SUMMARY.md`. Mark the phase ready for `/gsd:verify-work`.
</output>
