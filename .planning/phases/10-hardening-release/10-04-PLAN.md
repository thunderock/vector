---
phase: 10-hardening-release
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - Cargo.toml
  - README.md
  - .github/workflows/release.yml
  - .planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md
autonomous: false
requirements: [HARDEN-04]
must_haves:
  truths:
    - "Tag v1.0.0 publishes the Universal Vector.dmg to GitHub Releases with hand-written notes per D-18"
    - "DMG asset name is exactly Vector-1.0.0-universal.dmg per D-15"
    - "A companion .sha256 checksum file is uploaded alongside the DMG per D-16"
    - "README first content section is `## Install` with the xattr + open copy-paste block per D-17"
    - "Workspace version is bumped from CalVer 2026.5.10 to 1.0.0 per D-31 so DMG name reflects the tag (Pitfall C)"
    - "The release-cut task is gated on PERSIST-04 = Complete per D-25 (hard halt, no soft-fail)"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace.package.version = 1.0.0 (D-31)"
    - path: "README.md"
      provides: "## Install as section 1 with xattr + open block + ### Why the xattr step? subsection (D-17)"
    - path: ".github/workflows/release.yml"
      provides: ".sha256 sidecar upload + hand-written RELEASE-NOTES-1.0.0.md for v1.0.0 only (D-16, D-18, D-19)"
    - path: ".planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md"
      provides: "Hand-written v1.0.0 notes covering 51 v1 requirements + out-of-scope + install (D-18)"
  key_links:
    - from: ".github/workflows/release.yml release job"
      to: "shasum -a 256 + gh release upload Vector-*.dmg.sha256"
      via: "new Generate SHA256 step + amended Publish step (D-16)"
      pattern: ".sha256"
    - from: ".github/workflows/release.yml release job"
      to: ".planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md"
      via: "conditional: tag == v1.0.0 uses hand-written notes; else falls back to git-cliff (D-18)"
      pattern: "RELEASE-NOTES-1.0.0.md"
    - from: "release-cut checkpoint"
      to: ".planning/REQUIREMENTS.md PERSIST-04"
      via: "grep PERSIST-04 status before allowing tag push (D-25)"
      pattern: "PERSIST-04.*Complete"
---

<objective>
HARDEN-04: Tagging `v1.0.0` publishes the unsigned Universal `Vector.dmg` to GitHub Releases with install instructions front-and-center.

**Hard prerequisites (D-25):**
- 10-01 (snapshot suite) green
- 10-02 (vt corpus + perf gate) green
- 10-03 (cargo-geiger + token grep) green
- PERSIST-04 marked `Complete` in `.planning/REQUIREMENTS.md`

Purpose: Cut v1.0.0 — the moment Vector ships. README must be teammate-friendly (the install ritual is the FIRST thing a teammate sees), DMG must be discoverable + integrity-checkable, release notes must tell the story of what shipped.

Output: README restructure, workspace version bump, release.yml grown with SHA256 + hand-written-notes branch, RELEASE-NOTES-1.0.0.md drafted, and a checkpoint that gates the tag push behind PERSIST-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-hardening-release/10-CONTEXT.md
@.planning/phases/10-hardening-release/10-RESEARCH.md
@.planning/research/PITFALLS.md
@Cargo.toml
@README.md
@.github/workflows/release.yml

<interfaces>
<!-- Existing release.yml shape (Phase 1 land) -->

`release` job in `.github/workflows/release.yml` today:
1. checkout
2. resolve tag
3. download arm64 + x86_64 binaries from build-arm64 + build-x86_64 jobs
4. chmod + Pitfall-3 guard (file …| grep arm64/x86_64)
5. brew install + cargo install cargo-bundle
6. `cargo xtask dmg --universal --arm64 …/vector-app --x86_64 …/vector-app` → produces `target/dmg/Vector-{version}-universal.dmg`
7. Pitfall-3 guard (lipo -info confirms fat binary)
8. `git-cliff --latest -o RELEASE_NOTES.md`
9. Append xattr install footer to RELEASE_NOTES.md
10. `gh release create $TAG --title "Vector $TAG" --notes-file RELEASE_NOTES.md target/dmg/Vector-*-universal.dmg`

Phase 10 deltas to this job:
- NEW step: `shasum -a 256 Vector-*-universal.dmg > Vector-*-universal.dmg.sha256`
- AMEND publish step: include `*.sha256` in `gh release upload` / `gh release create`
- AMEND notes step: if `$TAG == v1.0.0`, use `.planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md` instead of git-cliff

PERSIST-04 grep pattern (D-25): match the bullet line in REQUIREMENTS.md and assert status text contains "Complete".
Current state (2026-05-26): line 75 of REQUIREMENTS.md = `- [ ] **PERSIST-04** *(revised 2026-05-22)*: …` — UNCHECKED, status = "Pending". Plan halts on this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README restructure (## Install as section 1) + workspace version bump to 1.0.0</name>
  <files>README.md, Cargo.toml</files>
  <read_first>
    - /Users/ashutosh/personal/vector/README.md (FULL — current structure)
    - /Users/ashutosh/personal/vector/Cargo.toml (workspace.package.version)
    - /Users/ashutosh/personal/vector/.planning/phases/10-hardening-release/10-CONTEXT.md (D-17, D-31)
    - /Users/ashutosh/personal/vector/.planning/phases/10-hardening-release/10-RESEARCH.md (Pitfall C — version-bump rationale)
  </read_first>
  <action>
    **A. README restructure (D-17):**

    Current README starts with `# Vector` (one-line tagline) then `## Install`. Per D-17 the install section is fine as section 1 — but the install ritual must include BOTH `xattr` AND `open` lines in the SAME copy-paste block, and a `### Why the xattr step?` subsection must follow.

    Rewrite `/Users/ashutosh/personal/vector/README.md` so:

    1. `# Vector` title + 1-line tagline (keep).
    2. `## Install` is the next heading, BEFORE anything else (no feature pitch, no screenshots before this).
    3. Install steps (numbered 1..4) with the copy-paste block in step 3:
       ```sh
       xattr -dr com.apple.quarantine /Applications/Vector.app
       open /Applications/Vector.app
       ```
       (Both lines in ONE fenced code block — that exact two-line ritual is the audience's install.)
    4. `### Why the xattr step?` subsection — one short paragraph: "Vector v1 ships unsigned (no Apple Developer subscription). macOS Gatekeeper quarantines downloaded apps; the xattr command removes the quarantine flag and `open` launches the app. v2 will sign + notarize (DIST-V2-01)."
    5. Then the existing remote-machine + status + build sections in their current order.

    Preserve every link that currently exists in README.md (`crates/vector-tunnel-agent/README.md`, `.planning/ROADMAP.md`, etc.).

    **B. Workspace version bump (D-31, Pitfall C):**

    In `/Users/ashutosh/personal/vector/Cargo.toml` change:
    ```
    [workspace.package]
    version = "2026.5.10"
    ```
    to:
    ```
    [workspace.package]
    version = "1.0.0"
    ```

    Sanity-check: `cargo metadata --no-deps --format-version 1 | jq -r '.workspace_packages[0].version'` outputs `1.0.0`.

    Also update the README's remote-machine section if it references `VERSION=2026.5.10` literally — change to `VERSION=1.0.0` so the example matches the post-tag world. (If the line uses `${VERSION}` placeholder + a `latest` redirect, no change needed; inspect first.)
  </action>
  <verify>
    <automated>grep -E "^## Install" README.md && grep -F "xattr -dr com.apple.quarantine /Applications/Vector.app" README.md && grep -F "open /Applications/Vector.app" README.md && grep -E "^### Why the xattr step\?" README.md && cargo metadata --no-deps --format-version 1 | jq -r '.workspace_packages[0].version' | grep -F "1.0.0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^## Install" README.md | head -1 | cut -d: -f1` outputs a small number (section 1 — within the first 10 lines after the title)
    - `grep -F "xattr -dr com.apple.quarantine /Applications/Vector.app" README.md` exits 0
    - `grep -F "open /Applications/Vector.app" README.md` exits 0
    - The two commands appear in the SAME fenced code block: `awk '/^```/{c++} c==1 && /xattr/{x=1} c==1 && /^open/{o=1} END{exit !(x&&o)}' README.md` exits 0
    - `grep -E "^### Why the xattr step\?" README.md` exits 0
    - `grep -F 'version = "1.0.0"' Cargo.toml` exits 0
    - `grep -F 'version = "2026.5.10"' Cargo.toml` exits 1 (old CalVer removed)
    - `cargo metadata --no-deps --format-version 1 | jq -r '.workspace_packages[0].version'` outputs exactly `1.0.0`
    - `cargo check --workspace` exits 0 (version bump didn't break anything)
  </acceptance_criteria>
  <done>
    README first content section is `## Install` with the two-line xattr+open block AND `### Why the xattr step?` follow-up. Workspace version is `1.0.0`. `cargo check --workspace` still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author RELEASE-NOTES-1.0.0.md + extend release.yml for SHA256 sidecar + hand-written notes branch</name>
  <files>.planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md, .github/workflows/release.yml</files>
  <read_first>
    - /Users/ashutosh/personal/vector/.github/workflows/release.yml (FULL — preserves all existing Pitfall-3 guards)
    - /Users/ashutosh/personal/vector/.planning/REQUIREMENTS.md (51 v1 requirement list)
    - /Users/ashutosh/personal/vector/.planning/ROADMAP.md (out-of-scope list at the bottom)
    - /Users/ashutosh/personal/vector/.planning/phases/10-hardening-release/10-CONTEXT.md (D-15, D-16, D-18, D-19, D-30)
    - /Users/ashutosh/personal/vector/.planning/phases/10-hardening-release/10-RESEARCH.md ("Phase 10 Delta — SHA256 Sidecar Upload")
  </read_first>
  <action>
    **A. Hand-write `.planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md`** (D-18, D-30 — planner chooses to land it as a planning-dir doc, not CHANGELOG.md, since CHANGELOG.md doesn't exist today and v1.0.1+ will use auto-generated notes).

    Structure:
    ```markdown
    # Vector 1.0.0

    A native macOS terminal — Rust, GPU-accelerated — with first-class VS Code Remote Tunnels support.

    ## What's in v1

    ### Local terminal (table stakes)
    - Native AppKit window with title bar, fullscreen, and standard window controls (WIN-01)
    - Tabs (Cmd-T / Cmd-Shift-]/[) and splits (Cmd-D / Cmd-Shift-D) (WIN-02, WIN-03)
    - GPU-accelerated rendering via wgpu/Metal, 60+ fps on Apple Silicon, ProMotion-aware, <1% idle CPU (RENDER-01..05, WIN-01)
    - Battle-tested VT parser (alacritty_terminal): truecolor, 256-color, grapheme-cluster widths, 10,000-line scrollback with regex search (CORE-01..06)
    - SIGWINCH handling, clean child-process lifecycle, TERM=xterm-256color (CORE-04, CORE-05)
    - winit/tokio threading skeleton: EventLoop on main, tokio on background, EventLoopProxy as the only seam (WIN-05)

    ### Polish (local daily-driver)
    - TOML config with hot-reload, profile inheritance, no scripting language (POLISH-01)
    - Bring-your-own-font + opt-in ligatures + Nerd Font glyphs (POLISH-02)
    - Light + dark themes + `.itermcolors` importer (POLISH-03)
    - OSC 7 (cwd), OSC 8 (hyperlinks), OSC 10/11/12 (colors), OSC 133 (semantic prompt marks) (POLISH-04)
    - OSC 52 clipboard (raw + DCS-wrapped for tmux pass-through) (POLISH-05)
    - Scrollback regex search with match navigation (POLISH-06)
    - Profiles (local / codespace / dev_tunnel) (POLISH-07)
    - Secure Keyboard Entry + basic IME composition (POLISH-08)

    ### Remote shells
    - GitHub OAuth Device Flow + Keychain token storage + Codespaces picker (AUTH-01..03, CS-01..03)
    - VS Code Remote Tunnels: list active tunnels, connect via Microsoft Dev Tunnels relay, visually distinct remote panes ([remote] badge + tinted tab) (DT-01..04)
    - Persistence + reconnect: TCP/SSH disconnect → Reconnecting state with exponential backoff, no blank screen, hot-swap PtyTransport without dropping bytes in flight (PERSIST-01..04)
    - Pitfall-8 tmux pass-through verified end-to-end: DCS-wrapped OSC 52, DECSCUSR, mouse 1006, TERM=xterm-256color all round-trip through user-started tmux on the remote (PERSIST-03 + PERSIST-04 revised 2026-05-22)
    - Domain / Pane / PtyTransport abstraction is the only seam between terminal and transport (WIN-04)

    ### Build & distribution
    - Universal binary (arm64 + x86_64 via lipo), macOS 13 (Ventura) baseline (BUILD-01..03)
    - GitHub Actions CI/CD: every push builds, every tag publishes (BUILD-04)
    - README documents the xattr Gatekeeper-bypass step (BUILD-05)

    ### Hardening
    - Renderer snapshot suite (HARDEN-01) — perceptual diff on 4 curated scenes, blocks merge on regression
    - VT conformance corpus (HARDEN-02) — 8 scenarios (alt-screen, scroll regions, tab stops, ED/EL, mouse 1006, OSC 52 round-trip, bracketed paste, DECSCUSR)
    - Perf gate (HARDEN-02) — idle CPU <1% + paste throughput at vsync cap on Apple Silicon (hard gate); Intel runners log-only
    - Unsafe-dep audit via cargo-geiger + allowlist (HARDEN-03)
    - Token redaction enforced at compile time (static grep gate) AND runtime (tracing-capture grep gate) (HARDEN-03)

    ## What's out of v1

    - Code signing + notarization (DIST-V2-01)
    - Sparkle auto-update (DIST-V2-02)
    - Public open-source push (audience is ~5 Adobe teammates)
    - AI autocomplete / command-history suggestions (BACKLOG 999.1, AI-V2-01/02)
    - Port-forwarding PORTS panel (RDEV-V2-01)
    - File transfer / scp-like UI (RDEV-V2-02)
    - Codespaces create/delete/rebuild from inside the app (RDEV-V2-03)
    - Arbitrary SSH targets as first-class profiles (RDEV-V2-04)
    - Full CJK IME candidate window (TERM-V2-01)
    - Command palette (TERM-V2-03)
    - Linux / Windows builds (PLAT-V2-01/02)

    ## How to install

    1. Download `Vector-1.0.0-universal.dmg` from this release.
    2. (Optional but recommended) Verify integrity: `shasum -a 256 -c Vector-1.0.0-universal.dmg.sha256`.
    3. Open the DMG and drag `Vector.app` to `/Applications`.
    4. Run once to remove the Gatekeeper quarantine and launch:

       ```sh
       xattr -dr com.apple.quarantine /Applications/Vector.app
       open /Applications/Vector.app
       ```

    See the [README](../../README.md) for the full install story.
    ```

    Verify against `.planning/REQUIREMENTS.md` that every checked v1 requirement is mentioned at least implicitly in the "What's in v1" bullets.

    **B. Extend `.github/workflows/release.yml`** — read the current `release` job carefully and add these deltas:

    1. **NEW step "Generate SHA256 checksum" — insert AFTER "Build Universal DMG" and BEFORE "Generate release notes":**
       ```yaml
       - name: Generate SHA256 checksum (HARDEN-04 D-16)
         run: |
           set -euo pipefail
           cd target/dmg
           for f in Vector-*-universal.dmg; do
             shasum -a 256 "$f" > "${f}.sha256"
             cat "${f}.sha256"
           done
       ```

    2. **AMEND "Generate release notes" step to branch on tag:**
       Replace the existing `git-cliff --latest -o RELEASE_NOTES.md` step with:
       ```yaml
       - name: Generate release notes
         run: |
           set -euo pipefail
           TAG="${{ steps.tag.outputs.tag }}"
           if [ "$TAG" = "v1.0.0" ]; then
             cp .planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md RELEASE_NOTES.md
             echo "Using hand-written notes for v1.0.0 (D-18)"
           else
             git-cliff --latest -o RELEASE_NOTES.md
           fi
       ```

    3. **AMEND "Publish or update GitHub Release" step** to include the `.sha256` sidecar in BOTH branches:
       Find the existing block:
       ```yaml
       gh release upload "$TAG" target/dmg/Vector-*-universal.dmg --clobber
       ```
       and the `gh release create` line:
       ```yaml
       gh release create "$TAG" --title "Vector $TAG" --notes-file RELEASE_NOTES.md target/dmg/Vector-*-universal.dmg
       ```
       Replace BOTH with versions that include the sidecar:
       ```yaml
       gh release upload "$TAG" target/dmg/Vector-*-universal.dmg target/dmg/Vector-*-universal.dmg.sha256 --clobber
       ```
       ```yaml
       gh release create "$TAG" --title "Vector $TAG" --notes-file RELEASE_NOTES.md target/dmg/Vector-*-universal.dmg target/dmg/Vector-*-universal.dmg.sha256
       ```

    4. **Add a final "Verify asset name matches workspace version (Pitfall C)" step before publish:**
       ```yaml
       - name: Pitfall-C guard — DMG version matches workspace package version
         run: |
           set -euo pipefail
           WS_VERSION=$(cargo metadata --no-deps --format-version 1 | jq -r '.workspace_packages[0].version')
           DMG_NAME=$(basename "$(ls target/dmg/Vector-*-universal.dmg | head -1)")
           EXPECTED="Vector-${WS_VERSION}-universal.dmg"
           if [ "$DMG_NAME" != "$EXPECTED" ]; then
             echo "::error::DMG name mismatch — got '$DMG_NAME' expected '$EXPECTED'"
             echo "Did you forget to bump workspace.package.version before tagging?"
             exit 1
           fi
       ```

    Validate YAML at the end: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md && grep -F "Vector 1.0.0" .planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md && grep -F "shasum -a 256" .github/workflows/release.yml && grep -F "RELEASE-NOTES-1.0.0.md" .github/workflows/release.yml && grep -F ".dmg.sha256" .github/workflows/release.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/ashutosh/personal/vector/.planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md` exists
    - Release notes mention every v1 category: `for s in "What's in v1" "What's out of v1" "How to install" "BUILD-01" "CORE-01" "RENDER-01" "WIN-01" "POLISH-01" "AUTH-01" "CS-01" "DT-01" "PERSIST-01" "HARDEN-01"; do grep -F "$s" .planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md || echo MISSING: $s; done` produces NO `MISSING:` lines
    - Release notes mention v2 deferrals: `grep -F "DIST-V2-01" .planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md` exits 0
    - `grep -F "shasum -a 256" .github/workflows/release.yml` exits 0
    - `grep -F "RELEASE-NOTES-1.0.0.md" .github/workflows/release.yml` exits 0
    - `grep -F "Vector-*-universal.dmg.sha256" .github/workflows/release.yml` exits 0
    - `grep -F 'Pitfall-C guard' .github/workflows/release.yml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0
    - Existing Pitfall-3 guards preserved: `grep -c 'Pitfall-3 guard' .github/workflows/release.yml` outputs `2`
  </acceptance_criteria>
  <done>
    RELEASE-NOTES-1.0.0.md drafted with 4 sections (in / out / install / link). release.yml grown with SHA256 step, conditional notes branch, sidecar upload in both create/upload, Pitfall-C version-match guard. All existing Pitfall-3 guards preserved.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: PERSIST-04 pre-flight + tag v1.0.0 + publish release</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /Users/ashutosh/personal/vector/.planning/REQUIREMENTS.md (PERSIST-04 line + Traceability table row)
    - /Users/ashutosh/personal/vector/.planning/phases/09-persistence-reconnect-tmux-auto-attach/09-VERIFICATION.md (sign-off status)
    - /Users/ashutosh/personal/vector/.planning/phases/10-hardening-release/RELEASE-NOTES-1.0.0.md (final notes)
  </read_first>
  <action>
    Orchestrated release cut. Claude does NOT push per CLAUDE.md; the user runs `git tag` + `git push`. Claude's role is pre-flight + post-tag verification.

    **Step 1 — Hard pre-flight check (D-25). Run this grep; it MUST exit 0 before proceeding:**
    ```
    grep -E "^\| PERSIST-04 \| Phase 9 \| Complete" .planning/REQUIREMENTS.md
    ```
    If non-zero, HALT. Surface to user: PERSIST-04 is pending UAT sign-off per `09-06-HUMAN-UAT.md`. Release is blocked until the joint UAT debt with `09-05-HUMAN-UAT.md` clears. No soft-fail.

    **Step 2 — Confirm all Phase 10 CI gates green on master:**
    ```
    gh run list --workflow=ci.yml --branch=master --limit=1 --json conclusion,name
    ```
    Must show `"conclusion":"success"`. If snapshot-suite, vt-conformance, perf-gate (arm64), cargo-deny, cargo-geiger, or token-redaction-grep is RED, fix before tagging.

    **Step 3 — Sanity-check version + README:**
    ```
    grep -F 'version = "1.0.0"' Cargo.toml
    cargo metadata --no-deps --format-version 1 | jq -r '.workspace_packages[0].version'   # must = 1.0.0
    grep -F "xattr -dr com.apple.quarantine /Applications/Vector.app" README.md
    grep -F "open /Applications/Vector.app" README.md
    grep -E "^### Why the xattr step\\?" README.md
    ```

    **Step 4 — Read RELEASE-NOTES-1.0.0.md end-to-end and cross-check against the 51 v1 requirements in `.planning/REQUIREMENTS.md`. Edit if any category is missing.**

    **Step 5 — Hand off to user. Output to user:**
    "Pre-flight passed. To cut v1.0.0, run:
    ```
    git tag v1.0.0
    git push origin v1.0.0
    ```
    Then wait ~10 minutes for release.yml to complete and reply 'tagged' to resume."

    **Step 6 — After user replies "tagged", verify the release:**
    ```
    gh release view v1.0.0 --json assets -q '.assets[].name'
    ```
    Must list BOTH `Vector-1.0.0-universal.dmg` and `Vector-1.0.0-universal.dmg.sha256`.

    **Step 7 — Update REQUIREMENTS.md Traceability table:** flip HARDEN-01..04 rows from `Pending` to `Complete`. Commit this final state-change.

    **Step 8 — User-driven teammate install dry-run (Step 8 in `<how-to-verify>`).**
  </action>
  <verify>
    <automated>grep -E "^\| HARDEN-0[1-4] \| Phase 10 \| Complete" .planning/REQUIREMENTS.md | wc -l | grep -F "4"</automated>
  </verify>
  <done>
    `gh release view v1.0.0` shows both DMG + .sha256 assets and hand-written notes. REQUIREMENTS.md Traceability table rows HARDEN-01..04 = Complete. Teammate dry-run install on a clean macOS account succeeds (xattr + open ritual launches Vector, local shell works).
  </done>
  <what-built>
    All Phase 10 CI gates (snapshot suite, vt-conformance, perf-gate, cargo-deny, cargo-geiger, token-redaction-grep) plus README install restructure, workspace version bump to 1.0.0, hand-written v1.0.0 release notes, and release.yml extended with SHA256 sidecar + hand-written-notes branch + Pitfall-C version-match guard.
  </what-built>
  <how-to-verify>
    Walk Steps 1–8 in `<action>` above. The user-facing rituals are:
    - Step 1 grep — proves PERSIST-04 cleared.
    - Step 5 — user runs `git tag v1.0.0 && git push origin v1.0.0`.
    - Step 6 — `gh release view v1.0.0` shows both assets with hand-written notes body matching RELEASE-NOTES-1.0.0.md.
    - Step 8 — On a fresh user account or teammate's machine: download DMG; run `shasum -a 256 -c Vector-1.0.0-universal.dmg.sha256` (must PASS); drag to /Applications; run `xattr -dr com.apple.quarantine /Applications/Vector.app && open /Applications/Vector.app`; confirm Vector launches and a local shell works.
  </how-to-verify>
  <acceptance_criteria>
    - Step 1 grep `grep -E "^\| PERSIST-04 \| Phase 9 \| Complete" .planning/REQUIREMENTS.md` exits 0
    - `gh release view v1.0.0 --json assets -q '.assets[].name'` lists `Vector-1.0.0-universal.dmg` AND `Vector-1.0.0-universal.dmg.sha256`
    - `gh release view v1.0.0 --json body -q '.body'` contains `## What's in v1`
    - `grep -E "^\| HARDEN-0[1-4] \| Phase 10 \| Complete" .planning/REQUIREMENTS.md | wc -l` outputs `4`
    - On fresh macOS account: DMG verifies against sidecar via `shasum -a 256 -c` AND Vector launches after the two-line xattr+open ritual
  </acceptance_criteria>
  <resume-signal>
    Type "tagged {release-url}" after user runs `git push origin v1.0.0` and the release workflow finishes. Or type "blocked: PERSIST-04 pending" if Step 1 grep fails.
  </resume-signal>
</task>

</tasks>

<verification>
- Local: `cargo check --workspace` green after version bump.
- Local: `grep -E "^## Install" README.md` returns line near top.
- Local: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0.
- CI: all 6 Phase 10 gates (snapshot, vt-conformance, perf-gate-arm64, cargo-deny, cargo-geiger, token-redaction-grep) green on master.
- CI: PERSIST-04 = Complete in REQUIREMENTS.md before the tag pre-flight grep runs.
- Post-tag: `gh release view v1.0.0` shows both Vector-1.0.0-universal.dmg + .sha256 sidecar, with hand-written notes body matching RELEASE-NOTES-1.0.0.md.
- Post-tag: teammate install dry-run works on a fresh user account.
</verification>

<success_criteria>
- HARDEN-04 acceptance line met: tagging v1.0.0 publishes the unsigned Universal Vector.dmg to GitHub Releases with install instructions front-and-center.
- D-15 satisfied: DMG asset = `Vector-1.0.0-universal.dmg`.
- D-16 satisfied: `.sha256` sidecar uploaded alongside.
- D-17 satisfied: README `## Install` is section 1 with xattr+open block + `### Why the xattr step?` follow-up.
- D-18 satisfied: hand-written notes for v1.0.0 (not git-cliff).
- D-19 satisfied: lipo+bundle+dmg+shasum+gh_upload in the `release` job.
- D-20 satisfied: tag style `v1.0.0`.
- D-25 satisfied: hard PERSIST-04 = Complete pre-flight blocks the tag if pending.
- D-31 satisfied: workspace version is `1.0.0`.
- Pitfall C mitigated: explicit version-match guard in release.yml.
</success_criteria>

<output>
After completion, create `.planning/phases/10-hardening-release/10-04-SUMMARY.md` recording:
- Release URL: https://github.com/colligo/vector/releases/tag/v1.0.0
- Final asset list (DMG + sha256 sidecar)
- Teammate-install dry-run result (Step 8)
- PERSIST-04 sign-off timestamp + UAT artifact reference
- Closing note: v1 ships; AI backlog (999.1) eligible to be planned next
</output>
