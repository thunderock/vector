---
phase: 09.1-prior-phase-gap-closure
plan: 04
type: execute
wave: 3
depends_on: ["09.1-02", "09.1-03"]
files_modified:
  - crates/vector-app/src/devtunnels_modal.rs
  - crates/vector-app/src/app.rs
  - crates/vector-app/tests/devtunnels_picker.rs
  - .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "When the DevTunnels picker opens in NotSignedIn state, a clickable `Sign in with Microsoft` button is visible"
    - "Clicking the button fires the same path as the menu item (DevTunnelsActor::Command::StartMicrosoftSignIn) and presents the MicrosoftAuthDeviceFlowModal"
    - "The picker stays open behind the device-flow modal; on MicrosoftSignedIn, the picker auto-refreshes by re-firing Command::Load"
    - "Footer copy reads 'Sign in with Microsoft to list Dev Tunnels.' (drops 'GitHub or' — consistent with Gap B)"
    - "No auto-present — the device-flow only opens on deliberate user click"
  artifacts:
    - path: "crates/vector-app/src/devtunnels_modal.rs"
      provides: "Sign-in NSButton inside DevTunnelsPickerModal; updated footer_copy()"
      contains: "Sign in with Microsoft"
    - path: "crates/vector-app/tests/devtunnels_picker.rs"
      provides: "Updated NOT_SIGNED_IN constant"
      contains: "Sign in with Microsoft to list Dev Tunnels."
  key_links:
    - from: "Sign-in button click handler in devtunnels_modal.rs"
      to: "DevTunnelsActor::Command::StartMicrosoftSignIn"
      via: "devtunnels_cmd_tx.try_send(...) routed through the actor command channel"
      pattern: "Command::StartMicrosoftSignIn"
---

<objective>
Gap D — Add a clickable `Sign in with Microsoft` button to `DevTunnelsPickerModal` (visible only in `FooterState::NotSignedIn`) that fires the same actor command as the menu item, AND amend the footer copy across all four sites to drop "GitHub or" (consistent with Gap B's deletion of GitHub OAuth).

Purpose: Per Phase 9 UAT, the picker can be opened by Cmd-Shift-T while signed-out, but offers no in-modal sign-in affordance — user has to dismiss the picker and find the menu. UI-SPEC amendment also required because Gap B deletes GitHub auth, making "GitHub or Microsoft" copy a lie.

Output: `DevTunnelsPickerModal` shows a Microsoft-blue button in the NotSignedIn state; footer copy + UAT toast copy + test constant + UI-SPEC table all read "Sign in with Microsoft to list Dev Tunnels."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-prior-phase-gap-closure/09.1-CONTEXT.md
@crates/vector-app/src/devtunnels_modal.rs
@crates/vector-app/src/app.rs
@crates/vector-app/src/devtunnels_actor.rs
@crates/vector-app/tests/devtunnels_picker.rs
@.planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md

<interfaces>
<!-- FooterState (crates/vector-app/src/devtunnels_modal.rs:40-60) -->
```rust
pub enum FooterState {
    Loading,
    EmptySignedIn,
    NotSignedIn,
    SignedInOtherProvider { provider: String },
    ApiError { reason: String },
    Loaded { shown: usize, total: usize },
}

pub fn footer_copy(state: &FooterState) -> String { ... }
```

<!-- DevTunnelsActor command (crates/vector-app/src/devtunnels_actor.rs:69) -->
```rust
pub enum Command {
    Load,
    Connect { tunnel_id, rows, cols, window_id },
    StartMicrosoftSignIn,
    SignOutMicrosoft,
    // ...
}
```

<!-- MICROSOFT_BLUE: [0.0, 0.471, 0.831, 1.0] (vector-render::tint_stripe) -->

<!-- DevTunnelsPickerModal::show signature (crates/vector-app/src/devtunnels_modal.rs:122) -->
```rust
impl DevTunnelsPickerModal {
    pub fn show(mtm: MainThreadMarker, ctx: DevTunnelsModalCtx) -> Self;
    pub fn handle_auth_required(&mut self, mtm: MainThreadMarker);
    pub fn handle_loaded(&mut self, mtm: MainThreadMarker, views: Vec<TunnelView>);
    // ...
}
```
</interfaces>

D-12: clickable `Sign in with Microsoft` button in `DevTunnelsPickerModal`. Visible only in `FooterState::NotSignedIn`. Uses `MICROSOFT_BLUE = [0.0, 0.471, 0.831, 1.0]`.
D-13: click handler routes through `DevTunnelsActor::Command::StartMicrosoftSignIn` — same path the menu item fires. Reuses `MicrosoftAuthDeviceFlowModal`. Picker stays open; on `MicrosoftSignedIn`, picker re-fires `Command::Load`.
D-14: NO auto-present. Deliberate user action (open picker) followed by another deliberate action (click button).
D-15: UI-SPEC amendment. Footer copy changes from `Sign in with GitHub or Microsoft to list Dev Tunnels.` to `Sign in with Microsoft to list Dev Tunnels.` Update all FOUR sites (verified by grep 2026-05-26):
  - `crates/vector-app/src/devtunnels_modal.rs:55` (footer_copy)
  - `crates/vector-app/src/devtunnels_modal.rs:356` (likely a test inside the modal file)
  - `crates/vector-app/src/app.rs:2160` (the toast on DevTunnelsAuthRequired)
  - `crates/vector-app/tests/devtunnels_picker.rs:34` (`const NOT_SIGNED_IN`)
  - `.planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md:177` (UI-SPEC table row)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend footer copy at all five sites — drop "GitHub or"</name>
  <files>
    crates/vector-app/src/devtunnels_modal.rs,
    crates/vector-app/src/app.rs,
    crates/vector-app/tests/devtunnels_picker.rs,
    .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md
  </files>
  <read_first>
    - crates/vector-app/src/devtunnels_modal.rs (lines 54-56 for footer_copy; line 356 for the second occurrence — grep first to confirm the exact line and context)
    - crates/vector-app/src/app.rs (line 2160 — the DevTunnelsAuthRequired arm sends a toast with this string)
    - crates/vector-app/tests/devtunnels_picker.rs (line 34 — `const NOT_SIGNED_IN: &str = "Sign in with GitHub or Microsoft to list Dev Tunnels.";`)
    - .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md (line 177 — the picker-footer-copy table row)
  </read_first>
  <action>
    Five sites confirmed by grep on 2026-05-26 (counts may shift after Gap B lands; re-verify with `grep -rIn "Sign in with GitHub or Microsoft" crates/ .planning/` first):

    1. `crates/vector-app/src/devtunnels_modal.rs:55` — inside `footer_copy()`, the `FooterState::NotSignedIn` arm:
       OLD: `"Sign in with GitHub or Microsoft to list Dev Tunnels.".to_string()`
       NEW: `"Sign in with Microsoft to list Dev Tunnels.".to_string()`

    2. `crates/vector-app/src/devtunnels_modal.rs:356` — the second occurrence (likely a `#[test]` asserting the footer copy):
       OLD: `"Sign in with GitHub or Microsoft to list Dev Tunnels."`
       NEW: `"Sign in with Microsoft to list Dev Tunnels."`

    3. `crates/vector-app/src/app.rs:2160` — the `UserEvent::DevTunnelsAuthRequired` arm's `ToastBanner::info(...)` payload:
       OLD: `"Sign in with GitHub or Microsoft to list Dev Tunnels.",`
       NEW: `"Sign in with Microsoft to list Dev Tunnels.",`

    4. `crates/vector-app/tests/devtunnels_picker.rs:34`:
       OLD: `const NOT_SIGNED_IN: &str = "Sign in with GitHub or Microsoft to list Dev Tunnels.";`
       NEW: `const NOT_SIGNED_IN: &str = "Sign in with Microsoft to list Dev Tunnels.";`

    5. `.planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md:177` — the picker footer copy table:
       OLD: `| Not signed in (neither provider) | \`Sign in with GitHub or Microsoft to list Dev Tunnels.\` |`
       NEW: `| Not signed in | \`Sign in with Microsoft to list Dev Tunnels.\` |`
       (Drop the parenthetical "(neither provider)" since there's only one provider in v1; the `FooterState::SignedInOtherProvider` row remains for cross-provider edge cases — leave that row alone.)

    Verify Gap B didn't leave any extra occurrences: after edits, `! grep -rIn "Sign in with GitHub or Microsoft" crates/ .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md` must return zero.

    Do NOT push.
  </action>
  <verify>
    <automated>cd /Users/ashutosh/personal/vector && ! grep -rIn "Sign in with GitHub or Microsoft" crates/ .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md && cargo test -p vector-app --test devtunnels_picker 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rIn "Sign in with GitHub or Microsoft" crates/` (zero matches in code)
    - `! grep -n "Sign in with GitHub or Microsoft" .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md` (zero matches in UI-SPEC)
    - `grep -c "Sign in with Microsoft to list Dev Tunnels." crates/vector-app/src/devtunnels_modal.rs` returns 2
    - `grep -q "Sign in with Microsoft to list Dev Tunnels." crates/vector-app/src/app.rs`
    - `grep -q "Sign in with Microsoft to list Dev Tunnels." crates/vector-app/tests/devtunnels_picker.rs`
    - `grep -q "Sign in with Microsoft to list Dev Tunnels." .planning/phases/08-vs-code-remote-tunnels-connect/08-UI-SPEC.md`
    - `cargo test -p vector-app --test devtunnels_picker` exits 0
  </acceptance_criteria>
  <done>
    All five sites updated; the character-locked UI-SPEC and its tests agree on the new shorter copy; build and devtunnels_picker tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the `Sign in with Microsoft` button to DevTunnelsPickerModal with click → Command::StartMicrosoftSignIn</name>
  <files>
    crates/vector-app/src/devtunnels_modal.rs,
    crates/vector-app/src/app.rs
  </files>
  <read_first>
    - crates/vector-app/src/devtunnels_modal.rs (full file — read at minimum lines 1-260 for the existing modal structure; `show` at line 122 sets up the footer NSTextField at FOOTER_X/FOOTER_Y/FOOTER_W/FOOTER_H; `handle_auth_required` at line 191 swaps the footer to NotSignedIn; the `views`/`row_fields`/`footer` fields are stored on the struct)
    - crates/vector-app/src/devtunnels_actor.rs (lines 60-140 — `Command` enum confirms `StartMicrosoftSignIn`; the handler at line 137 is `actor.handle_start_microsoft_signin().await`)
    - crates/vector-app/src/app.rs (search for `MicrosoftSignedIn` arm — it should re-trigger a picker reload by sending `Command::Load` if the picker is still presented; also search for `devtunnels_cmd_tx` to confirm the channel field name on App)
    - crates/vector-render or similar for the exact path of `MICROSOFT_BLUE` (grep `MICROSOFT_BLUE`; the constant is at `vector_render::tint_stripe::MICROSOFT_BLUE` per app.rs:860 usage)
  </read_first>
  <action>
    Goal: a button visible only in `FooterState::NotSignedIn`, tinted Microsoft-blue (`MICROSOFT_BLUE = [0.0, 0.471, 0.831, 1.0]`), with click handler that fires `DevTunnelsActor::Command::StartMicrosoftSignIn` via the existing `devtunnels_cmd_tx`. No auto-present. Picker stays open.

    Implementation outline (the executor MUST read the modal file in full first; AppKit specifics are pattern-mirrored, not invented):

    **2a. Add an NSButton field to `DevTunnelsPickerModal`.** In `crates/vector-app/src/devtunnels_modal.rs` near the existing field list (currently `panel`, `rows_container_frame`, `row_fields`, `footer`, `views`, `selected_index`, `filter_text`, `poll_cancel`):

    ```rust
    /// Sign-in button shown only when FooterState::NotSignedIn. Per Plan 09.1-04 / D-12.
    signin_button: Retained<NSButton>,
    /// ObjC target wrapping a one-shot Sender that dispatches StartMicrosoftSignIn.
    /// Held so AppKit's weak `target` reference stays alive.
    #[allow(dead_code)]
    signin_target: Retained<SigninTarget>,
    ```

    Add a new ObjC subclass `SigninTarget` mirroring the pattern of `MicrosoftMenuTarget` (`menu.rs:582-628`). It holds an `EventLoopProxy<UserEvent>` ivar and exposes a `#[unsafe(method(microsoftSignIn:))]` that fires `UserEvent::MicrosoftSignInRequested`. (Reuse the existing UserEvent — `app.rs`' arm for `MicrosoftSignInRequested` already calls into `devtunnels_cmd_tx.try_send(Command::StartMicrosoftSignIn)` per Plan 08-05 SUMMARY; verify this with grep before implementing the alternative.)

    Alternative (simpler) wiring: instead of a new ObjC class, route the click to `UserEvent::MicrosoftSignInRequested` reusing the existing `MicrosoftMenuTarget` pattern but specialized for this button. Keep the ObjC subclass local to `devtunnels_modal.rs`.

    **2b. Build the NSButton in `DevTunnelsPickerModal::show`.** Use Phase 6/8 NSPanel patterns (see `auth_modal.rs` or `microsoft_auth_modal.rs` for an existing NSButton example — grep `NSButton::` in `crates/vector-app/src/` to find the pattern).

    Frame: position the button ABOVE the footer (footer y=4, h=24, w=624 at x=8 per UI-SPEC §Picker frame constants). Suggested button frame: x=8, y=32, w=200, h=28 (8px tall, in the rows-container area; rows-container y=32 too, so this overlays the empty rows-area cleanly). Coordinate with UI-SPEC §Spacing — multiples of 4.

    Properties:
    - Title: `"Sign in with Microsoft"` (UI-SPEC verbatim per D-15 + S3 — match the menu item title)
    - `setHidden(true)` by default (NotSignedIn state arrives later via `handle_auth_required`)
    - Background/tint: NSButton doesn't natively take an RGBA; mirror the existing Microsoft-blue button pattern from `microsoft_auth_modal.rs` if present; otherwise set `setBezelStyle(NSBezelStyle::Rounded)` and apply an `NSAttributedString` title with `NSForegroundColorAttributeName` set to MICROSOFT_BLUE via `NSColor::colorWithRed:green:blue:alpha:`. The exact NSColor recipe: `NSColor::colorWithSRGBRed_green_blue_alpha(0.0, 0.471, 0.831, 1.0)`.
    - Target/action: `setTarget(Some(signin_target_as_anyobject))`, `setAction(Some(sel!(microsoftSignIn:)))`

    Construct the SigninTarget with the proxy passed into `show` via `DevTunnelsModalCtx` — extend the ctx struct with `proxy: EventLoopProxy<UserEvent>`. Update the only constructor call site (likely `app.rs::handle_open_devtunnels_picker_menu` or equivalent — grep `DevTunnelsModalCtx {` and add the field).

    Add the button as a subview of the panel's content view at the end of `show`:
    ```rust
    content.addSubview(&signin_button);
    ```

    **2c. Show/hide the button on state transitions.**
    - `handle_auth_required` (line 191): set `self.signin_button.setHidden(false);` after updating the footer text.
    - `handle_loaded` (line 167): set `self.signin_button.setHidden(true);`
    - `handle_load_failed` (line 181): set `self.signin_button.setHidden(true);` (ApiError isn't a sign-in problem).
    - `show` initial state: button is hidden because picker opens in `FooterState::Loading`.
    - On `MicrosoftSignedIn`: app.rs already (per D-13) re-fires `Command::Load`, which leads to `handle_loaded` (or `handle_load_failed`); the hide flips happen there.

    **2d. Wire MicrosoftSignInRequested → Command::StartMicrosoftSignIn in app.rs.** Grep `MicrosoftSignInRequested` in app.rs. Per Plan 08-05 the arm already calls `devtunnels_cmd_tx.try_send(Command::StartMicrosoftSignIn)`. If yes, leave alone. If not, add it — pattern:
    ```rust
    UserEvent::MicrosoftSignInRequested => {
        if let Some(tx) = &self.devtunnels_cmd_tx {
            let _ = tx.try_send(crate::devtunnels_actor::Command::StartMicrosoftSignIn);
        }
    }
    ```

    **2e. On `MicrosoftSignedIn`, ensure the picker auto-refreshes.** Grep `MicrosoftSignedIn` arm in app.rs. If the modal is currently presented, fire `Command::Load`:
    ```rust
    UserEvent::MicrosoftSignedIn => {
        // ... existing rebuild_microsoft_signin_section call from Plan 09.1-03 ...
        if self.devtunnels_modal.as_ref().is_some() {
            if let Some(tx) = &self.devtunnels_cmd_tx {
                let _ = tx.try_send(crate::devtunnels_actor::Command::Load);
            }
        }
    }
    ```

    The picker's `devtunnels_modal` field is on App — grep for it. If named differently, adjust.

    **2f. NO auto-present (D-14).** Do NOT add any code that opens the device-flow modal when the picker enters NotSignedIn. The button click is the only trigger.

    Per D-13, the existing `handle_auth_required` device-flow modal flow (separate ObjC modal, presented by app.rs on `UserEvent::MicrosoftDeviceFlowStarted`) is reused — no new modal needed.

    DO NOT push.
  </action>
  <verify>
    <automated>cd /Users/ashutosh/personal/vector && grep -q 'Sign in with Microsoft' crates/vector-app/src/devtunnels_modal.rs && grep -q 'StartMicrosoftSignIn' crates/vector-app/src/devtunnels_modal.rs && cargo build -p vector-app 2>&1 | tail -5 && cargo clippy -p vector-app --all-targets -- -D warnings 2>&1 | tail -10 && cargo test -p vector-app 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'signin_button' crates/vector-app/src/devtunnels_modal.rs` (the field exists)
    - `grep -q 'Sign in with Microsoft' crates/vector-app/src/devtunnels_modal.rs` (button title is present)
    - `grep -q 'MicrosoftSignInRequested\|StartMicrosoftSignIn' crates/vector-app/src/devtunnels_modal.rs` (the click routes to the right event/command)
    - `grep -n "setHidden" crates/vector-app/src/devtunnels_modal.rs` shows the button visibility toggled in `handle_auth_required`, `handle_loaded`, `handle_load_failed`
    - `! grep -B2 -A6 "handle_auth_required" crates/vector-app/src/devtunnels_modal.rs | grep -q "MicrosoftDeviceFlowStarted"` (no auto-present per D-14 — handle_auth_required does NOT trigger the device flow)
    - `cargo build -p vector-app` exits 0
    - `cargo clippy -p vector-app --all-targets -- -D warnings` exits 0
    - `cargo test -p vector-app` exits 0 (all picker tests, including the updated NOT_SIGNED_IN constant, pass)
  </acceptance_criteria>
  <done>
    `DevTunnelsPickerModal` shows a Microsoft-blue `Sign in with Microsoft` button when in NotSignedIn state; clicking it fires the same actor command as the menu item; on MicrosoftSignedIn the picker reloads automatically; no auto-present; build, clippy, and tests are clean.
  </done>
</task>

</tasks>

<verification>
- `cargo build -p vector-app` clean
- `cargo clippy -p vector-app --all-targets -- -D warnings` clean
- `cargo test -p vector-app` clean (including `cargo test -p vector-app --test devtunnels_picker`)
- `grep -rIn "Sign in with GitHub or Microsoft" crates/ .planning/` returns zero
- Manual (post-merge): `cargo run --release`, sign OUT of Microsoft via menu, open Cmd-Shift-T → picker shows the blue `Sign in with Microsoft` button + footer text. Click button → device-flow modal opens with code; picker stays behind. Complete sign-in → device-flow closes, picker refreshes with tunnels (or EmptySignedIn footer).
</verification>

<success_criteria>
Footer copy amended across all five sites; the picker presents an in-modal sign-in button in NotSignedIn state that routes through the existing actor command channel; no auto-present. Maps to ROADMAP §Phase 9.1 Success Criteria #4.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-prior-phase-gap-closure/09.1-04-SUMMARY.md`
</output>
