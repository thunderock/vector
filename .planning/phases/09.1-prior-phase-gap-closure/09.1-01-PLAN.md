---
phase: 09.1-prior-phase-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vector-app/src/app.rs
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "Typing `exit` in a local pane closes that pane immediately"
    - "Closing the last pane in a tab closes the tab; closing the last tab closes the window; closing the last window exits the app"
    - "Remote (DevTunnel) panes are not affected by this handler change (they enter Reconnecting on EOF, not PaneExited)"
  artifacts:
    - path: "crates/vector-app/src/app.rs"
      provides: "PaneExited handler that calls Mux::close_pane and routes CloseResult"
      contains: "UserEvent::PaneExited"
  key_links:
    - from: "crates/vector-app/src/app.rs UserEvent::PaneExited arm"
      to: "vector_mux::Mux::close_pane"
      via: "Mux::try_get().close_pane(pane_id)"
      pattern: "close_pane\\(pane_id\\)"
---

<objective>
Gap A — Replace the log-only `UserEvent::PaneExited` stub at `crates/vector-app/src/app.rs:1872-1874` with a real handler that calls `Mux::close_pane(pane_id)` and propagates the resulting `CloseResult` (close pane / close tab / close window / exit app), matching the Cmd-W close path at `app.rs:1660-1664`.

Purpose: Phase 4-05 left this as a stub. Today `exit` in a local pane leaves a dead pane on screen — `vim`/shell exit looks broken. Phase 9 UAT cannot proceed without this baseline pane lifecycle (D-01).

Output: Updated `PaneExited` arm; local-pane lifecycle behaves like iTerm/Apple Terminal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-prior-phase-gap-closure/09.1-CONTEXT.md
@crates/vector-app/src/app.rs
@crates/vector-mux/src/mux.rs
@crates/vector-mux/src/ids.rs

<interfaces>
<!-- Mux::close_pane signature (crates/vector-mux/src/mux.rs:230) -->
```rust
impl Mux {
    pub fn close_pane(&self, pane_id: PaneId) -> CloseResult { ... }
}
```

<!-- CloseResult variants (crates/vector-mux/src/ids.rs:59-66) -->
```rust
pub enum CloseResult {
    PaneClosed { tab_id: TabId },
    TabClosed { window_id: WindowId },
    WindowClosed { window_id: WindowId },
    LastWindowClosed,
}
```

<!-- Reference implementation — Cmd-W ClosePane arm at app.rs:1649-1666:
The handler:
  1. cancels per-pane cancel token (Plan 09-05) — `self.pane_cancel_tokens.remove(&id).map(|c| c.cancel())`
  2. removes `self.reconnecting_panes.remove(&id)` and `self.reconnect_first_keystroke_shown.remove(&id)`
  3. calls `mux.close_pane(active)`
  4. logs `?result, "close_pane cascade"`
  5. on `CloseResult::LastWindowClosed`, calls `event_loop.exit()`
This is the pattern to mirror exactly. -->
</interfaces>

D-01: handler calls `Mux::close_pane(pane_id)` and removes pane immediately. No `[exited]` sentinel.
D-02: no config knob in v1.
D-03: remote panes go through `Reconnecting` on EOF — not affected (PaneExited only fires for local PTY EOF via `pty_actor.rs:232` + `LocalDomain::reconnect_one_shot` returning `Ok(None)`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PaneExited log-only stub with close_pane handler</name>
  <files>crates/vector-app/src/app.rs</files>
  <read_first>
    - crates/vector-app/src/app.rs (the file being modified — read lines 1640-1720 for the existing ClosePane arm pattern at 1649-1666 and the current PaneExited stub at 1872-1874)
    - crates/vector-mux/src/mux.rs (lines 220-285 for close_pane signature and cascade behavior)
    - crates/vector-mux/src/ids.rs (lines 55-70 for CloseResult variants)
  </read_first>
  <action>
    Locate the current PaneExited stub in `crates/vector-app/src/app.rs` around lines 1872-1874 — exact current contents:

    ```rust
    UserEvent::PaneExited(pane_id) => {
        tracing::info!(?pane_id, "pane exited (Plan 04-05 will render sentinel)");
    }
    ```

    Replace with a handler that mirrors the Cmd-W `MuxCommand::ClosePane` pattern at lines 1649-1666 verbatim, adapted to take `pane_id` from the event instead of `mux.any_active_pane_id()`:

    1. Drop any per-pane cancel token: `if let Some(cancel) = self.pane_cancel_tokens.remove(&pane_id) { cancel.cancel(); }`
    2. Clean reconnect state: `self.reconnecting_panes.remove(&pane_id); self.reconnect_first_keystroke_shown.remove(&pane_id);`
    3. Call `Mux::try_get().map(|mux| mux.close_pane(pane_id))` and bind result.
    4. `tracing::info!(?pane_id, ?result, "PaneExited → close_pane cascade");`
    5. On `Some(vector_mux::CloseResult::LastWindowClosed)`, call `event_loop.exit()`.

    Per D-01: no `[exited]` sentinel — pane goes away immediately. Per D-03: remote panes never emit PaneExited (they enter Reconnecting on EOF), so this branch is local-pane-only by virtue of the producer at `pty_actor.rs:232`.

    The `event_loop` reference: check the surrounding match-arm body in app.rs for how it's accessed. The Cmd-W arm at line 1645 receives `event_loop: &ActiveEventLoop` as a fn parameter; the UserEvent dispatch happens in `user_event(...)` which also has access to `event_loop` — use the same identifier already in scope at line 1872.

    If `event_loop` is not directly in scope at the PaneExited arm site, mirror exactly the access pattern of any neighboring arm that calls `event_loop.exit()` (search backward from line 1872 with grep for `event_loop.exit()` in app.rs to find the in-scope identifier name).
  </action>
  <verify>
    <automated>cd /Users/ashutosh/personal/vector &amp;&amp; cargo build -p vector-app 2>&amp;1 | tail -5 &amp;&amp; cargo clippy -p vector-app --all-targets -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PaneExited(pane_id) =>" crates/vector-app/src/app.rs` finds the arm
    - `! grep -A4 "UserEvent::PaneExited(pane_id) =>" crates/vector-app/src/app.rs | grep -q "Plan 04-05 will render sentinel"` (the stub log line is gone)
    - `grep -A10 "UserEvent::PaneExited(pane_id) =>" crates/vector-app/src/app.rs | grep -q "close_pane(pane_id)"`
    - `grep -A12 "UserEvent::PaneExited(pane_id) =>" crates/vector-app/src/app.rs | grep -q "LastWindowClosed"`
    - `cargo build -p vector-app` exits 0
    - `cargo clippy -p vector-app --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    The PaneExited match-arm calls `Mux::close_pane(pane_id)`, propagates CloseResult, exits the event loop on LastWindowClosed, cleans per-pane reconnect state, and builds clean under clippy -D warnings. Typing `exit` in a local pane (manually verifiable) closes the pane.
  </done>
</task>

</tasks>

<verification>
- `cargo build -p vector-app` clean
- `cargo clippy -p vector-app --all-targets -- -D warnings` clean
- `cargo test -p vector-app` clean (no test regressions)
- Manual (post-merge, not blocking): `cargo run --release`, open a local pane, type `exit`, pane disappears
</verification>

<success_criteria>
PaneExited handler closes the local pane via Mux::close_pane and cascades CloseResult correctly. Maps to ROADMAP §Phase 9.1 Success Criteria #1.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-prior-phase-gap-closure/09.1-01-SUMMARY.md`
</output>
