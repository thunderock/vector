---
phase: 09.1-prior-phase-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/vector-app/src/lib.rs
  - crates/vector-app/src/app.rs
  - crates/vector-app/src/menu.rs
  - crates/vector-app/src/auth_actor.rs
  - crates/vector-app/src/codespaces_actor.rs
  - crates/vector-app/src/codespaces_modal.rs
  - crates/vector-app/src/relative_time.rs
  - crates/vector-codespaces/
  - crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "The Codespaces… menu item is gone from the Vector menu"
    - "The vector-codespaces crate no longer exists in the workspace"
    - "No vector_codespaces::* imports anywhere in the workspace"
    - "Workspace builds, clippy is clean, all tests pass"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace [members] without vector-codespaces"
      contains: "members"
    - path: "crates/vector-app/src/lib.rs"
      provides: "UserEvent enum without OpenCodespacesPicker/CodespacesLoaded/CodespacesLoadFailed/CodespaceStateChanged"
    - path: "crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs"
      provides: "SCAN_PATHS without vector-codespaces/src"
  key_links:
    - from: "crates/vector-app/src/menu.rs"
      to: "Vector menu (AppKit submenu)"
      via: "install_phase6_items no longer inserts Codespaces… item or openCodespaces: selector"
      pattern: "! openCodespaces"
---

<objective>
Gap B — Delete the `vector-codespaces` crate wholesale and remove every Codespaces UI surface from the Vector workspace per D-04..D-08 (CONTEXT.md lines 31-43). After this lands, the only sign-in path in v1 is Microsoft (for Dev Tunnels); GitHub OAuth is removed entirely.

Purpose: Phase 7 pivoted away from Codespaces (2026-05-19) but the UI surface — menu item, modal, actor, crate, arch-test scan path — was left in place. The Phase 9 UAT walk surfaced the abandoned menu item and unreachable code paths.

Output: Workspace with no `vector-codespaces` crate, no Codespaces UI/menu/actor, no GitHub OAuth code paths, no related UserEvent variants, all consumers cleanly migrated or deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-prior-phase-gap-closure/09.1-CONTEXT.md
@Cargo.toml
@crates/vector-app/src/lib.rs
@crates/vector-app/src/app.rs
@crates/vector-app/src/menu.rs
@crates/vector-app/src/auth_actor.rs
@crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs

D-04: delete `vector-codespaces` crate wholesale.
D-05: no GitHub OAuth in v1. `auth_actor.rs` either deletes GitHub paths or the file is deleted if nothing else lives in it.
D-06: removal inventory (see Task 1 below).
D-07: acceptance: `grep -rIn "codespace\|Codespace\|CODESPACE" crates/` returns only historical-pivot comments or zero matches; `cargo build --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings` all green.
D-08: handle as a single commit (or two: crate-removal+workspace, then app.rs/menu.rs surgery). Arch-test SCAN_PATHS change MUST land in same commit as crate deletion (otherwise lint fails on the first commit).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete vector-codespaces crate + workspace member + arch-test scan-path (single atomic commit)</name>
  <files>
    Cargo.toml,
    crates/vector-codespaces/ (entire directory deleted),
    crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs
  </files>
  <read_first>
    - Cargo.toml (workspace members list — line ~3 onwards; line 11 currently lists `crates/vector-codespaces`)
    - crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs (line 11 has `"vector-codespaces/src",` in SCAN_PATHS; line 131 has the explicit Phase-8 assertion `assert!(SCAN_PATHS.iter().any(|p| p == &"vector-codespaces/src"))`)
    - .planning/phases/09.1-prior-phase-gap-closure/09.1-CONTEXT.md (D-04, D-06, D-07, D-08)
  </read_first>
  <action>
    Perform the following in a single commit (atomicity required so the arch-test does not fail mid-step per D-08):

    1. **Delete the crate directory.** Run `git rm -r crates/vector-codespaces` (or equivalent — leave no files behind including hidden `.gitignore` or generated artifacts).

    2. **Remove from workspace `Cargo.toml`.** In the top-level `/Users/ashutosh/personal/vector/Cargo.toml`, edit the `[workspace] members = [...]` array to remove the line `"crates/vector-codespaces",` (currently around line 11). Leave all other crates intact.

    3. **Update `crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs`:**
       - Line 11: remove `"vector-codespaces/src",` from the `SCAN_PATHS` const array.
       - Line 131: delete the line `assert!(SCAN_PATHS.iter().any(|p| p == &"vector-codespaces/src"));`. The surrounding Phase-8 assertions for other crates stay. If the assertion at line 131 is part of a `#[test]` function dedicated to vector-codespaces, delete the whole test fn instead — but inspect first before assuming.

    4. **Sanity check before commit:** `cargo build --workspace` MUST succeed at this point. If it fails because other crates still depend on `vector_codespaces::*`, STOP — do not split this commit; continue to Task 2 first and combine commits. Per D-08, "the arch-test scan-path change must land in the same commit as the crate deletion."

    DO NOT push. Per project workflow.
  </action>
  <verify>
    <automated>cd /Users/ashutosh/personal/vector &amp;&amp; ! test -d crates/vector-codespaces &amp;&amp; ! grep -q '"crates/vector-codespaces"' Cargo.toml &amp;&amp; ! grep -q 'vector-codespaces/src' crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d crates/vector-codespaces` (directory is gone)
    - `! grep -q '"crates/vector-codespaces"' Cargo.toml`
    - `! grep -q 'vector-codespaces/src' crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs`
    - `! grep -q 'vector-codespaces' Cargo.toml`
  </acceptance_criteria>
  <done>
    Crate deleted, workspace updated, arch-test scan-path cleaned. Atomic-commit invariant from D-08 preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip vector_codespaces consumers from vector-app (app.rs, lib.rs, menu.rs, auth_actor.rs, codespaces_*.rs, relative_time.rs)</name>
  <files>
    crates/vector-app/src/lib.rs,
    crates/vector-app/src/app.rs,
    crates/vector-app/src/menu.rs,
    crates/vector-app/src/auth_actor.rs,
    crates/vector-app/src/codespaces_actor.rs (deleted),
    crates/vector-app/src/codespaces_modal.rs (deleted),
    crates/vector-app/src/relative_time.rs (possibly deleted),
    crates/vector-app/Cargo.toml (drop vector-codespaces dep if listed),
    crates/vector-app/tests/ (any tests referencing codespaces)
  </files>
  <read_first>
    - crates/vector-app/src/lib.rs (full file — note line 19-20 `pub mod codespaces_actor; pub mod codespaces_modal;` and lines 108-118 the 4 UserEvent variants: OpenCodespacesPicker, CodespacesLoaded(Arc&lt;Vec&lt;vector_codespaces::Codespace&gt;&gt;), CodespacesLoadFailed(String), CodespaceStateChanged{name, state: vector_codespaces::CodespaceState})
    - crates/vector-app/src/app.rs (lines 158, 162, 221, 423, 459-497, 502, 521, 536-618, 736-738, 1763, 1945, 2039-2045 — every `vector_codespaces::*`, `codespaces_modal`, `codespaces_actor`, `OpenCodespacesPicker`, `CodespacesLoaded`, `CodespacesLoadFailed`, `CodespaceStateChanged`, `handle_open_codespaces_picker`, `handle_codespaces_loaded`)
    - crates/vector-app/src/menu.rs (lines 436-452 — `Codespaces…` item, Cmd-Shift-G keybind, `openCodespaces:` selector; lines 526-529 — the `open_codespaces` ObjC method on `AuthMenuTarget`)
    - crates/vector-app/src/auth_actor.rs (full file — if any code path imports `vector_codespaces` or `gho_`/`ghp_`-token-store APIs, delete those; if nothing else lives in the file, delete the whole file and remove `pub mod auth_actor;` from lib.rs)
    - crates/vector-app/src/codespaces_actor.rs (will be deleted)
    - crates/vector-app/src/codespaces_modal.rs (will be deleted)
    - crates/vector-app/src/relative_time.rs (D-06: delete IF only used by CodespaceState; otherwise strip state_label/state_color functions only — grep usages first)
    - crates/vector-app/Cargo.toml (check if vector-codespaces is a listed dependency; remove if so)
  </read_first>
  <action>
    Mechanical removal of all Codespaces UI surface per D-06 inventory. Execute in this order so the workspace builds at each step (or, per D-08 latitude, combine with Task 1 commit):

    **2a. `crates/vector-app/src/lib.rs`:**
    - Delete lines 19-20: `pub mod codespaces_actor;` and `pub mod codespaces_modal;`.
    - In the `UserEvent` enum (lines 108-118), delete these four variants verbatim:
      - `OpenCodespacesPicker,`
      - `CodespacesLoaded(std::sync::Arc<Vec<vector_codespaces::Codespace>>),`
      - `CodespacesLoadFailed(String),`
      - `CodespaceStateChanged { name: String, state: vector_codespaces::CodespaceState, },`
    - Keep the surrounding comment block intact unless it references only the deleted variants (in which case delete it too).

    **2b. `crates/vector-app/src/app.rs`:** delete every reference enumerated below verbatim — these line numbers are pre-removal (line numbers will shift as you go; use grep to locate each anchor before each edit):
    - Line ~158: `codespaces_modal: Option<crate::codespaces_modal::CodespacesPickerModal>,` (struct field)
    - Line ~162: `codespaces_client: Option<std::sync::Arc<vector_codespaces::CodespacesClient>>,` (struct field)
    - Line ~221: `codespaces_modal: None,` (struct init)
    - Line ~423: the entire arm/branch that calls `let _ = proxy.send_event(UserEvent::OpenCodespacesPicker);` (likely the AppShortcut::OpenCodespacesPicker handler at line 736-738; check both sites)
    - Lines ~459-497: `handle_open_codespaces_picker` method (entire fn)
    - Lines ~502-535: `handle_codespaces_loaded` method (entire fn, takes `list: &std::sync::Arc<Vec<vector_codespaces::Codespace>>`)
    - Lines ~536-572: any neighboring `handle_codespaces_*` methods (`handle_codespace_state_changed`, `handle_codespaces_load_failed`, etc.) — grep `fn handle_codespace` to enumerate
    - Lines ~574-618: `handle_save_codespace_profile` or similar (only if codespaces-coupled)
    - Lines ~736-738: `AppShortcut::OpenCodespacesPicker => { ... }` arm in shortcut dispatch
    - Lines ~1763: `let token_present = vector_codespaces::TokenStore::new().load_access().is_some();` and the surrounding block (grep `vector_codespaces::TokenStore` to find both sites)
    - Lines ~1945: same `TokenStore` pattern
    - Lines ~2039-2046: `if let Some(picker) = self.codespaces_modal.take() { ... }` and `let _ = vector_codespaces::TokenStore::new().clear();`
    - Every match arm dispatching `UserEvent::OpenCodespacesPicker`, `UserEvent::CodespacesLoaded(...)`, `UserEvent::CodespacesLoadFailed(...)`, `UserEvent::CodespaceStateChanged{..}`, and `UserEvent::AuthSignInRequested`/`UserEvent::SignOut`/`UserEvent::AuthRequired`/`UserEvent::AuthDisplayCode`/`UserEvent::AuthCompleted`/`UserEvent::AuthFailed` if those are GitHub-only (per D-05; verify in the file — if `AuthSignInRequested` is solely the GitHub device flow, delete; the Microsoft equivalents are `MicrosoftSignInRequested` and friends and stay).
    - Any `use vector_codespaces::...;` import lines

    Per D-05 and D-06: if `auth_actor.rs` only contained the GitHub device-flow driver (Plan 06-02), delete the file entirely and remove `pub mod auth_actor;` from lib.rs. If it has a generic OAuth scaffold reused by Microsoft auth, keep that and delete only the GitHub-coupled pieces. Determine by reading `auth_actor.rs` first.

    Similarly delete the `AuthDeviceFlowModal` references in lib.rs / app.rs / menu.rs if they were GitHub-only (`crate::auth_modal::AuthDeviceFlowModal`). The Microsoft equivalent is `microsoft_auth_modal` and stays.

    **2c. `crates/vector-app/src/menu.rs`:**
    - Delete lines 436-453: the entire `Codespaces…` NSMenuItem block (`let codespaces = NSMenuItem::new(mtm); ... app_submenu.insertItem_atIndex(&codespaces, 2);`). Also delete the `let sep = NSMenuItem::separatorItem(mtm);` at line 447 only if it was specifically added for the Codespaces row — verify by checking whether any other item used it.
    - Adjust subsequent `insertItem_atIndex` indices: after removing the Codespaces item at index 2, the separator (was index 3) becomes index 2.
    - Delete lines 526-529: the `#[unsafe(method(openCodespaces:))]` method on `AuthMenuTarget`.
    - Per D-06 (and CONTEXT.md "Claude's Discretion"): consider renaming `install_phase6_items` to `install_app_menu_items` (or similar) — this is OPTIONAL. If renamed, also update the call site in main.rs/app.rs and any test references. If NOT renamed, leave the name as-is — downstream Plan 09.1-03 must use the actual name in scope.

    **2d. `crates/vector-app/src/codespaces_actor.rs` and `crates/vector-app/src/codespaces_modal.rs`:** delete both files.

    **2e. `crates/vector-app/src/relative_time.rs`:**
    - Grep for callers: `grep -rn "relative_time\|state_label\|state_color" crates/vector-app/src/`.
    - If the ONLY callers were in `codespaces_modal.rs` / `codespaces_actor.rs` (now deleted), delete the file and remove `pub mod relative_time;` from lib.rs.
    - If other callers remain, strip only the `state_label`/`state_color` functions that take a `vector_codespaces::CodespaceState`.

    **2f. `crates/vector-app/Cargo.toml`:**
    - Remove the `vector-codespaces` dep from `[dependencies]` if listed.

    **2g. Tests:**
    - Run `grep -rln "vector_codespaces\|CodespacesPickerModal\|codespaces_actor" crates/vector-app/tests/` and delete or strip any test files that exercise the deleted code paths.

    Compile-check after each substep when feasible. Commit can be one (combined with Task 1 per D-08) or two (Task 1 commit, then this Task 2 commit) — the user's repo workflow is "Commit each logical stage separately; do not push." DO NOT push.
  </action>
  <verify>
    <automated>cd /Users/ashutosh/personal/vector &amp;&amp; cargo build --workspace 2>&amp;1 | tail -10 &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | tail -10 &amp;&amp; cargo test --workspace --no-run 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rIn "vector_codespaces" crates/` (zero hits in code)
    - `! grep -rIn "codespaces_modal\|codespaces_actor\|CodespacesPickerModal\|CodespacesLoaded\|CodespacesLoadFailed\|CodespaceStateChanged\|OpenCodespacesPicker" crates/` (or matches are only in comments referencing the historical pivot; per D-07)
    - `! test -f crates/vector-app/src/codespaces_actor.rs`
    - `! test -f crates/vector-app/src/codespaces_modal.rs`
    - `! grep -q 'openCodespaces' crates/vector-app/src/menu.rs`
    - `! grep -q "Codespaces" crates/vector-app/src/menu.rs` (the menu item text is gone; any leftover hits would be in comments)
    - `cargo build --workspace` exits 0
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `cargo test --workspace --no-run` exits 0 (tests compile)
  </acceptance_criteria>
  <done>
    No vector_codespaces imports remain; the Codespaces menu item is gone; the codespaces_* modules are deleted; the 4 Codespaces UserEvent variants are removed; workspace builds clean and tests compile.
  </done>
</task>

</tasks>

<verification>
- `cargo build --workspace` clean
- `cargo clippy --workspace --all-targets -- -D warnings` clean
- `cargo test --workspace` clean (note: a green `cargo test` is what D-07 demands)
- `grep -rIn "codespace\|Codespace\|CODESPACE" crates/` returns only historical-pivot comments OR zero matches (D-07)
- Manual: `cargo run --release`, click Vector menu — no `Codespaces…` item
</verification>

<success_criteria>
The Codespaces UI surface (crate, menu item, modal, actor, UserEvent variants, GitHub OAuth code paths) is fully removed; the workspace builds and tests clean. Maps to ROADMAP §Phase 9.1 Success Criteria #2.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-prior-phase-gap-closure/09.1-02-SUMMARY.md`
</output>
