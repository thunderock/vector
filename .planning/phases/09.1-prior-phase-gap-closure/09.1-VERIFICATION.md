---
phase: 09.1-prior-phase-gap-closure
verified: 2026-05-27T05:44:39Z
status: passed
score: 5/5 must-haves verified
---

# Phase 9.1: Prior-Phase Gap Closure Verification Report

**Phase Goal:** Close five user-visible incompletions discovered during Phase 9 UAT so that Phase 9's reconnect/persist features are reachable end-to-end and v1.0.0 can ship with a polished surface.
**Verified:** 2026-05-27T05:44:39Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | Typing `exit` in a local pane closes the pane (PaneExited no longer log-only stub) | VERIFIED | `crates/vector-app/src/app.rs:1564-1578` invokes `Mux::try_get().map(|mux| mux.close_pane(pane_id))`, cancels per-pane token, drops reconnect tracking, exits event loop on LastWindowClosed |
| 2 | Codespaces menu item + crate + actor + modal + UserEvent variants + `install_phase6_items` all removed | VERIFIED | `find crates/ -name "codespaces*"` empty; `grep` finds zero code refs (only doc-comments mentioning historical mirroring); `crates/vector-app/src/menu.rs:377` confirms removal comment |
| 3 | `install_microsoft_menu_items` called at startup | VERIFIED | `crates/vector-app/src/app.rs:1454` calls `menu::install_microsoft_menu_items(mtm, proxy)` right after `install_main_menu()` in `resumed`; definition at `crates/vector-app/src/menu.rs:401` |
| 4 | Dev Tunnels picker shows clickable sign-in affordance in `NotSignedIn` state | VERIFIED | `devtunnels_modal.rs:123` holds `signin_button: Retained<NSButton>`; `make_signin_button` (line 329) wires `microsoftSignIn:` action (line 351); ObjC bridge dispatches `UserEvent::MicrosoftSignInRequested` (line 414) |
| 5 | Bundled `Vector.app` has CFBundleIconFile populated; `.icns` packaged; xtask icon wired | VERIFIED | `resources/Info.plist.partial:9-10` declares `<key>CFBundleIconFile</key><string>icon</string>`; `resources/icon.icns` exists (100997 bytes, Mac OS X icon); `resources/icon.svg` exists; `xtask/src/main.rs:8,32,51` declares `Cmd::Icon → icon::generate_icns`; `xtask/src/icon.rs` populates `icon.iconset/` |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `crates/vector-app/src/app.rs` (PaneExited handler) | close_pane cascade replaces log stub | VERIFIED | Lines 1564-1578: cancel token, remove reconnect entries, close_pane via Mux, exit on LastWindowClosed |
| `crates/vector-codespaces/` | Crate deleted | VERIFIED | Directory absent |
| `crates/vector-app/src/codespaces_actor.rs` | File deleted | VERIFIED | Absent (only `devtunnels_actor.rs` doc-comment mentions historical mirroring) |
| `crates/vector-app/src/codespaces_modal.rs` | File deleted | VERIFIED | Absent (only `devtunnels_modal.rs` doc-comment mentions it) |
| `crates/vector-app/src/menu.rs` (Microsoft section) | `install_microsoft_menu_items` defined and reachable | VERIFIED | Defined at line 401; cleanup comment at 377 confirms phase-6 items removed |
| `crates/vector-app/src/app.rs` (call site) | Calls `install_microsoft_menu_items` in `resumed` | VERIFIED | Line 1454 invokes inside unsafe main-thread block |
| `crates/vector-app/src/devtunnels_modal.rs` (signin button) | NSButton constructed + action wired | VERIFIED | `signin_button` field, `make_signin_button` factory, `SigninTarget` ObjC subclass dispatching `UserEvent::MicrosoftSignInRequested` |
| `crates/vector-app/resources/icon.svg` | SVG asset present | VERIFIED | 547 bytes |
| `crates/vector-app/resources/icon.icns` | ICNS asset built | VERIFIED | 100997 bytes, valid ic12 type |
| `crates/vector-app/resources/Info.plist.partial` | `CFBundleIconFile=icon` declared | VERIFIED | Lines 9-10 |
| `xtask/src/icon.rs` + main.rs wiring | `xtask icon` subcommand routes to generate_icns | VERIFIED | `mod icon` declared, `Cmd::Icon` variant, dispatch arm in match |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `PaneExited` event → close pane | `vector_mux::Mux::close_pane` | direct call | WIRED | app.rs:1573 `Mux::try_get().map(|mux| mux.close_pane(pane_id))` |
| `resumed` lifecycle → Microsoft menu install | `menu::install_microsoft_menu_items` | direct call w/ proxy | WIRED | app.rs:1454, guarded by MainThreadMarker + proxy presence |
| Sign-in NSButton click → app event loop | `UserEvent::MicrosoftSignInRequested` | ObjC selector `microsoftSignIn:` → proxy.send_event | WIRED | devtunnels_modal.rs:408-414 dispatches via stored EventLoopProxy |
| `Info.plist.partial` → bundled `.app` | cargo-bundle merges partial plist + ships `icon.icns` | bundle pipeline | WIRED | Partial plist declares CFBundleIconFile=icon; icon.icns sits alongside in resources/ |
| `xtask icon` → regenerate icon.icns | `icon::generate_icns(sh)` | direct fn call | WIRED | main.rs:51 dispatches Cmd::Icon variant |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| PaneExited handler | `pane_id` | `vector_pty::pty_actor` emits on EOF (PTY exit); routed through winit event loop | Yes — real PTY exit | FLOWING |
| Microsoft sign-in button | click → `UserEvent::MicrosoftSignInRequested` | NSButton target/action via ObjC SigninTarget subclass | Yes — real user click | FLOWING |
| Icon at runtime | macOS Finder reads CFBundleIconFile from Info.plist + loads icon.icns | cargo-bundle includes both | Yes — real bundle assets | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Workspace builds cleanly post-changes | `cargo check --workspace` | `Finished dev profile in 2.84s` (no errors, no warnings) | PASS |
| No lingering Codespaces code references | `grep -rIn -E "vector_codespaces\|codespaces_actor\|codespaces_modal\|OpenCodespacesPicker\|CodespacesLoaded\|CodespacesLoadFailed\|CodespaceStateChanged\|install_phase6_items" crates/` | All matches are comments (`//!` or `//`); zero code refs | PASS |
| `cargo test --workspace` per notes | (run by user prior to verification) | 478 tests, 0 failures (per phase notes) | PASS |
| `icon.icns` is a valid macOS icon | `file crates/vector-app/resources/icon.icns` | `Mac OS X icon, 100997 bytes, "ic12" type` | PASS |

### Requirements Coverage

Per phase notes: "none new — closes execution debt against existing requirements".

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| WIN-02 (pane lifecycle) | 09.1-01 | Local PTY exit closes its pane | SATISFIED | PaneExited handler now calls Mux.close_pane (app.rs:1564-1578) |
| Pivot cleanup (Phase 7) | 09.1-02 | Codespaces surface removed after tunnel pivot | SATISFIED | Crate + actor + modal + UserEvent variants + menu item all gone |
| DT-03 (sign-in path) | 09.1-03, 09.1-04 | Microsoft sign-in reachable from menu and picker | SATISFIED | Menu items installed at startup; NSButton in NotSignedIn footer |
| Build polish (Phase 1) | 09.1-05 | Bundled app ships with branded icon | SATISFIED | CFBundleIconFile set; icon.icns + iconset present; xtask icon wired |

### Anti-Patterns Found

None. Inspected each touched file for TODO/FIXME/placeholder/empty-impl patterns:

- `app.rs:1564-1578` — no TODO, real Mux call, proper cleanup
- `menu.rs:401-476` — fully implemented helper; warning logs are diagnostic, not stubs
- `devtunnels_modal.rs` signin path — real NSButton + real ObjC bridge dispatching real UserEvent
- `xtask/src/icon.rs` — real shell pipeline (sips + iconutil) generating concrete iconset

### Human Verification Required

None blocking. Optional smoke confirmations (already covered by Phase 9 UAT loop):

1. Visual confirmation that bundled `.app` shows the new V icon in Finder/Dock.
2. Click Microsoft sign-in NSButton in picker and observe device-code flow start.

Neither blocks goal achievement — both are downstream side-effects of artifacts already verified in code.

### Gaps Summary

No gaps. All five Phase 9 UAT incompletions are closed in the codebase:

1. **PaneExited** now executes the full close cascade — local `exit` will dismiss the pane.
2. **Codespaces surface** is fully excised — no crate, no actor, no modal, no events, no menu item.
3. **Microsoft menu items** are installed at `resumed` time alongside the main menu.
4. **Dev Tunnels picker** carries a real NSButton wired to `MicrosoftSignInRequested`.
5. **App bundle icon** is declared in `Info.plist.partial`, the `.icns` is on disk, and `xtask icon` regenerates it from the SVG source.

Workspace compiles cleanly; 478 tests pass per notes. Phase 9.1 has achieved its goal.

---

_Verified: 2026-05-27T05:44:39Z_
_Verifier: Claude (gsd-verifier)_
