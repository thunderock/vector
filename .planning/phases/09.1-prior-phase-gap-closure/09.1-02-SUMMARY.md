---
phase: 09.1-prior-phase-gap-closure
plan: 02
subsystem: cleanup
tags: [codespaces-removal, github-oauth-removal, workspace-pruning, arch-tests, auth]

# Dependency graph
requires:
  - phase: 06-github-auth-codespaces-picker
    provides: GitHub OAuth + Codespaces UI surface being removed wholesale
  - phase: 07-ssh-transport-codespaces-connect
    provides: pivot decision (2026-05-19) that orphaned the Phase-6 UI surface
  - phase: 09.1-prior-phase-gap-closure
    provides: D-04..D-08 (locked decisions for crate deletion + GitHub OAuth removal)
provides:
  - vector-codespaces crate removed from workspace
  - GitHub OAuth code paths removed from vector-app (auth_actor, auth_modal, install_auth_menu_items, rebuild_auth_menu_section)
  - 10 Phase-6 UserEvent variants removed (AuthSignInRequested, AuthDisplayCode, AuthCompleted, AuthFailed, AuthRequired, SignOut, OpenCodespacesPicker, CodespacesLoaded, CodespacesLoadFailed, CodespaceStateChanged)
  - 2 Phase-6 AppShortcut variants removed (OpenCodespacesPicker, SignInWithGitHub)
  - vector-codespaces/src dropped from Pitfall-14 SCAN_PATHS
  - relative_time.rs preserved as humanize-only (devtunnels_modal::format_row dep)
affects: [09.1-03-microsoft-menu-wiring, 09.1-04-picker-signin-affordance, 10-04-release-tag]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Atomic workspace+arch-test commit (D-08): crate-membership + SCAN_PATHS must update together or the first commit fails clippy"
    - "Wholesale-OAuth-path removal (D-05): Microsoft is sole sign-in path in v1; no GitHub fallback"

key-files:
  created:
    - crates/vector-app/src/relative_time.rs (re-created humanize-only; vector_codespaces dep dropped)
  modified:
    - Cargo.toml (workspace [members] pruned)
    - Cargo.lock (regenerated; vector-codespaces transitive deps gone)
    - crates/vector-app/Cargo.toml (drop vector-codespaces dep; drop zeroize)
    - crates/vector-app/src/lib.rs (drop 4 modules, 10 UserEvent variants)
    - crates/vector-app/src/app.rs (drop 4 App fields, ~280 lines of Phase-6 handlers + match arms)
    - crates/vector-app/src/menu.rs (drop install_auth_menu_items + rebuild_auth_menu_section + AuthMenuTarget)
    - crates/vector-input/src/keymap.rs (drop 2 AppShortcut variants + Cmd-Shift-G binding)
    - crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs (drop vector-codespaces/src from SCAN_PATHS)
  deleted:
    - crates/vector-codespaces/ (whole crate; already absent at HEAD from prior parallel agent run)
    - crates/vector-app/src/auth_actor.rs
    - crates/vector-app/src/auth_modal.rs
    - crates/vector-app/src/codespaces_actor.rs
    - crates/vector-app/src/codespaces_modal.rs
    - crates/vector-app/tests/auth_modal_state.rs
    - crates/vector-app/tests/relative_time.rs

key-decisions:
  - "D-04: delete vector-codespaces crate wholesale (no soft-deprecate)"
  - "D-05: no GitHub OAuth in v1; Microsoft is sole sign-in"
  - "D-08: single atomic commit covering crate deletion + arch-test SCAN_PATHS"
  - "relative_time.rs preserved as humanize-only (devtunnels_modal::format_row still depends on humanize)"
  - "AppShortcut::OpenCodespacesPicker + SignInWithGitHub removed from vector-input (not just app.rs) to keep the keymap surface honest"
  - "Cmd-Shift-G keybind freed (was OpenCodespacesPicker); available for v2 reassignment"

patterns-established:
  - "When deleting a workspace member, the arch-test SCAN_PATHS const must update in the same commit (D-08) — splitting causes the first commit to fail clippy because the scan-path points at a now-missing dir."
  - "vector-config::Kind::Codespace and append_codespace_profile helpers remain in vector-config — not in Gap B scope; touched only via the deleted vector-app callsites. These are dormant but reachable through the public crate API and have their own tests."

requirements-completed: []

# Metrics
duration: 18min
completed: 2026-05-26
---

# Phase 9.1 Plan 02: Codespaces Decommission Summary

**vector-codespaces crate + GitHub OAuth + Codespaces UI surface removed wholesale; Microsoft sign-in remains the sole v1 sign-in path; workspace builds and tests clean.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-26T22:09Z
- **Completed:** 2026-05-26T22:27Z
- **Tasks:** 2 (combined into 1 atomic commit per D-08)
- **Files modified:** 9 (8 modified + 1 re-created)
- **Files deleted:** 7 (whole crate + 6 vector-app files — most already absent in HEAD from a prior parallel agent run)

## Accomplishments

- `vector-codespaces` crate fully gone from the workspace; Cargo.lock pruned of all transitive deps it was pulling in.
- vector-app no longer imports `vector_codespaces` anywhere in code (zero `grep -rIn "vector_codespaces" crates/` hits).
- Vector menu's `Codespaces…` item, `Sign in with GitHub`, and `Sign out` items are gone (the entire `install_auth_menu_items` + `rebuild_auth_menu_section` + `AuthMenuTarget` block deleted from `menu.rs`).
- Pitfall-14 token-redaction arch-test scan list shrank in lockstep with the deletion — no first-commit lint failure.
- vector-input's keymap surface trimmed: `Cmd-Shift-G` (OpenCodespacesPicker) freed; `AppShortcut::SignInWithGitHub` removed.
- D-07 acceptance: `cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` all green; `grep -rIn "codespace" crates/` returns only historical-pivot comments and the still-dormant `vector-config::Kind::Codespace` helpers (out of Gap B scope).

## Task Commits

1. **Task 1+2 (combined per D-08): Delete vector-codespaces crate + strip vector_codespaces consumers from vector-app** — `2701942` (refactor)

Single atomic commit covering: workspace member removal, arch-test SCAN_PATHS shrink, vector-app source surgery (`lib.rs` modules + UserEvent variants, `app.rs` fields + handlers + match arms, `menu.rs` GitHub auth menu block, `Cargo.toml` deps), `vector-input` AppShortcut trims, and the humanize-only `relative_time.rs` re-creation.

## Files Created/Modified

### Modified
- `Cargo.toml` — drop `crates/vector-codespaces` from `[workspace] members`.
- `Cargo.lock` — regenerated; vector-codespaces' transitive dep tree (octocrab portions, keyring bindings, etc.) pruned.
- `crates/vector-app/Cargo.toml` — drop `vector-codespaces` dep; drop `zeroize` (auth_actor was sole user); keep `chrono` (devtunnels_actor) + `tokio-util` (Microsoft device flow).
- `crates/vector-app/src/lib.rs` — drop `pub mod auth_actor` / `auth_modal` / `codespaces_actor` / `codespaces_modal` (kept `relative_time` since devtunnels_modal still uses `humanize`); drop the 10 Phase-6 UserEvent variants.
- `crates/vector-app/src/app.rs` — net -498 / +0 (mostly `-`): drop 4 App fields (`auth_modal`, `pending_auth_cancellation`, `codespaces_modal`, `codespaces_client`), drop all Phase-6 handler methods (handle_auth_*, handle_open_codespaces_picker, handle_codespaces_loaded, codespaces_connect/start/save/dismiss, codespaces_picker_is_key, tick_auth_modal), drop the `tick_auth_modal()` call in `render_window`, drop the install_auth_menu_items + Keychain TokenStore lookup at startup, drop the ProfileSelected divert-to-GitHub-sign-in branch, drop the 10 UserEvent match arms, drop the entire Codespaces picker keyboard-routing block.
- `crates/vector-app/src/menu.rs` — drop the entire Phase-6 auth menu section: `install_auth_menu_items`, `rebuild_auth_menu_section`, `AuthMenuRefs`, `AUTH_MENU_REFS`, and the inner `auth_target` module containing `AuthMenuTarget`. Microsoft section + `SignInState` enum + `install_microsoft_menu_items` preserved.
- `crates/vector-input/src/keymap.rs` — drop `AppShortcut::OpenCodespacesPicker` + `AppShortcut::SignInWithGitHub` variants and the `"G/g" → OpenCodespacesPicker` Cmd-Shift binding.
- `crates/vector-arch-tests/tests/no_token_in_debug_or_log.rs` — drop `"vector-codespaces/src"` from `SCAN_PATHS`; drop the trailing Phase-6 scan-path assertion.

### Re-created
- `crates/vector-app/src/relative_time.rs` — humanize + humanize_option only (no `vector_codespaces::CodespaceState` dependency). `devtunnels_modal::format_row` uses `humanize` for the `· {last_seen}` row template.

### Deleted (already absent in HEAD from a parallel agent run prior to this session — confirmed via `git log --diff-filter=D`)
- `crates/vector-codespaces/` (whole crate)
- `crates/vector-app/src/auth_actor.rs`
- `crates/vector-app/src/auth_modal.rs`
- `crates/vector-app/src/codespaces_actor.rs`
- `crates/vector-app/src/codespaces_modal.rs`
- `crates/vector-app/tests/auth_modal_state.rs`
- `crates/vector-app/tests/relative_time.rs`

## Decisions Made

- **Combined Task 1 + Task 2 into one commit** (D-08 explicitly permits this; the alternative was two commits but the first would fail clippy because vector-app still imported `vector_codespaces`). The atomic commit invariant from D-08 is satisfied either way.
- **Kept `vector-input::AppShortcut::OpenCodespacesPicker` removal in same commit** rather than splitting into a vector-input PR — the keymap is small and the variant referenced a now-deleted UserEvent.
- **Did not rename `install_phase6_items`** — the function never existed under that name; the actual symbol was `install_auth_menu_items`, which is fully deleted. So no rename was needed; the Discretion call in CONTEXT.md is moot.
- **Preserved `vector-config::Kind::Codespace` and `append_codespace_profile`** — these live in `vector-config`, not `vector-app`, and they have their own unit tests. They are now dormant (no callers in vector-app) but reachable as crate-public API. Removing them is out of Gap B scope and would require a follow-up cleanup.

## Deviations from Plan

### Workflow deviations (not deviation-rule deviations)

**1. Several files were already deleted at HEAD before this session started.**
- **Found during:** Task 1 setup (`git rm crates/vector-codespaces/...` succeeded with normal output, but later `git status` showed no staged deletions for these paths — they were already absent from HEAD).
- **Root cause:** Commit `21767ee2` ("fix(09.1-01): replace PaneExited log-only stub with close_pane handler") accidentally bundled in the deletion of `vector-codespaces/`, `auth_actor.rs`, `auth_modal.rs`, `codespaces_actor.rs`, `codespaces_modal.rs`, `auth_modal_state.rs`, `relative_time.rs`, and `tests/relative_time.rs` (scope spilled from Gap A into Gap B). When Plan 09.1-02 started, those files were already gone from the index.
- **Impact:** Plan 09.1-02 became "fix the leftover consumers that 09.1-01 didn't clean up" rather than "delete files + clean consumers." The acceptance criteria still hold; the net commit covers the same surface, just shifted toward the .rs surgery side. No re-work needed.

### Auto-fixed Issues

**1. [Rule 2 — Missing Critical] `relative_time` module reconstructed humanize-only**
- **Found during:** Combined Task 1+2 (cargo build error: `failed to resolve: could not find relative_time in the crate root` from `devtunnels_modal.rs:96`).
- **Issue:** Plan's read-first guidance said "delete the file IF its only use is CodespaceState; otherwise strip state_label/state_color functions only." The earlier 09.1-01 commit deleted the file unconditionally — but `devtunnels_modal::format_row` calls `relative_time::humanize` for the `· {last_seen}` row column. Build broke.
- **Fix:** Re-created `relative_time.rs` with `humanize` + `humanize_option` only (no `vector_codespaces::CodespaceState` dep). `state_label` + `state_color` stay deleted (those functions did take a `CodespaceState`).
- **Files modified:** `crates/vector-app/src/relative_time.rs` (new), `crates/vector-app/src/lib.rs` (re-added `pub mod relative_time;`).
- **Verification:** `cargo build --workspace` clean.
- **Committed in:** 2701942 (Task 1+2 commit).

**2. [Rule 1 — Bug] Removed unused `NamedKey` import after deleting picker keyboard-routing block**
- **Found during:** Build warned `unused import: NamedKey`.
- **Issue:** `winit::keyboard::NamedKey` was imported solely for the Codespaces picker keyboard routing block (Enter/Esc/Arrow keys); after that block was deleted, the import was orphaned. `clippy -D warnings` would fail.
- **Fix:** Narrowed import to `use winit::keyboard::Key;` only.
- **Files modified:** `crates/vector-app/src/app.rs`.
- **Verification:** `cargo clippy --workspace --all-targets -- -D warnings` clean.
- **Committed in:** 2701942 (same commit).

---

**Total deviations:** 2 auto-fixed (1 missing critical, 1 bug)
**Impact on plan:** Both auto-fixes essential to keep the workspace building. No scope creep beyond plan boundaries.

## Issues Encountered

- **Phantom `git status`:** After the initial `git rm -r crates/vector-codespaces`, `git status` showed no staged deletions for those paths. Investigation revealed commit `21767ee2` had already deleted them. The session adapted: the `git rm` commands were no-ops, and the actual surgery work shifted entirely to the `.rs` consumer-removal side.
- **`auth_modal.rs` deletion not in plan:** Plan listed `auth_modal.rs` only obliquely ("delete AuthDeviceFlowModal references"). Since `auth_modal.rs` exclusively hosts `AuthDeviceFlowModal` and is exclusively coupled to the GitHub device flow, it was deleted wholesale, matching D-05.

## User Setup Required

None — internal cleanup, no external services or config touched.

## Next Phase Readiness

- **09.1-03 (Microsoft menu wiring) unblocked:** `install_microsoft_menu_items` exists in `menu.rs:556` (renumbered after deletion of the Phase-6 block). The menu surface no longer has the GitHub items above it, so Microsoft items will sit at the natural top of the Vector menu when `main.rs` calls them.
- **09.1-04 (picker sign-in affordance) unblocked:** Footer copy in `devtunnels_modal.rs:54` can be updated per D-15 (`Sign in with GitHub or Microsoft …` → `Sign in with Microsoft …`).
- **Dormant `vector-config` Codespaces helpers** (`Kind::Codespace`, `append_codespace_profile`, `derive_profile_name`) remain reachable through the public API. Out of Gap B scope; flagged for a future cleanup phase if any code path is added that would rely on their absence.
- **Cmd-Shift-G keybind freed** — available for v2 reassignment.

---
*Phase: 09.1-prior-phase-gap-closure*
*Plan: 02*
*Completed: 2026-05-26*

## Self-Check: PASSED

- SUMMARY.md created at `.planning/phases/09.1-prior-phase-gap-closure/09.1-02-SUMMARY.md`
- `crates/vector-app/src/relative_time.rs` re-created (humanize-only)
- `crates/vector-codespaces/`, `auth_actor.rs`, `auth_modal.rs`, `codespaces_actor.rs`, `codespaces_modal.rs` all confirmed gone from working tree
- Commit `2701942` present in git log
- `cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` all green
