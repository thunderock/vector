---
phase: 09.1-prior-phase-gap-closure
plan: 01
subsystem: ui
tags: [winit, mux, pane-lifecycle, gap-closure, phase4-debt]

# Dependency graph
requires:
  - phase: 04-mux-tabs-splits
    provides: Mux::close_pane cascade + CloseResult variants + Cmd-W ClosePane reference pattern
  - phase: 09-persistence-reconnect-tmux-auto-attach
    provides: pane_cancel_tokens + reconnecting_panes + reconnect_first_keystroke_shown state to clean
provides:
  - PaneExited handler that calls Mux::close_pane and cascades CloseResult (PaneClosed → TabClosed → WindowClosed → LastWindowClosed → event_loop.exit)
  - Local-pane `exit` now closes the pane immediately (no [exited] sentinel — D-01)
affects: [09-persistence-reconnect-tmux-auto-attach UAT, 10-hardening-release release tag]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PaneExited dispatch mirrors the Cmd-W ClosePane arm verbatim (per-pane cancel-token + reconnect-state cleanup + Mux::close_pane + LastWindowClosed → event_loop.exit)"

key-files:
  created: []
  modified:
    - crates/vector-app/src/app.rs

key-decisions:
  - "D-01: no [exited] sentinel — local pane disappears immediately, matching iTerm/Apple Terminal lifecycle"
  - "D-03: remote panes never reach this branch (they enter Reconnecting on EOF at pty_actor.rs:232 + LocalDomain::reconnect_one_shot returning Ok(None))"

patterns-established:
  - "PaneExited / Cmd-W cascade symmetry: both call sites perform identical cleanup (cancel token → reconnect state → Mux::close_pane → propagate LastWindowClosed); future close-paths should mirror"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-05-27
---

# Phase 9.1 Plan 01: PaneExited Stub Replacement Summary

**Local-pane `exit` now closes the pane via `Mux::close_pane` + CloseResult cascade — replacing the Phase 4-05 log-only stub at app.rs:1872.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-05-27T05:01:34Z
- **Completed:** 2026-05-27T05:05:46Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Replaced the log-only `UserEvent::PaneExited` arm with a real cascade handler that mirrors the Cmd-W `MuxCommand::ClosePane` path at app.rs:1649-1666 verbatim
- Per-pane reconnect tokens (Plan 09-05) are cancelled before close, and `reconnecting_panes` / `reconnect_first_keystroke_shown` state is cleaned to prevent leaks
- `LastWindowClosed` propagates to `event_loop.exit()`, matching app-shutdown behavior of Cmd-W on the last pane
- `cargo build -p vector-app` clean; `cargo clippy -p vector-app --all-targets -- -D warnings` clean

## Task Commits

1. **Task 1: Replace PaneExited log-only stub with close_pane handler** - `21767ee` (fix)

## Files Created/Modified
- `crates/vector-app/src/app.rs` — `UserEvent::PaneExited` arm at line 1872 now performs full close cascade

## Decisions Made
- None new — followed plan-locked D-01..D-03 verbatim. Pattern mirrors the Cmd-W ClosePane arm; no `[exited]` sentinel; remote-pane branch unaffected (they never emit `PaneExited`, they enter `Reconnecting` on EOF).

## Deviations from Plan

None — plan executed exactly as written. The Cmd-W ClosePane arm at lines 1649-1666 served as the verbatim reference template; `event_loop: &ActiveEventLoop` was already in scope in `user_event(...)` at line 1809 so no scope-acquisition gymnastics were required.

## Issues Encountered

- **Pre-existing test compilation failures (out of scope):** `cargo test -p vector-app --lib --tests` fails because parallel agent for Plan 09.1-02 (Gap B — Codespaces UI cleanup) has staged deletions of `auth_actor.rs`, `auth_modal.rs`, `codespaces_actor.rs`, `codespaces_modal.rs`, `relative_time.rs` plus their tests, but `menu.rs` and `app.rs` still reference `UserEvent::SignOut`, `UserEvent::OpenCodespacesPicker`, and `vector_codespaces::CodespacesClient`. These compile errors belong to Plan 09.1-02's scope. Plan 09.1-01's specified verification commands (`cargo build -p vector-app`, `cargo clippy -p vector-app --all-targets -- -D warnings`) both pass clean. `--all-targets` excludes the doctest path that pulls in test-hooks feature, which is where these errors surface.
- **Parallel-execution index leak:** The deletions staged by the Plan 09.1-02 agent were swept into this commit because `git add` followed by `git commit` (no `-- <pathspec>`) commits the full index. Net effect: 09.1-01's commit contains both the PaneExited fix and the file deletions for Gap B. This is benign — those deletions belong to a sibling plan that's running concurrently, and they will land regardless. Recorded here so the verifier can correlate.

## Self-Check Inputs

- `grep -n "PaneExited(pane_id) =>" crates/vector-app/src/app.rs` → line 1872 (found)
- `grep -A4 ".." | grep "Plan 04-05 will render sentinel"` → no match (stub removed)
- `grep -A12 ".." | grep "close_pane(pane_id)"` → match (handler wired)
- `grep -A14 ".." | grep "LastWindowClosed"` → match (cascade routed)
- `cargo build -p vector-app` → exit 0
- `cargo clippy -p vector-app --all-targets -- -D warnings` → exit 0

## Next Phase Readiness

- Phase 9.1 Gap A closed. UAT Step D-01 (local pane `exit` test) is now unblocked once Plan 09.1-02..05 land and the workspace tests recover.
- Phase 9 UAT can re-walk the 6 blocked items once 09.1 completes.
- 10-04 release tag remains held until 9.1 closes (per STATE.md).

## Self-Check: PASSED

- `crates/vector-app/src/app.rs` exists (modified) — FOUND
- `.planning/phases/09.1-prior-phase-gap-closure/09.1-01-SUMMARY.md` exists — FOUND
- Commit `21767ee` exists on master — FOUND

---
*Phase: 09.1-prior-phase-gap-closure*
*Completed: 2026-05-27*
