# Phase 9.1: Prior-Phase Gap Closure — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-26
**Phase:** 09.1-prior-phase-gap-closure
**Areas discussed:** Gap A (PaneExited), Gap B (Codespaces decommission), Gap D (Picker sign-in), Gap E (App icon)
**Skipped (mechanical):** Gap C (Microsoft menu wiring) — single function call, no design space.

---

## Gap A — PaneExited behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Close immediately | Pane disappears; tab closes if last pane; window closes if last tab. iTerm/Apple Terminal default. | ✓ |
| Render `[exited]` sentinel | Pane stays open with banner at top; output preserved; Cmd-W dismisses. | |
| Close immediately + config knob | Default close, with TOML `[panes] hold_on_exit` toggle. | |

**User's choice:** Close immediately (recommended).
**Notes:** No config knob in v1 — matches scope discipline. Reconsider in v2.

---

## Gap B — Codespaces decommission strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Delete the entire crate | Remove `vector-codespaces` wholesale + delete `auth_actor.rs` GitHub usage + remove arch-test scan-path. No GitHub-auth path in v1. | ✓ |
| Split: keep token-store + GitHubAuth as `vector-github-auth` | Rename the crate, delete only `client.rs` + `model.rs`. Preserves OAuth for v2. | |
| Delete codespaces UI only; leave crate alone | Minimal-touch; keeps orphan crate reachable only via `auth_actor.rs`. | |

**User's choice:** Delete the entire crate.
**Notes:** Implies v1 has no GitHub auth at all — Microsoft is the only sign-in. Simpler surface, smaller binary. `vector-github-auth` was offered as a v2 re-introduction path (deferred).

---

## Gap D — Picker sign-in affordance

| Option | Description | Selected |
|--------|-------------|----------|
| Button in picker | NSButton with MICROSOFT_BLUE tint above the footer; fires same actor command as menu. | ✓ |
| Auto-present device-flow modal | On first open in NotSignedIn, automatically push the device-flow modal in front of the picker. | |
| Both — button + auto-present on first open | Auto-present once per session; button as recovery on subsequent opens. | |

**User's choice:** Button in picker (recommended).
**Notes:** No auto-present — avoids hijack-feel. UI-SPEC footer copy also amended to drop "GitHub or" since Gap B removes GitHub.

---

## Gap E — App icon source

| Option | Description | Selected |
|--------|-------------|----------|
| Typographic V mark | Monospaced "V" on dark rounded-square; generated by `xtask icon` script → 1024×1024 PNG → `iconutil` → `.icns`. | ✓ |
| Supply own PNG/.icns | Drop file into `resources/`; plan just wires through cargo-bundle. | |
| Skip Gap E for v1 — ship with default | Accept blank dock icon; defer to v2. Phase shrinks 5 → 4 plans. | |

**User's choice:** Typographic V mark (recommended).
**Notes:** Visual specifics (background hex, V color, corner radius) deferred to Claude's discretion. Designed artwork is a v2 enhancement.

---

## Claude's Discretion

- Gap E visual specifics (background color hex, V glyph color, corner radius, font choice for the V).
- Whether to slice Gap B into one large commit or two (crate-removal + app-surgery) — planner picks based on what fits the cargo-husky pre-commit hook gates.
- Whether to rename `install_phase6_items` after Gap B (most of "Phase 6" content leaves with the cleanup).
- Plan execution order across the five gaps (most are independent).

## Deferred Ideas

- Hold-on-exit config knob (Gap A v2)
- `vector-github-auth` crate (Gap B v2 — only if GitHub OAuth re-enters scope)
- Auto-present device-flow on first picker open (Gap D v2 UX polish)
- Designed app icon artwork (Gap E v2)
- Phase 9 deferred polish gaps (glyph row in reconnect bar, light-mode palette wiring, fade-out animation, cursor dim during reconnect) — already noted as Phase 9 deferred items; not in scope for 9.1.
