---
phase: 09.1-prior-phase-gap-closure
plan: 03
type: execute
wave: 2
depends_on: ["09.1-02"]
files_modified:
  - crates/vector-app/src/main.rs
  - crates/vector-app/src/app.rs
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "On app startup, the `Sign in with Microsoft`, `Sign out of Microsoft`, and `Dev Tunnels…` items appear in the Vector menu"
    - "Clicking the menu items routes through MicrosoftMenuTarget to MicrosoftSignInRequested / MicrosoftSignOutRequested / OpenDevTunnelsPickerMenu UserEvents"
    - "The auth-state-change → menu-rebuild chain is intact (rebuild_microsoft_signin_section runs after MicrosoftSignedIn / sign-out)"
  artifacts:
    - path: "crates/vector-app/src/app.rs OR crates/vector-app/src/main.rs"
      provides: "install_microsoft_menu_items invocation after main menu install"
      contains: "install_microsoft_menu_items"
  key_links:
    - from: "main-thread startup path (App::resumed or fn main)"
      to: "crates/vector-app/src/menu.rs::install_microsoft_menu_items"
      via: "unsafe call with MainThreadMarker + EventLoopProxy<UserEvent>"
      pattern: "install_microsoft_menu_items\\("
---

<objective>
Gap C — Call `install_microsoft_menu_items(mtm, proxy)` at app startup so the `Sign in with Microsoft` / `Sign out of Microsoft` / `Dev Tunnels…` items actually appear in the Vector menu. The function exists at `menu.rs:563` (Plan 08-05) but is never called — verified live in Phase 9 UAT.

Purpose: Without this call, the Microsoft sign-in path is invisible. Users cannot reach the device-flow modal except via Cmd-Shift-T, and the "Sign in with Microsoft" menu item promised by UI-SPEC §S3 doesn't exist at runtime.

Output: The Microsoft menu items are installed on startup, and the auth-state-change → menu-rebuild chain is verified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-prior-phase-gap-closure/09.1-CONTEXT.md
@crates/vector-app/src/main.rs
@crates/vector-app/src/menu.rs

<interfaces>
<!-- install_microsoft_menu_items signature (crates/vector-app/src/menu.rs:563-565) -->
```rust
pub unsafe fn install_microsoft_menu_items(
    mtm: MainThreadMarker,
    proxy: EventLoopProxy<UserEvent>,
)
```

<!-- The function inserts three items at the top of the Vector menu (per Plan 08-05 SUMMARY):
- "Sign in with Microsoft" (fires MicrosoftSignInRequested)
- "Sign out of Microsoft" (fires MicrosoftSignOutRequested; hidden until signed in)
- "Dev Tunnels…" with Cmd-Shift-T (fires OpenDevTunnelsPickerMenu)
Idempotent (OnceLock-gated). Prints `tracing::warn!` if main menu isn't installed yet. -->

<!-- rebuild_microsoft_signin_section exists at menu.rs:638. Plan 08-05 SUMMARY claims app.rs already calls it on MicrosoftSignedIn / sign-out events. Verify this chain during execution per D-11. -->
</interfaces>

D-09: call `install_microsoft_menu_items` after the main menu and `install_phase6_items` (or its renamed successor per Gap B / D-06) complete.
D-10: no new UserEvent variants needed — `MicrosoftSignInRequested` / `MicrosoftSignOutRequested` / `OpenDevTunnelsPickerMenu` already exist (lib.rs lines 157-161).
D-11: verify that the `MicrosoftSignedIn` / sign-out → `rebuild_microsoft_signin_section` chain is intact in app.rs; if missing, add it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire install_microsoft_menu_items at startup and verify the rebuild chain</name>
  <files>crates/vector-app/src/app.rs (most likely site) OR crates/vector-app/src/main.rs</files>
  <read_first>
    - crates/vector-app/src/main.rs (full file; `fn main` does not visibly install the main menu — locate the actual site via grep)
    - crates/vector-app/src/menu.rs (lines 540-640 — `install_microsoft_menu_items` body + `rebuild_microsoft_signin_section`; lines 380-460 — `install_phase6_items` or its renamed successor)
    - crates/vector-app/src/app.rs (grep `install_phase6_items` and `rebuild_microsoft_signin_section` to locate current call sites)
    - .planning/phases/08-vs-code-remote-tunnels-connect/08-05-SUMMARY.md (confirms `install_microsoft_menu_items` is defined but never called)
  </read_first>
  <action>
    Step 1 — Locate the existing menu install site. Run:

    ```
    grep -rn "install_phase6_items\|install_app_menu_items\|install_main_menu\|setMainMenu" crates/vector-app/src/
    ```

    Most likely the main menu is installed inside `App::resumed` (winit's resumed event, which mirrors `applicationDidFinishLaunching`). The existing `install_phase6_items` call is the anchor.

    Step 2 — Add the `install_microsoft_menu_items` call immediately AFTER the existing `install_phase6_items` (or its renamed successor) call. Both `mtm: MainThreadMarker` and `proxy: EventLoopProxy<UserEvent>` are already in scope at that point because the existing call uses them.

    Insert verbatim:

    ```rust
    // Plan 09.1-03 (Gap C): install Microsoft sign-in + Dev Tunnels menu items
    // (defined in Plan 08-05 menu.rs but not yet called).
    unsafe { crate::menu::install_microsoft_menu_items(mtm, proxy.clone()); }
    ```

    Coordination with Plan 09.1-02: If `install_phase6_items` was renamed to `install_auth_menu_items` or `install_app_menu_items` per D-06 latitude, the anchor name changes but the relative insertion point ("immediately after the auth menu install") is the same. Determine the actual name by grep at execution time.

    Step 3 — Verify the rebuild chain (D-11). In `crates/vector-app/src/app.rs`, grep for `rebuild_microsoft_signin_section`:

    ```
    grep -n "rebuild_microsoft_signin_section" crates/vector-app/src/app.rs
    ```

    Per D-11 / Plan 08-05 SUMMARY this chain is wired. The expected call sites:
    - `UserEvent::MicrosoftSignedIn` arm: `unsafe { crate::menu::rebuild_microsoft_signin_section(mtm, /*token_present=*/true, login_opt); }`
    - Sign-out completion (likely the `UserEvent::MicrosoftSignOutRequested` arm after `MicrosoftTokenStore::clear()`): `unsafe { crate::menu::rebuild_microsoft_signin_section(mtm, false, None); }`

    If grep finds matches at both arms, leave them alone. If grep finds zero or only one match, add the missing call(s) — the function signature is `pub unsafe fn rebuild_microsoft_signin_section(_mtm: MainThreadMarker, token_present: bool, login: Option<&str>)` per `menu.rs:638` neighborhood (verify exact signature before use).

    Step 4 — Sanity check. Run `cargo build -p vector-app` and `cargo clippy -p vector-app --all-targets -- -D warnings`. The expected `tracing::warn!("install_microsoft_menu_items: no main menu installed yet")` warning from `menu.rs:569` should NOT fire at runtime if the call placement is correct (post-main-menu).

    DO NOT push.
  </action>
  <verify>
    <automated>cd /Users/ashutosh/personal/vector && grep -rqn "install_microsoft_menu_items(" crates/vector-app/src/ && cargo build -p vector-app 2>&1 | tail -5 && cargo clippy -p vector-app --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rqn "install_microsoft_menu_items(" crates/vector-app/src/` matches a CALL (not just the definition) — verify with `grep -rn "install_microsoft_menu_items(" crates/vector-app/src/ | wc -l` returns 2 or more (one definition site in menu.rs, at least one call site in app.rs or main.rs)
    - `grep -rn "rebuild_microsoft_signin_section" crates/vector-app/src/app.rs | wc -l` returns at least 2 (one for sign-in arm, one for sign-out arm)
    - `cargo build -p vector-app` exits 0
    - `cargo clippy -p vector-app --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `install_microsoft_menu_items` is called once at startup after the main menu and auth-menu install; the rebuild_microsoft_signin_section chain is wired for both sign-in and sign-out arms; build and clippy are clean. Manually verifiable: `cargo run --release` shows `Sign in with Microsoft`, `Sign out of Microsoft` (hidden), and `Dev Tunnels…` in the Vector menu.
  </done>
</task>

</tasks>

<verification>
- `cargo build -p vector-app` clean
- `cargo clippy -p vector-app --all-targets -- -D warnings` clean
- Manual (post-merge): `cargo run --release` → Vector menu contains `Sign in with Microsoft`, `Dev Tunnels…` (with Cmd-Shift-T)
- Manual: click "Sign in with Microsoft" → MicrosoftSignInRequested fires → device-flow modal appears
</verification>

<success_criteria>
The three Microsoft menu items are installed at startup and route to their UserEvent handlers; the sign-in/out → menu-rebuild chain works end-to-end. Maps to ROADMAP §Phase 9.1 Success Criteria #3.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-prior-phase-gap-closure/09.1-03-SUMMARY.md`
</output>
