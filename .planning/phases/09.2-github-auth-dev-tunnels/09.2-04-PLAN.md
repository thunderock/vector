---
phase: 09.2-github-auth-dev-tunnels
plan: 04
type: execute
wave: 4
depends_on: ["09.2-03", "09.2-01"]
files_modified:
  - crates/vector-tunnel-agent/src/auth.rs
  - crates/vector-tunnels/tests/live_devtunnel_smoke.rs
  - .planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md
autonomous: false
requirements: [DT-03]
must_haves:
  truths:
    - "A live GitHub sign-in completes from inside the app (modal button or menu) and persists a token to github_refresh_token"
    - "After sign-in the Dev Tunnels picker lists the user's tunnels"
    - "Connecting to an agent-hosted tunnel opens a [remote] pane that echoes typed input"
    - "If the spike showed the agent's gh-CLI token is rejected by the relay, the agent's GitHub client ID is bumped to Iv1.e7b89e013f801f03"
  artifacts:
    - path: ".planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md"
      provides: "Live UAT smoke checklist + sign-off"
      contains: "Sign in with GitHub"
  key_links:
    - from: "app sign-in"
      to: "github_refresh_token Keychain slot"
      via: "GitHubTokenStore::save"
      pattern: "github_refresh_token"
    - from: "picker"
      to: "agent-hosted tunnel pane"
      via: "DevTunnelTransport over the relay (unchanged)"
      pattern: "remote"
---

<objective>
Prove the full GitHub-auth path end-to-end on real hardware: GitHub sign-in → picker lists tunnels → connect to an agent-hosted tunnel → type in the remote pane. This resumes Phase 9 UAT Test 3+ (which hit `AADSTS500011` on the Microsoft path) and is the real gate for the phase. Also apply the D-4 contingency: if the spike (09.2-01) showed the agent's `gh`-CLI-issued GitHub token is rejected by the relay, bump the agent's GitHub client ID.

Purpose: Verify the live end-to-end sign-in + connect path (Phase success criteria 1, 2, 4) and close the conditional agent client-ID question (criterion 5).
Output: a conditional one-line agent client-ID change, an updated live-smoke env var, and a signed-off `09.2-SMOKE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.2-github-auth-dev-tunnels/09.2-RESEARCH.md
@.planning/phases/09.2-github-auth-dev-tunnels/09.2-01-SUMMARY.md
@.planning/phases/09.2-github-auth-dev-tunnels/09.2-03-SUMMARY.md

<interfaces>
From crates/vector-tunnel-agent/src/auth.rs (the D-4 contingency target):
```rust
pub const GITHUB_CLIENT_ID: &str = "178c6fc778ccc68e1d6a"; // gh CLI fallback (line 14)
// Conditional bump to "Iv1.e7b89e013f801f03" ONLY if the spike showed the relay
// rejects the gh-CLI token. The agent stays dual-provider, GitHub-default (D-4).
```

From crates/vector-tunnels/tests/live_devtunnel_smoke.rs:
```
// gated on VECTOR_E2E_TUNNEL_ID + VECTOR_E2E_MICROSOFT_TOKEN env vars
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply D-4 agent client-ID contingency + update live-smoke env var</name>
  <files>crates/vector-tunnel-agent/src/auth.rs, crates/vector-tunnels/tests/live_devtunnel_smoke.rs</files>
  <read_first>
    - .planning/phases/09.2-github-auth-dev-tunnels/09.2-01-SUMMARY.md "Spike Verdict" — the D-4 answer (does the agent's gh-CLI token get rejected by list_tunnels?)
    - crates/vector-tunnel-agent/src/auth.rs:11-16 (GITHUB_CLIENT_ID const)
    - crates/vector-tunnels/tests/live_devtunnel_smoke.rs (env-var gates)
  </read_first>
  <action>
    Read the spike verdict in 09.2-01-SUMMARY first, then:

    CONDITIONAL (D-4): IF the spike showed the agent's `gh`-CLI client ID token (`178c6fc778ccc68e1d6a`) is REJECTED by `list_tunnels` (401) while the Dev Tunnels GitHub App token (`Iv1.e7b89e013f801f03`) is accepted (200), THEN change `crates/vector-tunnel-agent/src/auth.rs:14` `GITHUB_CLIENT_ID` from `"178c6fc778ccc68e1d6a"` to `"Iv1.e7b89e013f801f03"`, and update the `GITHUB_SCOPES` line if the Dev Tunnels GitHub App ignores the `read:user` scope (GitHub Apps ignore OAuth scopes — set to empty string or remove the scope param, matching what the driver in 09.2-02 used). Keep the agent dual-provider, GitHub-default — do NOT touch the Microsoft path. Add a one-line comment: `// D-4: bumped to Dev Tunnels GitHub App per 09.2-01 spike — relay rejected gh-CLI token`.

    IF the spike showed the `gh`-CLI token is accepted (200), make NO change to `auth.rs` and record "D-4: no bump needed — gh-CLI token accepted by relay" in the SUMMARY.

    ALWAYS: update `crates/vector-tunnels/tests/live_devtunnel_smoke.rs` to add a `VECTOR_E2E_GITHUB_TOKEN` env-var gate alongside (or replacing) `VECTOR_E2E_MICROSOFT_TOKEN`, so the live smoke can run on a GitHub-issued token. Keep the test `#[ignore]`-gated.
  </action>
  <verify>
    <automated>cargo build -p vector-tunnel-agent && cargo test -p vector-tunnels --test live_devtunnel_smoke -- --list 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p vector-tunnel-agent` succeeds
    - `grep -q 'VECTOR_E2E_GITHUB_TOKEN' crates/vector-tunnels/tests/live_devtunnel_smoke.rs` succeeds
    - IF agent bumped: `grep -q 'Iv1.e7b89e013f801f03' crates/vector-tunnel-agent/src/auth.rs` succeeds AND a `D-4` comment is present; ELSE the SUMMARY records "no bump needed"
    - `cargo clippy -p vector-tunnel-agent --all-targets -- -D warnings` passes
  </acceptance_criteria>
  <done>The agent client-ID bump is applied or explicitly skipped per the spike verdict (recorded in SUMMARY); the live-smoke test gates on a GitHub token; both crates build clippy-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author the live UAT smoke checklist</name>
  <files>.planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md</files>
  <read_first>
    - .planning/phases/09-persistence-reconnect-tmux-auto-attach/09-SMOKE.md (existing smoke skeleton convention to mirror)
    - .planning/phases/09.2-github-auth-dev-tunnels/09.2-VALIDATION.md "Manual-Only Verifications" table
  </read_first>
  <action>
    Create `.planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md` with a numbered checklist the human walks on real hardware. Include, as separate checkable items:
    1. Launch `Vector.app`; open the Dev Tunnels picker (Cmd-Shift-T). Confirm the `NotSignedIn` footer reads "Sign in with GitHub to list Dev Tunnels." and the in-picker button reads "Sign in with GitHub".
    2. Confirm the menu shows "Sign in with GitHub" / "Sign out of GitHub" / "Dev Tunnels…" items.
    3. Click "Sign in with GitHub" → confirm the modal shows a user code + github.com/login/device URL → authorize in a browser → modal closes signed-in; toast reads "Signed in to GitHub.".
    4. Confirm a token is persisted: `security find-generic-password -s vector -a github_refresh_token` returns an entry (do NOT print the secret).
    5. Confirm the picker now lists ≥1 tunnel (name / host / last-seen).
    6. Start `vector-tunnel-agent` on a reachable host (the host registers via GitHub-default). Connect to it from the picker → a new `[remote]`-badged tab opens → type a command → confirm it echoes from the remote shell.
    7. Token-leak check: `grep -r 'gho_\|ghp_\|eyJ'` across logs returns zero hits.
    Add a "Sign-Off" section with pass/fail per item and a final verdict line.
  </action>
  <verify>
    <automated>test -f .planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md && grep -q 'github_refresh_token' .planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md` exists
    - `grep -q 'Sign in with GitHub' .planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md` succeeds
    - `grep -q 'github_refresh_token' .planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md` succeeds
    - `grep -q 'remote' .planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md` succeeds (the connect step)
    - It has a numbered checklist and a Sign-Off section
  </acceptance_criteria>
  <done>A complete live UAT smoke checklist exists covering sign-in, token persistence, picker listing, agent connect, remote echo, and token-leak audit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Walk the live GitHub-auth smoke and sign off (human checkpoint)</name>
  <files>.planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md</files>
  <what-built>The complete GitHub-auth Dev Tunnels path: app constructs `GitHubAuth`/`GitHubTokenStore`, sign-in modal + menu + picker button all say GitHub, the actor emits `AuthProvider::GitHub`, and the agent client ID is bumped-or-not per the spike.</what-built>
  <how-to-verify>
    Walk `.planning/phases/09.2-github-auth-dev-tunnels/09.2-SMOKE.md` end-to-end on real hardware with a live `vector-tunnel-agent`:
    1. Build + launch the app (`cargo run -p vector-app` or the bundled `Vector.app`).
    2. Sign in with GitHub from inside the app; authorize in a browser; confirm the modal closes signed-in and the toast says "Signed in to GitHub.".
    3. Confirm `security find-generic-password -s vector -a github_refresh_token` returns an entry.
    4. Confirm the picker lists your tunnels.
    5. Start `vector-tunnel-agent` on a reachable host, connect from the picker, type in the `[remote]` pane, and confirm it echoes.
    6. Run the token-leak grep; confirm zero hits.
    This resumes Phase 9 UAT Test 3+.
  </how-to-verify>
  <resume-signal>Mark each 09.2-SMOKE.md item pass/fail and type "approved" if the end-to-end path works, or describe the failing step.</resume-signal>
  <action>Human-only: walk 09.2-SMOKE.md end-to-end on real hardware with a live vector-tunnel-agent; mark each item pass/fail. No code is written in this task.</action>
  <verify>Every 09.2-SMOKE.md item marked pass/fail with a final verdict; Phase 9 UAT Test 3+ reachable on the GitHub path.</verify>
  <done>Smoke walked and signed off; end-to-end GitHub sign-in → picker → connect → remote echo confirmed.</done>

</task>

</tasks>

<verification>
- `cargo build -p vector-tunnel-agent` + `cargo build -p vector-app` succeed.
- `cargo test --workspace && cargo test -p vector-arch-tests` pass.
- The D-4 contingency is resolved (bumped or explicitly skipped) and recorded in SUMMARY.
- `09.2-SMOKE.md` exists and has been walked + signed off by the human (criterion 4).
</verification>

<success_criteria>
- Live GitHub sign-in completes and persists `github_refresh_token` (criterion 1).
- Picker lists tunnels with a GitHub token (criterion 2 — proven live, building on the 09.2-01 spike).
- Connect to an agent-hosted tunnel opens a working `[remote]` pane (criterion 4).
- No regression to relay/transport/agent; agent stays dual-provider GitHub-default with the conditional bump only if the spike required it (criterion 5).
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-github-auth-dev-tunnels/09.2-04-SUMMARY.md` recording the D-4 outcome (agent bumped Y/N), the smoke sign-off result, and confirmation that Phase 9 UAT Test 3+ is now reachable on the GitHub path.
</output>
