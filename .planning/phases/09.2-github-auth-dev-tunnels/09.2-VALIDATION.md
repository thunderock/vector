---
phase: 09.2
slug: github-auth-dev-tunnels
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 09.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust workspace) |
| **Config file** | none — workspace Cargo.toml |
| **Quick run command** | `cargo test -p vector-tunnels --lib` |
| **Full suite command** | `cargo test -p vector-tunnels -p vector-app` |
| **Estimated runtime** | ~30-60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p vector-tunnels --lib` + `cargo clippy -p vector-tunnels --all-targets -- -D warnings`
- **After every plan wave:** Run `cargo test -p vector-tunnels -p vector-app`
- **Before `/gsd:verify-work`:** Full suite green + the live smoke (manual)
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 09.2-01-* | 01 | 1 | DT-03 (spike) | integration (manual gate) | `cargo test -p vector-tunnels --test client_id_spike -- --ignored --nocapture` | ❌ W0 | ⬜ pending |
| 09.2-02-* | 02 | 2 | DT-03 | unit | `cargo test -p vector-tunnels --lib device_flow_github` | ❌ W0 | ⬜ pending |
| 09.2-02-* | 02 | 2 | DT-03 | unit | `cargo test -p vector-tunnels --lib token_store::github` | ❌ W0 | ⬜ pending |
| 09.2-03-* | 03 | 3 | DT-03 | build/lint | `cargo build -p vector-app && cargo clippy -p vector-app --all-targets -- -D warnings` | ✅ | ⬜ pending |
| 09.2-04-* | 04 | 4 | DT-03 | manual | live device-flow smoke (see Manual-Only) | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/vector-tunnels/tests/client_id_spike.rs` — `#[ignore]` env-gated integration test (Plan 09.2-01); proves device-flow against `Iv1.e7b89e013f801f03` is enabled and that the resulting token (and the `gh`-CLI token) is/isn't accepted by `GET /api/v1/tunnels`.
- [ ] `crates/vector-tunnels/src/auth/device_flow_github.rs` — new driver with `#[cfg(test)]` unit tests for request shaping + poll/backoff (`slow_down` +5s, `authorization_pending`, expiry).
- [ ] Token-store unit test for the `github_refresh_token` Keychain slot round-trip.

*The spike test is a hard gate: Plan 09.2-02 does not start until 09.2-01's result is recorded.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GitHub device-flow browser auth completes | DT-03 | Requires a human to open github.com/login/device and enter the code with a real GitHub account | Click "Sign in with GitHub" → confirm user code shown → authorize in browser → modal closes signed-in |
| Picker lists tunnels after sign-in | DT-03 | Requires a live Dev Tunnels account + agent-hosted tunnel | After sign-in, picker shows ≥1 tunnel (name/host/last-seen) |
| Connect to agent-hosted remote pane | DT-03 | Requires a running `vector-tunnel-agent` on a reachable host | Select tunnel → new `[remote]` tab → type → echoes from remote shell (resumes Phase 9 UAT Test 3+) |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies (live smoke is unavoidably manual)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (spike test + driver unit tests)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
