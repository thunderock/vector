---
status: testing
phase: 09.2-github-auth-dev-tunnels
source: [09.2-SMOKE.md, 09.2-01-SUMMARY.md, 09.2-02-SUMMARY.md, 09.2-03-SUMMARY.md]
started: 2026-06-02T18:33:54Z
updated: 2026-06-02T18:33:54Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Picker copy says GitHub (signed out)
expected: |
  Open the Dev Tunnels picker (Cmd-Shift-T) while signed out. The NotSignedIn
  footer reads "Sign in with GitHub to list Dev Tunnels." and the in-picker
  button reads "Sign in with GitHub".
awaiting: user response

## Tests

### 1. Picker copy says GitHub (signed out)
expected: Picker (Cmd-Shift-T) while signed out — footer reads "Sign in with GitHub to list Dev Tunnels."; in-picker button reads "Sign in with GitHub".
result: [pending]

### 2. Menu bar says GitHub
expected: Menu bar shows "Sign in with GitHub", "Sign out of GitHub", and "Dev Tunnels…" items.
result: [pending]

### 3. Sign in with GitHub (device flow)
expected: Click "Sign in with GitHub" — modal shows a user code + github.com/login/device URL; authorize in browser; modal closes signed-in; toast reads "Signed in to GitHub."
result: [pending]

### 4. Token persisted in Keychain
expected: `security find-generic-password -s vector -a github_refresh_token` returns a keychain entry (exit 0); the secret is never printed.
result: [pending]

### 5. Picker lists tunnels
expected: Re-open/refresh the picker — it lists >=1 tunnel (name / host / last-seen).
result: [pending]

### 6. Connect to agent-hosted tunnel (the gate)
expected: With vector-tunnel-agent running on a reachable host, select its tunnel and connect — a new [remote]-badged tab opens and a typed command echoes from the remote shell. (Connect token fix a621814 in place.)
result: [pending]

### 7. Token-leak audit
expected: `grep -rIn 'gho_\|ghp_\|eyJ' ~/Library/Logs/vector* ./*.log 2>/dev/null` returns zero hits — no token/JWT material in any log.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0

## Gaps

[none yet]
