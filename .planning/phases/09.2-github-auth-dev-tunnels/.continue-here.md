---
phase: 09.2-github-auth-dev-tunnels
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-30T14:57:09.068Z
---

<current_state>
Phase 09.2 (GitHub-auth Dev Tunnels) — plans 01-03 complete. Plan 09.2-04 is the
live end-to-end smoke gate; we are mid-walk on Task 3 (human-verify). The full
GitHub-auth path now works up to listing: sign-in OK, picker loads, the agent
authenticates + registers + hosts a tunnel (`silent-book-tphgwm9`). Five
real-API bugs were found and fixed live this session. The ONLY remaining smoke
step is **connect** (open a [remote] tab and type in the remote shell).

A `vector-tunnel-agent` is RUNNING in the background hosting `silent-book-tphgwm9`
(token cached at ~/.config/vector/agent-token). The Vector app must be RESTARTED
to pick up the latest app-side fix (4f1882e, the `tags` field).
</current_state>

<completed_work>

- Task 1: Bump agent GitHub client ID to Dev Tunnels App (D-4) — Done (c1d7c86)
- Task 2: Author live smoke test + 09.2-SMOKE.md checklist — Done (c1d7c86)
- Task 3: Live smoke walk — In progress. Working so far: GitHub sign-in,
  picker loads (no decode error), agent device-flow auth, tunnel registration,
  relay hosting. Fixed 5 bugs (commits below).

5 live-smoke fixes this session (all TDD, committed to master, NOT pushed):
- 433ef41 — list endpoint returns a bare JSON array, not {value:[...]}
- 800aa19 — agent device-flow: wait `interval` before first poll + tolerate
  transient incorrect_device_code (RFC 8628 §3.4 race)
- 10c42e5 — sanitize macOS hostname into a Dev Tunnels-valid name (regex)
- 9420105 — drop custom tunnel name (relay 403) + align `vector-agent` label
- 4f1882e — read the label from the relay's `tags` field (was reading `labels`)

Out-of-band this session (also on master, NOT pushed):
- CI remediation after first push since Phase 4: 01c4791, 6cd185e, 197cf33, 3127c41
- Quick task 260529-hni (unify agent .deb + app .dmg into release.yml): b54858f,
  f211a85, 7839af2, 563e1df
</completed_work>

<remaining_work>

- Task 3 (finish): restart the app → confirm picker lists `silent-book-tphgwm9`
  → select it → connect → [remote] tab opens → type a command and see it echo
  from the remote shell → token-leak grep → fill 09.2-SMOKE.md sign-off.
- Then: phase verification + mark 09.2 complete (unblocks held 10-04 release tag
  and the Phase 9 UAT items).
</remaining_work>

<decisions_made>

- List API returns a bare array (verified live: empty account → `[]`, 200).
- Agent must wait `interval` before the first device-flow poll and treat
  incorrect_device_code as transient (bounded retries).
- Agent must NOT set a custom tunnel name (relay feature disabled → 403);
  identify by label, display the auto-assigned tunnel id.
- The `vector-agent` label is returned under `tags`; agent + picker both use the
  plain string `vector-agent` (not `vector-agent: true`).
</decisions_made>

<blockers>
- Connect path (`get_access_token`, POST /api/v1/tunnels/{id}/access?scopes=connect,
  expects `{"token":...}`) is UNVERIFIED against the real API. If connect throws
  "error decoding response body" or an HTTP error, it's the same class of bug —
  probe the real response with the cached github token via curl, then fix the
  decode/model (see api.rs get_access_token).
</blockers>

<context>
Phase 9 UAT died at Microsoft auth (AADSTS500011) before any of these code paths
ran against the live API, so 09.2's GitHub auth is the first time they executed —
hence the cascade of fixture-vs-reality bugs. Fix loop each time: curl the real
endpoint with the cached token (`security find-generic-password -s vector -a
github_refresh_token -w` → access_token) → write a failing test → fix → green.
The agent's own auth (auth.rs) is separate from the app's driver
(vector-tunnels/src/auth/device_flow_github.rs). commit_docs=false, so .planning
stays uncommitted; do NOT push (user pushes asynchronously).
</context>

<next_action>
Start with: re-run the agent if it stopped (`cargo run -p vector-tunnel-agent`),
restart the Vector app (`cargo run -p vector-app`), open the picker, confirm it
lists `silent-book-tphgwm9`, then click connect. If connect fails with a decode
error, curl POST /api/v1/tunnels/{id}/access?scopes=connect with the cached token
to see the real body shape and fix get_access_token's decode.
</next_action>
