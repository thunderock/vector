---
phase: 09.2-github-auth-dev-tunnels
plan: 01
subsystem: auth
tags: [github, device-flow, oauth, dev-tunnels, spike, reqwest]

# Dependency graph
requires:
  - phase: 08-vs-code-remote-tunnels-connect
    provides: vector-tunnels list_tunnels client + AuthProvider::GitHub header format
provides:
  - "Spike verdict gating Plans 09.2-02 and 09.2-04: Dev Tunnels GitHub App client ID Iv1.e7b89e013f801f03 has device flow enabled, list_tunnels accepts its token (200), and GitHub issues refresh_token + expires_in (8h)"
  - "D-4 resolution: gh-CLI client ID 178c6fc778ccc68e1d6a token is REJECTED by list_tunnels (403); agent MUST bump to Iv1.e7b89e013f801f03"
  - "client_id_spike.rs — reusable #[ignore]-gated regression artifact for token-path acceptance"
affects: [09.2-02, 09.2-04, token-store, agent-client-id, refresh-path]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "#[ignore] + env-gate (VECTOR_SPIKE_RUN=1) integration test driving real device flow against live endpoints"
    - "No #[derive(Debug)] on structs holding access_token/device_code; never print token value, only presence/length"

key-files:
  created:
    - crates/vector-tunnels/tests/client_id_spike.rs
  modified: []

key-decisions:
  - "D-2 settled: Iv1.e7b89e013f801f03 (Dev Tunnels GitHub App) is the correct app-side client ID — device flow enabled and list_tunnels returns 200"
  - "D-4 settled: agent client ID MUST bump from 178c6fc778ccc68e1d6a to Iv1.e7b89e013f801f03 — the gh-CLI token is rejected by list_tunnels (403)"
  - "Refresh path is LIVE not dormant: Dev Tunnels GitHub App issues refresh_token + expires_in=28800s (8h), so Wave 2 token store persists real refresh + expiry"

patterns-established:
  - "Spike-before-driver hard gate: settle token-path acceptance with a runnable test before any driver code"

requirements-completed: [DT-03]

# Metrics
duration: ~35min
completed: 2026-05-28
---

# Phase 09.2 Plan 01: Client-ID Spike Summary

**Live device-flow spike proves the Dev Tunnels GitHub App client ID `Iv1.e7b89e013f801f03` is accepted by `list_tunnels` (200) with a refresh_token + 8h expiry, while the gh-CLI ID is rejected (403) — settling D-2 and forcing the agent client-ID bump (D-4).**

## Performance

- **Duration:** ~35 min (incl. human browser authorization for both client IDs)
- **Started:** 2026-05-29T02:54:00Z
- **Completed:** 2026-05-28 (verdict captured)
- **Tasks:** 2 (1 auto + 1 human-verify checkpoint)
- **Files modified:** 1

## Accomplishments

- Wrote `crates/vector-tunnels/tests/client_id_spike.rs` — an `#[ignore]` + `VECTOR_SPIKE_RUN=1` env-gated `#[tokio::test]` that drives a real GitHub device flow for both client IDs and calls live `list_tunnels` with each token.
- Human ran the spike with browser authorization for both client IDs; verdict captured.
- Settled the only real risk in the phase (token-path acceptance) before any driver code: D-2 confirmed, D-4 resolved, refresh path confirmed LIVE.

## Spike Verdict

**Captured output (2026-05-28):**

```
=== Client ID: Iv1.e7b89e013f801f03 ===
token acquired for Iv1.e7b89e013f801f03 (len=40, refresh_token=true, expires_in=Some(28800))
list_tunnels status for Iv1.e7b89e013f801f03: 200

=== Client ID: 178c6fc778ccc68e1d6a ===
token acquired for 178c6fc778ccc68e1d6a (len=40, refresh_token=false, expires_in=None)
list_tunnels status for 178c6fc778ccc68e1d6a: 403
```

### Per-client-ID verdict table

| client_id | device_flow_enabled | refresh_token | expires_in | list_tunnels_status |
|---|---|---|---|---|
| `Iv1.e7b89e013f801f03` (Dev Tunnels GitHub App) | true | true | Some(28800) (8h) | **200 — accepted** |
| `178c6fc778ccc68e1d6a` (gh-CLI) | true | false | None | **403 — rejected by relay** |

### Four gating answers

1. **Device flow enabled for `Iv1.e7b89e013f801f03`?** YES. Device flow completed with no `device_flow_disabled` error; token acquired (len=40).
2. **`list_tunnels` acceptance?** `Iv1.e7b89e013f801f03` = **200 (accepted)**; `178c6fc778ccc68e1d6a` = **403 (rejected by the relay)**. The relay gates on the issuing GitHub App, not just on token validity.
3. **Refresh token + `expires_in`?** YES for the Dev Tunnels GitHub App — `refresh_token` issued and `expires_in = 28800s (8h)`. The gh-CLI ID issues neither. **Consequence:** Wave 2 (09.2-02) token store persists the refresh token + real expiry, and the refresh path is **LIVE, not dormant**.
4. **D-4 contingency (agent client-ID bump)?** YES — the agent MUST bump its client ID to `Iv1.e7b89e013f801f03`. The gh-CLI token (`178c6fc778ccc68e1d6a`) is rejected by `list_tunnels` (403) while the Dev Tunnels GitHub App token is accepted (200). **Wave 4 (09.2-04) applies the agent client-ID bump.**

## Task Commits

1. **Task 1: Write the client-ID spike integration test** - `0b68009` (test)
2. **Task 2: Run the spike + record verdict (human-verify checkpoint)** - no code commit (human-only run; verdict recorded in this SUMMARY)

**Plan metadata:** not committed — `commit_docs=false`, `.planning` left uncommitted per project config.

## Files Created/Modified

- `crates/vector-tunnels/tests/client_id_spike.rs` - `#[ignore]` + env-gated `#[tokio::test]` driving real GitHub device flow + live `list_tunnels` for both client IDs; prints a verdict table.

## Decisions Made

- **D-2 (which app-side client ID):** `Iv1.e7b89e013f801f03` (Dev Tunnels GitHub App). It has device flow enabled and its token is accepted by `list_tunnels` (200).
- **D-4 (agent client-ID bump):** REQUIRED. The gh-CLI ID's token is rejected (403) by `list_tunnels`; the agent must use `Iv1.e7b89e013f801f03`. Applied in 09.2-04.
- **Refresh path status:** LIVE. The Dev Tunnels GitHub App issues `refresh_token` + `expires_in=28800` (8h), so 09.2-02's token store must persist refresh + real expiry rather than treating refresh as dormant.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The success path predicted in 09.2-RESEARCH was confirmed: `Iv1.e7b89e013f801f03` → device flow completes, `list_tunnels` = 200.

## User Setup Required

None - no external service configuration required (the spike is human-run on demand; not part of the shipping build path).

## Next Phase Readiness

- HARD GATE cleared. Plans 09.2-02 (GitHub device-flow driver + token store) and 09.2-04 (agent client-ID bump) are unblocked.
- 09.2-02 token store must persist `refresh_token` + `expires_in` (8h) — refresh path is live.
- 09.2-04 must bump the agent client ID to `Iv1.e7b89e013f801f03`.

## Self-Check: PASSED

- `crates/vector-tunnels/tests/client_id_spike.rs` — FOUND
- `.planning/phases/09.2-github-auth-dev-tunnels/09.2-01-SUMMARY.md` — FOUND
- Commit `0b68009` — FOUND

---
*Phase: 09.2-github-auth-dev-tunnels*
*Completed: 2026-05-28*
