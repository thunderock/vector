---
phase: 09.2-github-auth-dev-tunnels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vector-tunnels/tests/client_id_spike.rs
autonomous: false
requirements: [DT-03]
must_haves:
  truths:
    - "A device flow against the Dev Tunnels GitHub App client ID Iv1.e7b89e013f801f03 completes without device_flow_disabled"
    - "The GitHub token issued by that client ID is accepted by GET /api/v1/tunnels (200, not 401)"
    - "The spike records whether GitHub issued a refresh_token and an expires_in for each client ID"
  artifacts:
    - path: "crates/vector-tunnels/tests/client_id_spike.rs"
      provides: "#[ignore] env-gated integration test driving device flow + list_tunnels for both client IDs"
      contains: "Iv1.e7b89e013f801f03"
  key_links:
    - from: "client_id_spike.rs"
      to: "https://global.rel.tunnels.api.visualstudio.com/api/v1/tunnels"
      via: "reqwest GET with header Authorization: github <token>"
      pattern: "github \\{"
---

<objective>
Settle decisions D-2 and D-4 with a runnable spike BEFORE any driver code is written. The single real risk in this phase is token-path acceptance: (a) does the Dev Tunnels GitHub App client ID `Iv1.e7b89e013f801f03` have device flow enabled, and (b) does the resulting token get accepted by the Dev Tunnels Management API (`list_tunnels` → 200, not 401)? The spike also tests the `gh`-CLI client ID (`178c6fc778ccc68e1d6a`) against `list_tunnels` to settle whether the agent's client ID must also be bumped (D-4).

Purpose: This is a HARD GATE. Plan 09.2-02 (the driver) does NOT begin until this spike's result is recorded in this plan's SUMMARY.
Output: `crates/vector-tunnels/tests/client_id_spike.rs` — an `#[ignore]`-gated, env-driven integration test that prints a verdict table, plus the recorded spike result in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.2-github-auth-dev-tunnels/09.2-CONTEXT.md
@.planning/phases/09.2-github-auth-dev-tunnels/09.2-RESEARCH.md

<interfaces>
<!-- Existing GitHub device-flow request/poll shape to copy — agent code, already correct for GitHub. -->
From crates/vector-tunnel-agent/src/auth.rs:
```
GITHUB_DEVICE_CODE_URL = "https://github.com/login/device/code"
GITHUB_TOKEN_URL       = "https://github.com/login/oauth/access_token"
// device-code POST: .header(ACCEPT, "application/json").form(&[("client_id", id),("scope", scopes)])
// token POST: .header(ACCEPT, "application/json").form(&[("client_id", id),("device_code", dc),("grant_type","urn:ietf:params:oauth:grant-type:device_code")])
// verification_uri falls back to verification_url (auth.rs:178-183)
```

From crates/vector-tunnels/src/api.rs:
```
pub const TUNNELS_BASE_URL: &str = "https://global.rel.tunnels.api.visualstudio.com";
// list_tunnels path is GET {TUNNELS_BASE_URL}/api/v1/tunnels
```

From crates/vector-tunnels/src/model.rs:69-92:
```
pub enum AuthProvider { GitHub(String), Microsoft(String) }
// AuthProvider::GitHub(t).format_header() -> "github <t>"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the client-ID spike integration test</name>
  <files>crates/vector-tunnels/tests/client_id_spike.rs</files>
  <read_first>
    - crates/vector-tunnels/tests/live_devtunnel_smoke.rs (existing #[ignore] + env-gate convention to mirror)
    - crates/vector-tunnel-agent/src/auth.rs:141-275 (device-code request + token poll shape, Accept header, verification_uri/url fallback)
    - crates/vector-tunnels/src/api.rs:19,90-117 (TUNNELS_BASE_URL + list_tunnels path)
    - .planning/phases/09.2-github-auth-dev-tunnels/09.2-RESEARCH.md "The Client-ID Spike (Plan 09.2-01) Design" section
  </read_first>
  <action>
    Create `crates/vector-tunnels/tests/client_id_spike.rs` as a standalone `#[tokio::test]` annotated `#[ignore]` and gated on env var `VECTOR_SPIKE_RUN=1` (early-return if unset, exactly like `live_devtunnel_smoke.rs` gates on its env vars). Use `reqwest` (already a dep) directly — do NOT depend on any not-yet-written driver.

    For EACH client_id in the array `["Iv1.e7b89e013f801f03", "178c6fc778ccc68e1d6a"]`:
    1. POST `https://github.com/login/device/code` with `.header(reqwest::header::ACCEPT, "application/json")` and form body `[("client_id", client_id)]` (NO scope param for the Dev Tunnels GitHub App; for the gh-CLI ID pass `("scope","read:user")` — branch on the id). Parse JSON. ASSERT the response does NOT contain `"error": "device_flow_disabled"` — if it does, print `DEVICE FLOW DISABLED for {client_id}` and record it in the verdict, then `continue` to the next id.
    2. Print the `user_code` and `verification_uri` (fall back to `verification_url` per agent auth.rs:178-181) to stdout via `println!` so the human can authorize.
    3. Poll `https://github.com/login/oauth/access_token` with `.header(ACCEPT,"application/json")` and form `[("client_id",client_id),("device_code",dc),("grant_type","urn:ietf:params:oauth:grant-type:device_code")]` in a loop honoring `interval` (sleep), handling `authorization_pending` (keep polling) and `slow_down` (add 5s). Stop on `access_token` present, on `expired_token`/`access_denied` error, or after `expires_in` elapses. Record whether `refresh_token` and `expires_in` are present in the success body.
    4. GET `{TUNNELS_BASE_URL}/api/v1/tunnels` (i.e. `https://global.rel.tunnels.api.visualstudio.com/api/v1/tunnels`) with header `Authorization: github {access_token}`. Record the HTTP status code (expect 200; 401 = relay rejected this token's issuing app).

    After both ids, `println!` a verdict table with one row per client_id: `{client_id} | device_flow_enabled={bool} | refresh_token={bool} | expires_in={Option<u64>} | list_tunnels_status={u16}`.

    Pitfall-14: do NOT `#[derive(Debug)]` on any local struct holding `access_token`/`device_code`; if you need a struct, write a manual `Debug` mirroring `device_flow_microsoft.rs:55-65`. Do NOT `tracing::*!` or `println!` the access_token value itself — only its presence/length. The arch-lint scans `vector-tunnels/src` not `tests/`, but keep the discipline so the spike is a safe regression artifact.
  </action>
  <verify>
    <automated>cargo test -p vector-tunnels --test client_id_spike -- --list 2>&1 | grep -q client_id_spike</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Iv1.e7b89e013f801f03' crates/vector-tunnels/tests/client_id_spike.rs` succeeds
    - `grep -q '178c6fc778ccc68e1d6a' crates/vector-tunnels/tests/client_id_spike.rs` succeeds
    - `grep -q 'device_flow_disabled' crates/vector-tunnels/tests/client_id_spike.rs` succeeds (the explicit assertion)
    - `grep -q '#\[ignore\]' crates/vector-tunnels/tests/client_id_spike.rs` succeeds
    - `grep -q 'api/v1/tunnels' crates/vector-tunnels/tests/client_id_spike.rs` succeeds
    - `grep -q 'github ' crates/vector-tunnels/tests/client_id_spike.rs` succeeds (the Authorization header)
    - `cargo test -p vector-tunnels --test client_id_spike` compiles (test is skipped via #[ignore], must still build)
    - `cargo clippy -p vector-tunnels --all-targets -- -D warnings` passes
  </acceptance_criteria>
  <done>Spike test compiles, is #[ignore]-gated + env-gated, tests both client IDs against device flow and list_tunnels, and prints a verdict table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the client-ID spike and record the verdict (human checkpoint)</name>
  <files>crates/vector-tunnels/tests/client_id_spike.rs</files>
  <what-built>An `#[ignore]`-gated spike test `crates/vector-tunnels/tests/client_id_spike.rs` that drives a real GitHub device flow against both the Dev Tunnels GitHub App client ID and the gh-CLI client ID, then calls the live Dev Tunnels `list_tunnels` endpoint with each token.</what-built>
  <how-to-verify>
    1. Run: `VECTOR_SPIKE_RUN=1 cargo test -p vector-tunnels --test client_id_spike -- --ignored --nocapture`
    2. For the FIRST client ID printed (`Iv1.e7b89e013f801f03`): open the printed `verification_uri` (https://github.com/login/device) in a browser, enter the printed user code, and authorize with your GitHub account.
    3. Repeat the authorization for the SECOND client ID (`178c6fc778ccc68e1d6a`) when its code prints.
    4. Read the final verdict table. Record, for `Iv1.e7b89e013f801f03`: did device flow complete (not `device_flow_disabled`)? Was a `refresh_token` issued? Was `expires_in` present? Did `list_tunnels` return 200?
    Expected (success path per RESEARCH): `Iv1.e7b89e013f801f03` → device flow completes, `list_tunnels` = 200.
  </how-to-verify>
  <resume-signal>Paste the verdict table (or describe the outcome). State explicitly: (D-4 contingency) does the agent's client ID need to bump to `Iv1.e7b89e013f801f03`? (i.e. is the `gh`-CLI token rejected by list_tunnels while the Dev Tunnels GitHub App token is accepted?) And: does the Dev Tunnels GitHub App issue refresh tokens + expires_in?</resume-signal>
  <action>Human-only: run the spike with VECTOR_SPIKE_RUN=1 and authorize in a browser; record the verdict in the SUMMARY. No code is written in this task.</action>
  <verify>Verdict table for both client IDs recorded in 09.2-01-SUMMARY (device_flow_enabled, refresh_token, expires_in, list_tunnels status, D-4 answer).</verify>
  <done>Spike run by human; verdict + four gating answers recorded in SUMMARY.</done>

</task>

</tasks>

<verification>
- `cargo test -p vector-tunnels --test client_id_spike` builds.
- The spike has been run by the human and the verdict recorded in this plan's SUMMARY, including: device-flow-enabled for `Iv1.e7b89e013f801f03` (Y/N), list_tunnels status for each client ID, refresh_token presence, expires_in presence, and the D-4 verdict (agent client-ID bump needed Y/N).
</verification>

<success_criteria>
The spike result is recorded in SUMMARY and answers: (1) is device flow enabled for `Iv1.e7b89e013f801f03`? (2) does `list_tunnels` accept its token (200)? (3) does GitHub issue refresh_token + expires_in? (4) does the agent's `gh`-CLI client ID also need to bump (D-4)? These four answers gate Plans 09.2-02 and 09.2-04.
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-github-auth-dev-tunnels/09.2-01-SUMMARY.md`. The SUMMARY MUST contain a "Spike Verdict" section with the per-client-ID table and the four gating answers above, because downstream plans (09.2-02 token-store shape, 09.2-04 agent bump) read it.
</output>
