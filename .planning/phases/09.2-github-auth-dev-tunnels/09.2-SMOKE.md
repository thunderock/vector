# Phase 9.2 SMOKE — GitHub-auth Dev Tunnels end-to-end

Resumes Phase 9 UAT Test 3+ (which hit `AADSTS500011` on the Microsoft path). This is the real gate for the phase: live GitHub sign-in → picker lists tunnels → connect to an agent-hosted tunnel → type in the `[remote]` pane.

**User:** astiwari
**Date:** {YYYY-MM-DD — user fills}
**Provider:** GitHub (Dev Tunnels GitHub App `Iv1.e7b89e013f801f03`)
**Tunnel id:** {redacted-on-publish}
**Remote host:** {your reachable box — Ubuntu 22.04 or equivalent}
**Remote agent:** `vector-tunnel-agent` built from this tree (D-4 client-ID bump applied)

## Build + launch

```
cargo run -p vector-app
# or launch the bundled Vector.app
```

## Checklist (walk in order on real hardware)

| # | Step | Expected | Pass/Fail |
|---|------|----------|-----------|
| 1 | Open the Dev Tunnels picker (Cmd-Shift-T) while signed out | `NotSignedIn` footer reads **"Sign in with GitHub to list Dev Tunnels."**; the in-picker button reads **"Sign in with GitHub"** | ⬜ pass / ❌ fail / 📝 |
| 2 | Open the menu bar | Menu shows **"Sign in with GitHub"**, **"Sign out of GitHub"**, and **"Dev Tunnels…"** items | ⬜ pass / ❌ fail / 📝 |
| 3 | Click **"Sign in with GitHub"** | Modal shows a user code + `github.com/login/device` URL → authorize in a browser → modal closes signed-in; toast reads **"Signed in to GitHub."** | ⬜ pass / ❌ fail / 📝 |
| 4 | Confirm token persisted (do NOT print the secret): `security find-generic-password -s vector -a github_refresh_token` | Returns a keychain entry (exit 0); the secret value is never printed | ⬜ pass / ❌ fail / 📝 |
| 5 | Re-open / refresh the picker | Picker now lists ≥1 tunnel (name / host / last-seen) | ⬜ pass / ❌ fail / 📝 |
| 6 | Start `vector-tunnel-agent` on a reachable host (registers via GitHub-default), then connect to it from the picker → type a command in the new tab | A new **`[remote]`**-badged tab opens; the typed command echoes from the remote shell | ⬜ pass / ❌ fail / 📝 |
| 7 | Token-leak audit: `grep -rIn 'gho_\|ghp_\|eyJ' ~/Library/Logs/vector* ./*.log 2>/dev/null` (adjust to your log paths) | Zero hits (no access/refresh token or JWT material in any log) | ⬜ pass / ❌ fail / 📝 |

## Notes / failing-step detail

{Record any failure: which step, observed vs expected, screenshots/log excerpts.}

## Sign-Off

- [ ] Item 1 — picker footer + button copy say GitHub
- [ ] Item 2 — menu items say GitHub
- [ ] Item 3 — sign-in modal → browser authorize → signed-in toast
- [ ] Item 4 — `github_refresh_token` persisted in Keychain
- [ ] Item 5 — picker lists tunnels
- [ ] Item 6 — connect to agent-hosted tunnel → `[remote]` pane echoes
- [ ] Item 7 — token-leak grep returns zero hits

**Final verdict:** {APPROVED — end-to-end GitHub path works  |  FAILED at step N — see notes}

**Approved by:** ___________  **Date:** ___________
