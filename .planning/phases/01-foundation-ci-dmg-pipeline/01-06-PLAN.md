---
phase: 01-foundation-ci-dmg-pipeline
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - .github/workflows/release.yml
  - README.md
  - CHANGELOG.md
  - docs/adr/0001-rust-workspace-layout.md
  - docs/adr/0002-winit-tokio-threading.md
  - docs/adr/0003-architecture-lint-mechanism.md
  - docs/adr/0004-dmg-pipeline.md
  - docs/adr/0005-versioning-calver.md
  - docs/adr/0006-runner-labels-and-branch-protection.md
  - docs/setup.md
autonomous: false
requirements: [BUILD-04, BUILD-05, BUILD-02]
priority: P0

must_haves:
  truths:
    - "Tagging v* (e.g. v2026.05.10) triggers .github/workflows/release.yml and publishes Vector-2026.05.10-universal.dmg to a GitHub Release"
    - "The Release body contains the xattr -dr com.apple.quarantine /Applications/Vector.app instruction in a fenced sh code block (one of the three D-26 places)"
    - "README.md contains the xattr instruction in a fenced sh code block under an Install heading (one of the three D-26 places)"
    - "5+ ADR markdown files exist under docs/adr/ following the MADR template, covering: workspace layout, winit/tokio threading, architecture-lint mechanism, DMG pipeline, CalVer versioning, and runner-labels-plus-branch-protection"
    - "docs/setup.md documents the manual GitHub branch protection configuration steps that satisfy D-34 + D-35"
    - "CHANGELOG.md exists at repo root with the Keep a Changelog header (cliff.toml will populate sections on next release)"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "tag-triggered Universal DMG publish to GitHub Releases with xattr instruction"
      contains: "tags: ['v*']"
      contains_extra: "xattr -dr com.apple.quarantine"
    - path: "README.md"
      provides: "Install instructions including xattr step (D-26 place 1 of 3)"
      contains: "xattr -dr com.apple.quarantine /Applications/Vector.app"
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog header; populated by git-cliff on each release"
      contains: "# Changelog"
    - path: "docs/adr/0001-rust-workspace-layout.md"
      provides: "ADR for D-01..D-04 workspace decisions, MADR format"
    - path: "docs/adr/0002-winit-tokio-threading.md"
      provides: "ADR for D-08..D-11 threading invariant, MADR format"
    - path: "docs/adr/0003-architecture-lint-mechanism.md"
      provides: "ADR for D-08 per-crate test mechanism + CI grep redundancy"
    - path: "docs/adr/0004-dmg-pipeline.md"
      provides: "ADR for D-17..D-26 DMG packaging + tip+tagged release pattern"
    - path: "docs/adr/0005-versioning-calver.md"
      provides: "ADR for D-27..D-29 CalVer + Conventional Commits + git-cliff CHANGELOG"
    - path: "docs/adr/0006-runner-labels-and-branch-protection.md"
      provides: "ADR for D-21 amendment (macos-15-intel) and D-34..D-35 branch protection setup"
    - path: "docs/setup.md"
      provides: "Manual one-time setup steps: branch protection rules, GitHub Releases permission, brew installs"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "xtask dmg pipeline (Plan 01-04)"
      via: "cargo xtask dmg --universal --arm64 ... --x86_64 ..."
      pattern: "cargo xtask dmg --universal"
    - from: "README.md Install section"
      to: "xattr -dr com.apple.quarantine /Applications/Vector.app"
      via: "fenced sh code block"
      pattern: 'xattr -dr com\\.apple\\.quarantine'
    - from: ".github/workflows/release.yml release body"
      to: "git-cliff --latest"
      via: "RELEASE_NOTES.md generated from cliff.toml + tag"
      pattern: "git-cliff --latest"
    - from: "docs/adr/0006-runner-labels-and-branch-protection.md"
      to: "GitHub UI / API branch protection state"
      via: "manual configuration documented in docs/setup.md; verified via gh api repos/{owner}/vector/branches/main/protection"
      pattern: "branches/main/protection"
---

<objective>
Land the tagged-release pipeline (release.yml), the public-facing README +
CHANGELOG with xattr instructions in two of the three D-26 surfaces, the
five (plus one bonus for D-21 amendment + branch protection) MADR-format
ADRs, and the docs/setup.md that captures the manual GitHub branch protection
steps. Closes BUILD-04 (tagged-release half) and BUILD-05.

Purpose: Phase 1 isn't done until a `git tag v2026.05.10 && git push --tags`
publishes a downloadable Universal DMG to GitHub Releases AND the public
surfaces (README, Release body, DMG background) all carry the xattr
incantation byte-identically. ADRs document the architectural decisions so
Phase 2's planner can read why the workspace is the shape it is.

Output: A repo where (a) tagged release flow is wired, (b) the README is no
longer a one-liner — it's the install + run guide, (c) ADRs document
D-01..D-35 plus the D-21 runner-label amendment, (d) docs/setup.md captures
the GitHub manual setup the user must perform once.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-ci-dmg-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-ci-dmg-pipeline/01-RESEARCH.md
@.planning/phases/01-foundation-ci-dmg-pipeline/01-UI-SPEC.md
@.planning/phases/01-foundation-ci-dmg-pipeline/01-VALIDATION.md
@.planning/phases/01-foundation-ci-dmg-pipeline/01-05-SUMMARY.md

<interfaces>
<!-- release.yml topology from 01-RESEARCH.md §"Release workflow .github/workflows/release.yml". -->

Trigger: on push tags 'v*'
Jobs:
  build-arm64    (macos-14)
  build-x86_64   (macos-15-intel)  ← AMENDMENT to D-21
  release        (macos-14, needs both builds, permissions: contents: write)
                 → downloads both per-arch artifacts
                 → brew install create-dmg librsvg + cargo install cargo-bundle@0.10.0
                 → cargo xtask dmg --universal --arm64 ... --x86_64 ...
                 → git-cliff --latest -o RELEASE_NOTES.md  (uses cliff.toml from Plan 01-04)
                 → gh release create $TAG --title "Vector $TAG" --notes-file RELEASE_NOTES.md target/dmg/*.dmg

README.md structure from 01-UI-SPEC.md §"README install block":
  # Vector
  Fast native macOS terminal with first-class GitHub Codespaces and Dev Tunnels support.
  ## Install
  1. Download the latest Vector-{version}-universal.dmg
  2. Open DMG, drag to /Applications
  3. xattr command in fenced sh
  4. Open Vector

ADR template (MADR 4.0):
  # {NN}. {Title}
  - Status: accepted
  - Date: 2026-05-10
  - Deciders: solo (the user)
  - Tags: {phase-1, build, threading, etc.}
  ## Context and Problem Statement
  ## Decision Drivers
  ## Considered Options
  ## Decision Outcome
  ## Pros and Cons of the Options
  ## Consequences
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: release.yml + README install block + initial CHANGELOG</name>
  <files>.github/workflows/release.yml, README.md, CHANGELOG.md</files>
  <read_first>
    - 01-CONTEXT.md D-18 (auto-publish on tag), D-21 (matrix; AMENDED to macos-15-intel), D-22 (cargo xtask), D-26 (xattr in 3 places), D-27 (CalVer), D-29 (CHANGELOG via git-cliff)
    - 01-RESEARCH.md §"Release workflow `.github/workflows/release.yml`" — verbatim YAML (encodes the macos-15-intel amendment)
    - 01-UI-SPEC.md §"README install block" — exact markdown body
    - 01-UI-SPEC.md §"GitHub Release body template" — exact markdown body
    - existing /home/colligo/vector/README.md (single line; this plan REPLACES with full install block)
  </read_first>
  <action>
**1. Create `.github/workflows/release.yml`** verbatim from 01-RESEARCH.md
§"Release workflow", with the macos-15-intel amendment baked in:

```yaml
name: release

on:
  push:
    tags: ['v*']

env:
  MACOSX_DEPLOYMENT_TARGET: "13.0"
  CARGO_HUSKY_DONT_INSTALL_HOOKS: "1"
  RUST_BACKTRACE: short

jobs:
  build-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          targets: aarch64-apple-darwin
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: rel-arm64
      - run: cargo build --release --target aarch64-apple-darwin -p vector-app
      - uses: actions/upload-artifact@v4
        with:
          name: vector-aarch64
          path: target/aarch64-apple-darwin/release/vector-app

  build-x86_64:
    runs-on: macos-15-intel    # AMENDMENT to D-21
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          targets: x86_64-apple-darwin
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: rel-x86_64
      - run: cargo build --release --target x86_64-apple-darwin -p vector-app
      - uses: actions/upload-artifact@v4
        with:
          name: vector-x86_64
          path: target/x86_64-apple-darwin/release/vector-app

  release:
    needs: [build-arm64, build-x86_64]
    runs-on: macos-14
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          targets: aarch64-apple-darwin
      - uses: actions/download-artifact@v4
        with:
          name: vector-aarch64
          path: artifacts/aarch64
      - uses: actions/download-artifact@v4
        with:
          name: vector-x86_64
          path: artifacts/x86_64
      - name: Make per-arch binaries executable
        run: |
          chmod +x artifacts/aarch64/vector-app
          chmod +x artifacts/x86_64/vector-app
      - name: Pitfall-3 guard — verify per-arch Mach-O before merge
        run: |
          set -e
          file artifacts/aarch64/vector-app | grep -q 'arm64'
          file artifacts/x86_64/vector-app | grep -q 'x86_64'
      - name: Install build tools
        run: |
          brew install create-dmg librsvg git-cliff
          cargo install cargo-bundle@0.10.0 --locked
      - name: Build Universal DMG
        run: |
          cargo xtask dmg --universal \
            --arm64 artifacts/aarch64/vector-app \
            --x86_64 artifacts/x86_64/vector-app
      - name: Pitfall-3 guard — verify bundled Mach-O is fat
        run: |
          set -e
          BIN="target/release/bundle/osx/Vector.app/Contents/MacOS/vector-app"
          INFO=$(lipo -info "$BIN")
          echo "$INFO"
          echo "$INFO" | grep -q 'x86_64'
          echo "$INFO" | grep -q 'arm64'
      - name: Generate release notes
        run: git-cliff --latest -o RELEASE_NOTES.md
      - name: Verify release notes are non-empty
        run: |
          if [ ! -s RELEASE_NOTES.md ]; then
            echo "::error::git-cliff produced empty RELEASE_NOTES.md"
            exit 1
          fi
      - name: Append xattr install footer to release notes
        run: |
          cat >> RELEASE_NOTES.md <<'EOF'

## Install

Download `Vector-${{ github.ref_name }}-universal.dmg` from this release page, open it, and drag `Vector.app` to `/Applications`.

The first time you launch, macOS will block the unsigned app. Run this once:

```sh
xattr -dr com.apple.quarantine /Applications/Vector.app
```

Built from commit `${{ github.sha }}` on macOS 13+ (Ventura). Apple Silicon and Intel both supported (Universal binary).
EOF
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "Vector ${{ github.ref_name }}" \
            --notes-file RELEASE_NOTES.md \
            target/dmg/Vector-*-universal.dmg
```

Key encoded decisions:
- Trigger: `on: push: tags: ['v*']` (D-18).
- macos-15-intel for x86_64 (AMENDMENT to D-21; matches Plan 01-05 ci.yml).
- xattr instruction in the Release body (D-26 place 3 of 3 — Plan 01-05's tip body was place 1; README is place 2; tagged release body is place 3).
- `git-cliff --latest -o RELEASE_NOTES.md` produces the version section; we append the install footer so xattr is present even if cliff produces an empty body for a no-changes tag.
- Pitfall-3 guards both before merge (per-arch) and after (bundled).
- `permissions: contents: write` for `gh release create`.

Note: the DMG filename in the release matches `Vector-{version}-universal.dmg`
(D-28 — no -tip- suffix on tagged releases). xtask's release flow already
produces this filename.

**2. Replace `README.md`** verbatim from 01-UI-SPEC.md §"README install block":

```markdown
# Vector

Fast native macOS terminal with first-class GitHub Codespaces and Dev Tunnels support.

## Install

1. Download the latest `Vector-{version}-universal.dmg` from [GitHub Releases](https://github.com/colligo/vector/releases/latest).
2. Open the DMG and drag `Vector.app` to `/Applications`.
3. The first time you launch, macOS will block the unsigned app. Run this once in Terminal:

```sh
xattr -dr com.apple.quarantine /Applications/Vector.app
```

4. Open Vector from `/Applications` (or Launchpad). You should see a window titled `Vector` with a small build identifier in the bottom-right corner.

## Status

Phase 1 (Foundation & CI/DMG Pipeline) — early bootstrap. Phases 2–10 fill in
the terminal core, GPU renderer, mux, polish, GitHub auth, Codespaces SSH,
Dev Tunnels, persistence, and hardening. See `.planning/ROADMAP.md`.

## Build from source

Requires Rust 1.88.0+ (auto-pinned via `rust-toolchain.toml`) and Xcode CLI tools.

```sh
brew install create-dmg librsvg git-cliff
cargo install cargo-bundle@0.10.0
cargo xtask dmg --universal     # produces target/dmg/Vector-{version}-universal.dmg
```

## License

MIT. See `LICENSE`.
```

Rules per UI-SPEC: xattr line in fenced `sh` block, byte-identical to the DMG
background and Release body. NO badges above the H1 (Phase 1 is too early).
"unsigned app" appears explicitly so the xattr step doesn't feel arbitrary.

**3. Create initial `CHANGELOG.md`** with the Keep a Changelog header that
matches `cliff.toml`'s template (so the first `cargo xtask release` invocation
populates it cleanly):

```markdown
# Changelog

All notable changes to Vector are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/);
versions follow CalVer (`YYYY.MM.DD`).

## [Unreleased]

(populated by `cargo xtask release` via git-cliff on the next tagged release)
```

git-cliff appends version sections above this on each release.
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml && grep -q "tags: \['v\*'\]" .github/workflows/release.yml && grep -q 'macos-15-intel' .github/workflows/release.yml && ! grep -q 'macos-13' .github/workflows/release.yml && grep -q 'cargo xtask dmg --universal' .github/workflows/release.yml && grep -q 'xattr -dr com.apple.quarantine /Applications/Vector.app' .github/workflows/release.yml && grep -q 'git-cliff --latest -o RELEASE_NOTES.md' .github/workflows/release.yml && grep -q 'gh release create' .github/workflows/release.yml && grep -q 'lipo -info' .github/workflows/release.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" && test -f README.md && grep -q '# Vector' README.md && grep -q 'xattr -dr com.apple.quarantine /Applications/Vector.app' README.md && grep -B1 'xattr -dr com.apple.quarantine' README.md | grep -q '```sh' && wc -l README.md | awk '{print $1}' | xargs -I{} test {} -gt 10 && test -f CHANGELOG.md && grep -q '# Changelog' CHANGELOG.md && grep -q 'Keep a Changelog' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/release.yml` exists; YAML parses (`python3 -c "import yaml; yaml.safe_load(...)"`)
    - `release.yml` triggers on `tags: ['v*']`
    - `release.yml` uses `macos-15-intel` for x86_64 (NOT `macos-13`)
    - `release.yml` invokes `cargo xtask dmg --universal --arm64 ... --x86_64 ...`
    - `release.yml` invokes `git-cliff --latest -o RELEASE_NOTES.md`
    - `release.yml` appends the xattr footer (literal `xattr -dr com.apple.quarantine /Applications/Vector.app`) to RELEASE_NOTES.md before `gh release create`
    - `release.yml` contains the same Pitfall-3 guards as ci.yml (per-arch + bundled lipo -info checks)
    - `release.yml` runs `gh release create "${{ github.ref_name }}" --title "Vector ${{ github.ref_name }}" --notes-file RELEASE_NOTES.md target/dmg/Vector-*-universal.dmg`
    - `release.yml` has `permissions: contents: write` on the release job
    - `README.md` is no longer a single line — at least 20 lines
    - `README.md` contains the xattr line `xattr -dr com.apple.quarantine /Applications/Vector.app` inside a fenced `sh` code block (verify with grep that the line above the xattr line is exactly `` ```sh ``)
    - `README.md` mentions `unsigned app` explicitly
    - `README.md` does NOT contain status badges above the H1 (`grep -B0 'Vector' README.md | head -1` returns the H1 directly, no img/shield content above)
    - `CHANGELOG.md` exists with `# Changelog` header and `Keep a Changelog` reference
    - `CHANGELOG.md` contains an `## [Unreleased]` section
  </acceptance_criteria>
  <done>release.yml committed; README is the install + build guide with xattr in fenced sh; CHANGELOG.md scaffolded; xattr instruction now present in README + Release body (D-26 places 2 and 3); DMG background image (Plan 01-04) is place 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ADRs 0001..0006 (MADR template) + docs/setup.md</name>
  <files>
    docs/adr/0001-rust-workspace-layout.md,
    docs/adr/0002-winit-tokio-threading.md,
    docs/adr/0003-architecture-lint-mechanism.md,
    docs/adr/0004-dmg-pipeline.md,
    docs/adr/0005-versioning-calver.md,
    docs/adr/0006-runner-labels-and-branch-protection.md,
    docs/setup.md
  </files>
  <read_first>
    - 01-CONTEXT.md D-33 (5 ADRs minimum: 0001 workspace layout, 0002 winit-tokio threading, 0003 architecture-lint mechanism, 0004 dmg pipeline, 0005 versioning-calver)
    - 01-CONTEXT.md D-21, D-34, D-35 (the 6th ADR captures the macos-13→macos-15-intel runner amendment AND the branch protection setup)
    - 01-RESEARCH.md §"Constraint Drift" — the macos-15-intel amendment narrative
    - 01-RESEARCH.md §"Open Questions → Q2" — branch protection automation choice (manual via GitHub UI per RESEARCH recommendation)
    - 01-VALIDATION.md §"Manual-Only Verifications" row "GitHub branch protection rules active on main"
    - https://adr.github.io/madr/ (MADR template — the 6 sections enumerated in <interfaces>)
  </read_first>
  <action>
**ADR length rule (per CLAUDE.md "succinct comments"):** Each ADR is ~30–60
lines. Sections are short: 1–3 sentences each, NOT multi-paragraph essays.
The "Decision Outcome" section names the choice and references the locked
CONTEXT.md decision IDs (D-XX) by number — that IS the trace.

**Create `docs/adr/0001-rust-workspace-layout.md`:**

```markdown
# 0001. Rust workspace layout

- Status: accepted
- Date: 2026-05-10
- Deciders: solo (user)
- Tags: phase-1, build, structure

## Context and Problem Statement

Vector ships from a single repo. We need a structure that scales to ~14 logical
units (terminal core, renderer, mux, transports, config, secrets, fonts, input,
theme, app shell) without making `cargo build` glacial or hiding architectural
boundaries.

## Decision Drivers

- Incremental compile speed (touching one crate must not rebuild all)
- Architectural visibility (folder structure mirrors the layered diagram)
- Single source of truth for dep versions (no russh-style dual-version drift)
- xtask separate from main resolver graph

## Considered Options

- Single-crate workspace with module layout
- 14-crate workspace with shared `[workspace.dependencies]`
- 14-crate workspace plus xtask in the same workspace
- 14-crate workspace plus xtask as a SEPARATE workspace

## Decision Outcome

14-crate workspace + xtask SEPARATE workspace, per D-01..D-04. Workspace deps
pinned once at the root in `[workspace.dependencies]` (D-02); each crate uses
`dep.workspace = true`. xtask lives at `xtask/` with its own `Cargo.toml` and
`Cargo.lock`, invoked via `.cargo/config.toml` alias.

## Consequences

- Adding a new crate = `mkdir crates/vector-foo + Cargo.toml + src/lib.rs +
  tests/no_tokio_main.rs` + listing in `Cargo.toml [workspace.members]`.
- xtask deps don't pollute the main resolver — fast incremental builds.
- Single dep bump propagates to all 14 crates atomically.
- `cargo deny` audits the main workspace only; xtask deps are unaudited (accepted).
```

**Create `docs/adr/0002-winit-tokio-threading.md`:**

```markdown
# 0002. winit/tokio main-thread ownership

- Status: accepted
- Date: 2026-05-10
- Deciders: solo (user)
- Tags: phase-1, threading, win-05, pitfall-5

## Context and Problem Statement

macOS forces the AppKit event loop on the main thread. Async I/O (PTY, SSH,
HTTPS, OAuth, port-forward) is tokio-native. Mixing them naively (spawning
tokio on the main thread, or wrapping winit inside `#[tokio::main]`) causes
deadlocks and sporadic AppKit crashes that surface only under load (Phase 3
renderer, Phase 9 reconnect).

## Decision Drivers

- macOS hard requirement: AppKit on main thread
- Tokio runtime is multi-thread by default; we want that for I/O fan-out
- Cross-thread signal must be safe + cheap
- Future-self protection: regression must fail the build, not just lint

## Considered Options

- `#[tokio::main]` on `vector-app::main` (rejected — pulls tokio onto main,
  fights winit)
- `OnceCell<Runtime>` global (rejected — every async caller has to know about
  it; unclear ownership)
- Per-subsystem current-thread runtimes (rejected — no shared executor; type
  interop nightmare)
- winit on main + dedicated I/O thread spawning multi-thread tokio runtime,
  EventLoopProxy::send_event as the sole cross-thread channel

## Decision Outcome

winit on main + dedicated I/O thread, per D-08..D-11. `vector-app::main`
spawns a `std::thread` named `tokio-io`; that thread builds
`tokio::runtime::Builder::new_multi_thread().enable_all().build()` and calls
`rt.block_on(io_main(proxy))`. The main thread runs `event_loop.run_app(...)`.
Cross-thread signal: `EventLoopProxy::send_event(UserEvent)`.

## Consequences

- One `block_on(...)` in the entire codebase — at the bottom of the I/O
  thread. Allowlisted by the architecture-lint test (ADR 0003).
- A 500ms `Tick(n)` smoke test (D-10) proves the channel works under real
  AppKit + winit + tokio at startup.
- `clippy::await_holding_lock = "deny"` (D-11) prevents the related anti-pattern
  (Mutex held across `.await`) at the workspace lint level.
- Phase 3 renderer plugs in without re-architecting the threading model.
```

**Create `docs/adr/0003-architecture-lint-mechanism.md`:**

```markdown
# 0003. Architecture-lint mechanism

- Status: accepted
- Date: 2026-05-10
- Deciders: solo (user)
- Tags: phase-1, ci, lint, win-05

## Context and Problem Statement

Decision 0002's threading rule must survive future contributors and future-self
under deadline pressure. A clippy lint can be silenced; an ADR can be ignored.
We need a structural enforcement that fails the build on regression.

## Decision Drivers

- Belt-and-braces: test in code + grep redundancy in CI
- Per-crate (not per-workspace) so xtask can legitimately use `block_on`
- Test failure surfaces within `cargo test` — same place all other failures live

## Considered Options

- Workspace-wide grep in a CI step only (rejected — no local feedback)
- Cargo lint plugin (rejected — heavy machinery for a 50-line check)
- Per-crate `tests/no_tokio_main.rs` integration test + CI grep redundancy

## Decision Outcome

Per-crate `tests/no_tokio_main.rs`, per D-08. Each of the 14 crates carries a
copy of the test that scans its own `src/` for `#[tokio::main]`,
`#[tokio::test]`, `Builder::new_current_thread()`, `Runtime::new()`, and
`block_on(` (allowlist: `vector-app/src/main.rs`). CI workflow's `test` job
also runs an `rg`-based grep step over `crates/**/*.rs` (excluding the test
files themselves) — same forbid list — as redundancy if a future crate is
added without copying the test.

## Consequences

- New crate added without copying the test → CI `test` job's file-count guard
  fails (Plan 01-05 ci.yml step "Architecture-lint per-crate test file count").
- Local dev sees the violation in `cargo test --workspace` within ~10s.
- xtask is in a separate workspace and is NOT touched by `cargo test
  --workspace`; xtask is ALLOWED to use `block_on` (it's a build tool, not
  the running app).
```

**Create `docs/adr/0004-dmg-pipeline.md`:**

```markdown
# 0004. DMG packaging pipeline

- Status: accepted
- Date: 2026-05-10
- Deciders: solo (user)
- Tags: phase-1, build, dmg, build-02, build-03, build-04, build-05

## Context and Problem Statement

Vector ships unsigned via `.dmg` for v1 (signing deferred to v2). We need a
DMG produced identically by CI and by a local `cargo xtask dmg` invocation
(per D-22), in both Universal (CI / tagged release) and host-arch-only
(local dev) modes.

## Decision Drivers

- BUILD-03 — local DMG must equal CI DMG bytewise per the same code path
- BUILD-02 — Universal binary on every push to main
- BUILD-04 — tagged releases auto-publish; tip + tagged pattern (ghostty model)
- BUILD-05 — xattr instruction in three places (D-26)
- Don't hand-roll: cargo-bundle for `.app`, create-dmg for styled DMG

## Considered Options

- Hand-rolled `mkdir Vector.app/Contents/{MacOS,Resources}` + Info.plist
- `tauri-bundler` (rejected — Tauri-shaped, webview deps)
- `cargo-bundle 0.10` + `create-dmg` shell script (chosen)

## Decision Outcome

Per D-25, D-22, D-17, D-18, D-19, D-26. Pipeline:
1. CI matrix builds per-arch binaries (`macos-14` for arm64, `macos-15-intel`
   for x86_64; ADR 0006 covers the runner-label amendment).
2. `package` (CI) or local `cargo xtask dmg --universal` runs `lipo -create`
   to merge per-arch binaries; Pitfall-3 guards verify the result is fat.
3. cargo-bundle reads the merged binary at `target/release/vector-app` (Wave-0
   spike confirmed this works) and produces `Vector.app`.
4. `iconutil` generates `.icns` from `icon.svg` via `rsvg-convert`.
5. `create-dmg` wraps `Vector.app` into a styled DMG with the xattr
   instruction rasterized into the background.
6. Tip pushes overwrite a pinned `tip` GitHub Release; tagged pushes (`v*`)
   create a permanent Release with `git-cliff`-generated notes + xattr footer.

## Consequences

- DMG byte-identity local↔CI: only true if the same xtask version, the same
  `cargo-bundle@0.10.0`, the same `librsvg` rsvg-convert version, and the
  same git SHA are used. We accept "byte-identical content; possibly different
  metadata timestamps" as the practical guarantee.
- The xattr line appears in: README install block, DMG background image, tip
  release body (Plan 01-05), tagged release body (this plan). Four places —
  one more than D-26's three for added discoverability; harmless redundancy.
- Wave-0 spike (Plan 01-04 SUMMARY) documented the cargo-bundle universal-binary
  path behavior — fallback path (cargo-bundle --bin + post-process) is on the
  bench if cargo-bundle ever regresses.
```

**Create `docs/adr/0005-versioning-calver.md`:**

```markdown
# 0005. Versioning, CHANGELOG, and Conventional Commits

- Status: accepted
- Date: 2026-05-10
- Deciders: solo (user)
- Tags: phase-1, release, ci, dx

## Context and Problem Statement

Vector ships from a solo dev to a small audience (the user + a few Adobe
teammates). We need a version scheme that's calendar-meaningful, an automated
CHANGELOG generator, and commit-message structure that supports both.

## Decision Drivers

- CalVer reads as "when shipped" (matters for an unsigned dev tool)
- CHANGELOG hand-editing is a tax we don't want
- Conventional Commits → automated CHANGELOG via git-cliff
- CI must enforce commit format, otherwise the automation breaks silently

## Considered Options

- SemVer + manual CHANGELOG (rejected — premature semver theater for a v1)
- CalVer `YYYY.MM.DD` (chosen)
- CalVer with same-day -N suffix (rejected — D-27, one release per day)
- commitlint (Node) vs convco (Rust binary)

## Decision Outcome

Per D-27, D-29, D-30. CalVer `YYYY.MM.DD` set in `[workspace.package].version`;
`cargo xtask release` bumps it to today's date and tags `v{version}`.
git-cliff (cliff.toml) generates Keep-a-Changelog-format sections from
Conventional Commits. CI enforces commit format with `convco` (single Rust
binary; matches our Rust-only stance — see RESEARCH.md §"Conventional Commits").

## Consequences

- Same-day re-releases are not supported (a second `cargo xtask release`
  fails because the tag exists). Acceptable for v1 cadence.
- The 11 commit-type → CHANGELOG-section mappings live in `cliff.toml`;
  unconventional commits don't break the build (filter_unconventional = false)
  but won't appear in CHANGELOG sections.
- `convco` is downloaded from GitHub Releases in CI (~5s); avoids dragging Node
  into the toolchain.
```

**Create `docs/adr/0006-runner-labels-and-branch-protection.md`:**

```markdown
# 0006. CI runner labels + main branch protection

- Status: accepted
- Date: 2026-05-10
- Deciders: solo (user)
- Tags: phase-1, ci, gating, security

## Context and Problem Statement

(a) D-21 of CONTEXT.md specified `macos-13` as the x86_64 runner. That
runner image was retired by GitHub on 2025-12-04 (per RESEARCH.md §Constraint
Drift). We need a documented amendment.

(b) D-34 + D-35 specify branch protection on `main`: required status checks,
linear history, force-push disabled, PR review required (with 0 reviewers
for now). GitHub branch protection is configured in the GitHub UI / API,
not in repo files. We need an audit trail.

## Decision Drivers

- Use a runner image that's actually available
- Document the EOL window so we have advance warning before the next change
- Make the branch protection state inspectable + reproducible

## Considered Options

- Runner: `macos-13` (rejected — retired)
- Runner: `cargo-zigbuild` cross-compile on `macos-14` (rejected — extra
  complexity; we don't need it because `macos-15-intel` exists)
- Runner: `macos-15-intel` (chosen — Intel macOS 15, available until Aug 2027)
- Branch protection via probot/settings (rejected — extra GitHub App install)
- Branch protection via GitHub UI manual setup, documented in `docs/setup.md`

## Decision Outcome

(a) Runner: `macos-15-intel` for x86_64 builds in both `ci.yml` and
`release.yml`. The `cargo-zigbuild` fallback is no longer needed for v1.
**EOL warning: macos-15-intel is GitHub's last Intel runner, available until
August 2027.** Set a calendar reminder in 2027 Q1 to revisit.

(b) Branch protection configured via GitHub UI per `docs/setup.md`. Required
status checks: `lint`, `commitlint`, `test`, `deny`. Linear history required.
Force-push disabled on `main`. PR review = 0 reviewers (the gate exists; just
not requiring approvals while solo). Universal-DMG build is NOT a required
check (too slow for the PR feedback loop, per D-34).

## Consequences

- A future `macos-15-intel` retirement (post-Aug 2027) needs a migration plan.
  `cargo-zigbuild` on `macos-14` is the documented v2 fallback.
- Branch protection isn't enforced by source control; periodic audit via
  `gh api repos/{owner}/vector/branches/main/protection` is the only way to
  verify it stayed configured. Add an entry to a future `docs/release-checklist.md`.
- Commit signing is NOT required in v1 (solo dev). A v2 ADR will add it.
```

**Create `docs/setup.md`** — manual one-time configuration steps for the user:

```markdown
# Setup — One-time manual configuration

Run these once on a fresh checkout.

## 1. Local development tools

```sh
# Xcode CLI (required for lipo, iconutil, hdiutil, codesign, file)
xcode-select --install

# Build tools used by xtask
brew install create-dmg librsvg git-cliff

# Cargo subcommands
cargo install cargo-bundle@0.10.0 --locked
cargo install cargo-deny --locked
```

## 2. Rust toolchain

`rust-toolchain.toml` pins channel `1.88.0` and both Apple Darwin targets.
`rustup` will auto-install on first `cargo build`. Verify:

```sh
rustup show
# active toolchain: 1.88.0-aarch64-apple-darwin (or x86_64)
```

## 3. GitHub branch protection (one-time per repo)

D-34 / D-35 require this. Configure in the GitHub UI at
`Settings → Branches → Branch protection rules → Add rule` for `main`:

- ☑ Require a pull request before merging
  - Required approving reviews: **0** (gate exists; solo dev, not blocking)
  - ☑ Dismiss stale pull request approvals when new commits are pushed
- ☑ Require status checks to pass before merging
  - ☑ Require branches to be up to date before merging
  - Required status checks (must match the job names in `.github/workflows/ci.yml`):
    - `lint`
    - `commitlint`
    - `test`
    - `deny`
  - (Universal-DMG build is intentionally NOT a required check — too slow for
    PR feedback loop per D-34.)
- ☑ Require linear history
- ☑ Do not allow force pushes
- ☐ Allow deletions (off — protect the branch)
- ☐ Require deployments to succeed (not used)
- ☐ Require signed commits (deferred to v2)

Verify via the API:

```sh
gh api repos/colligo/vector/branches/main/protection
```

Should report `required_status_checks.contexts: [lint, commitlint, test, deny]`,
`required_linear_history: { enabled: true }`, `allow_force_pushes: { enabled: false }`.

## 4. Git hooks (cargo-husky auto-installs)

`cargo-husky` installs `.git/hooks/pre-commit` on first `cargo build`. The hook
runs `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings`. To
opt out (rare): set `CARGO_HUSKY_DONT_INSTALL_HOOKS=1` in your shell.

## 5. First build + DMG

```sh
cargo build --workspace
cargo test --workspace --tests
cargo deny check
cargo xtask dmg              # host-arch-only DMG, ~30s
cargo xtask dmg --universal  # Universal DMG, ~2 min cold
```

The output DMG is at `target/dmg/Vector-{version}-universal.dmg`.

## 6. First release (when ready to ship a tag)

```sh
cargo xtask release    # bumps CalVer, runs git-cliff, commits, tags v{date}
git push --follow-tags # triggers .github/workflows/release.yml
```

CI will publish the Universal DMG to GitHub Releases.
```
  </action>
  <verify>
    <automated>for f in 0001-rust-workspace-layout 0002-winit-tokio-threading 0003-architecture-lint-mechanism 0004-dmg-pipeline 0005-versioning-calver 0006-runner-labels-and-branch-protection; do test -f "docs/adr/$f.md" || { echo "missing $f"; exit 1; }; done && for f in docs/adr/0001-*.md docs/adr/0002-*.md docs/adr/0003-*.md docs/adr/0004-*.md docs/adr/0005-*.md docs/adr/0006-*.md; do grep -q '## Context and Problem Statement' "$f" && grep -q '## Decision Drivers' "$f" && grep -q '## Considered Options' "$f" && grep -q '## Decision Outcome' "$f" && grep -q '## Consequences' "$f" || { echo "MADR sections missing in $f"; exit 1; }; done && grep -q 'D-08\|D-09\|D-10\|D-11' docs/adr/0002-winit-tokio-threading.md && grep -q 'D-21' docs/adr/0006-runner-labels-and-branch-protection.md && grep -q 'macos-15-intel' docs/adr/0006-runner-labels-and-branch-protection.md && grep -q 'August 2027\|Aug 2027' docs/adr/0006-runner-labels-and-branch-protection.md && test -f docs/setup.md && grep -q 'gh api repos/colligo/vector/branches/main/protection' docs/setup.md && grep -q 'required_linear_history' docs/setup.md && grep -q 'lint' docs/setup.md && grep -q 'commitlint' docs/setup.md && grep -q 'test' docs/setup.md && grep -q 'deny' docs/setup.md && grep -q 'cargo install cargo-bundle@0.10.0' docs/setup.md && grep -q 'brew install create-dmg librsvg' docs/setup.md</automated>
  </verify>
  <acceptance_criteria>
    - Six ADR files exist under `docs/adr/`: `0001-rust-workspace-layout.md`, `0002-winit-tokio-threading.md`, `0003-architecture-lint-mechanism.md`, `0004-dmg-pipeline.md`, `0005-versioning-calver.md`, `0006-runner-labels-and-branch-protection.md`
    - Each ADR contains all 5 MADR sections: `## Context and Problem Statement`, `## Decision Drivers`, `## Considered Options`, `## Decision Outcome`, `## Consequences`
    - Each ADR has a frontmatter-like header with `Status: accepted`, `Date: 2026-05-10`, `Deciders: solo (user)`, `Tags:`
    - ADR 0002 references decision IDs `D-08`, `D-09`, `D-10`, `D-11` by name
    - ADR 0006 references `D-21` (the amended runner label decision) and `D-34` + `D-35` (branch protection); contains the literal string `macos-15-intel`; mentions August/Aug 2027 EOL
    - Each ADR is succinct: ≤80 lines (per CLAUDE.md "succinct comments")
    - `docs/setup.md` exists with sections: local tools (xcode-select, brew, cargo install), Rust toolchain (rustup), GitHub branch protection (with gh api verification command), git hooks (cargo-husky), first build, first release
    - `docs/setup.md` contains `gh api repos/colligo/vector/branches/main/protection`
    - `docs/setup.md` lists the four required status check names in branch protection: `lint`, `commitlint`, `test`, `deny` (matching ci.yml job names)
    - `docs/setup.md` mentions `brew install create-dmg librsvg git-cliff` AND `cargo install cargo-bundle@0.10.0`
  </acceptance_criteria>
  <done>Six ADRs + docs/setup.md committed; all link back to CONTEXT.md decision IDs; branch protection setup is reproducible from the doc.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: configure GitHub branch protection per docs/setup.md</name>
  <what-built>The repo now contains release.yml, README, CHANGELOG, six ADRs, and docs/setup.md. The branch protection rules described in docs/setup.md MUST be configured in the GitHub UI to satisfy D-34 + D-35. This is the rare `checkpoint:human-action` because GitHub branch protection state is genuinely outside Claude's reach — it lives in GitHub's UI/API and requires a logged-in human with admin access to the repo.</what-built>
  <how-to-verify>
**Step 1: Configure branch protection.**

Open https://github.com/colligo/vector/settings/branches and follow the steps
in `docs/setup.md` §"3. GitHub branch protection". The required-status-check
names must match `.github/workflows/ci.yml` exactly: `lint`, `commitlint`,
`test`, `deny`.

**Step 2: Verify via gh CLI.**

```sh
gh api repos/colligo/vector/branches/main/protection
```

Expected JSON contains:
- `required_status_checks.contexts` includes `lint`, `commitlint`, `test`, `deny`
- `required_linear_history.enabled: true`
- `allow_force_pushes.enabled: false`
- `allow_deletions.enabled: false`

**Step 3: Cut the first tagged release (smoke test for release.yml).**

After branch protection is in place, run:

```sh
cargo xtask release
git push --follow-tags
```

This will:
1. Bump `[workspace.package].version` in `Cargo.toml` to today's CalVer date
2. Run git-cliff to populate CHANGELOG.md
3. Commit `chore(release): v2026.05.10` (or today's date)
4. Tag `v2026.05.10`
5. After your `git push --follow-tags`, GitHub Actions will trigger
   `.github/workflows/release.yml`

Watch the release run:

```sh
gh run watch
```

When complete:

```sh
gh release view v2026.05.10
```

Should show:
- A DMG asset named `Vector-2026.05.10-universal.dmg`
- A body that includes the `xattr -dr com.apple.quarantine /Applications/Vector.app` line in a fenced sh code block
- Built from the current commit SHA

**Step 4: Smoke-test the released DMG.**

Download the DMG from the Release page, mount it, drag Vector.app to
`/Applications`, run `xattr -dr com.apple.quarantine /Applications/Vector.app`,
and double-click. Confirm the same Plan-01-03 visual appears (window, ticking
title, version overlay, menu bar).

**If anything fails:**
- Document the failure in 01-06-SUMMARY.md.
- Common known issues: the `convco check HEAD~10..HEAD || true` tolerance lets
  early non-conventional commits pass; if a tagged release fails because
  git-cliff produced an empty body, the workflow's "Verify release notes are
  non-empty" step fails — fix by ensuring at least one Conventional Commit
  lands between the previous tag and this one.

Type "approved" once branch protection is in place AND the first tagged
release publishes a downloadable DMG with the xattr instruction in the body.
  </how-to-verify>
  <resume-signal>Type "approved" once branch protection is configured per docs/setup.md AND the first `v*` tag publishes a DMG to GitHub Releases with the xattr instruction in the body.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API ↔ branch protection state | Branch protection rules live outside the repo. Mitigated by docs/setup.md providing a reproducible `gh api` verification command; periodic audit during phase transitions. |
| `git-cliff` template ↔ release body | Compromised cliff.toml could inject arbitrary text into the release body. Mitigated by review-before-merge; cliff.toml lives in repo and is part of the PR-review surface. |
| Release.yml `permissions: contents: write` ↔ GITHUB_TOKEN | Token scoped to write releases on this repo only; standard GitHub Actions practice. |
| Tagged-release DMG ↔ teammates downloading | Phase 1 ships unsigned. xattr instruction is the user-side trust handshake (D-26). v2 (DIST-V2-01 signing) closes the gap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31 | Tampering | branch protection silently disabled | mitigate | docs/setup.md verification command; phase-boundary audit (a `release-checklist.md` is a future ADR — call out for Phase 10). |
| T-01-32 | Tampering | unsigned DMG modified in transit | accept | GitHub Releases hosts assets over HTTPS; integrity is GitHub's responsibility. v2 signing closes the residual gap. |
| T-01-33 | Information Disclosure | RELEASE_NOTES.md content | accept | Public-by-design; content is git-cliff's CHANGELOG section + xattr footer; no credentials. |
| T-01-34 | Repudiation | unsigned tag points to a different commit than reviewed | mitigate | Branch protection requires linear history + force-push disabled; tagged-release commit SHA is logged in the release body via `${{ github.sha }}`. Commit signing is a v2 follow-up. |
| T-01-35 | DoS | accidental tag push triggers a release for an unintended state | mitigate | `cargo xtask release` is the documented release ritual (CalVer + git-cliff + commit + tag, no push). User reviews the diff before `git push --follow-tags`. Release.yml itself is idempotent — re-running on the same tag does nothing because `gh release create` errors on duplicate. |
| T-01-36 | Spoofing | someone pushes a v* tag without going through xtask release | accept | Solo dev; manual discipline. v2 might add a `[prepush]` hook that refuses to push v* tags created outside `cargo xtask release`. |
</threat_model>

<verification>
**Automated:**
- All 6 ADR files exist with all 5 MADR sections
- README contains xattr in fenced sh
- release.yml parses + uses macos-15-intel + invokes cargo xtask dmg + git-cliff + gh release create
- docs/setup.md references the 4 required status check names + the gh api verification

**Manual (checkpoint):**
- Branch protection configured in GitHub UI per docs/setup.md
- `gh api repos/colligo/vector/branches/main/protection` reports the expected JSON
- `cargo xtask release` + `git push --follow-tags` triggers release.yml and produces a tagged GitHub Release
- The Release body contains the xattr instruction line
- The Release DMG asset downloads, mounts, and Vector.app launches after `xattr -dr com.apple.quarantine`
</verification>

<success_criteria>
1. BUILD-04 (tagged-release half): `git push --follow-tags` for `v2026.05.10` produces a GitHub Release with the Universal DMG asset and the xattr instruction in the body.
2. BUILD-05: README contains the xattr instruction in a fenced sh block (one of the three D-26 surfaces; DMG bg from Plan 01-04 is another; tagged-release body from this plan is the third — D-26 satisfied).
3. ADRs 0001..0006 document D-01..D-35 architectural decisions in MADR format.
4. docs/setup.md captures the manual GitHub branch protection step + reproducible verification command.
5. Branch protection D-34 + D-35 in force on `main`: required status checks (lint, commitlint, test, deny), linear history, force-push disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ci-dmg-pipeline/01-06-SUMMARY.md`
documenting:
- The first tagged-release run URL and asset list
- Confirmation that `xattr` instruction is byte-identical across README, DMG bg, tip release body, tagged release body (4 places — D-26 satisfied)
- The `gh api` JSON output snippet showing branch protection in force
- Total Phase 1 duration (commit SHAs at start and end)
- A note that Phase 1 is now COMPLETE — Phase 2 (Headless Terminal Core) inherits: 14-crate workspace, threading skeleton, architecture-lint pattern, CI infrastructure, ADR practice
</output>
